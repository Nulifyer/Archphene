pkgname=wcurl
pkgver=0.0
pkgrel=1
pkgdesc='Archphene package replacement device-test baseline'
arch=(any)
license=(MIT)
conflicts=('curl')

package() {
  install -Dm644 /dev/null "$pkgdir/usr/share/archphene-test/wcurl-baseline"
}
