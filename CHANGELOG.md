# Changelog

Notable user-facing changes will be recorded here.

## Unreleased

### Added

- Added an executable standalone-script device-mutation policy. It currently enforces explicit or exactly restored behavior across app-data resets, optional APK installation, device reboot, and permission/settings writes, and fails if its coverage unexpectedly shrinks.
- Added recovery-safe Linux-to-Android Export progress and cancellation. Rust reports completed chunks without moving file bytes through JNI, the Files page shows byte/percent progress and a temporary Cancel action, and cancellation removes the incomplete destination. A persistable write grant plus durable running record lets startup delete a real nonempty partial output after manager process death; unsupported providers fail before mutation. Exact cancellation, recovery, normal completion/restart, fatal-log, and visually inspected full-device gates pass on the emulator and Samsung.
- Added bounded Linux-to-Android multi-file Share. DocumentsUI can select up to 32 regular Archphene Home files; the manager deduplicates exact URIs, negotiates exact/same-family/general MIME types, and sends one read-only `ACTION_SEND_MULTIPLE` clip while excluding its own import loop. MIME policy unit tests, exact two-URI grants, single-file regression, fatal logs, cleanup, and visually inspected full-device selection/chooser gates pass on the x86_64 emulator and AArch64 Samsung.
- Added single-file Linux-to-Android Export. The Files page selects a regular Archphene Home source, then Android's `ACTION_CREATE_DOCUMENT` picker chooses the destination and filename. Rust copies the two Android-owned descriptors through a fixed 32 KiB buffer with a 16 GiB limit, so file bytes do not cross Kotlin/JNI. Cancellation, retry, exact destination bytes, unchanged source, durable completion, cleanup, StrictMode/fatal logs, Open/Share regression, and visually inspected phone/wide gates pass on both maintained devices.
- Added direct Android opening for one Linux-owned document. The Files page now selects a bounded regular file from Archphene Home and offers its resolved MIME type through Android's `ACTION_VIEW` chooser with a read-only URI grant, while excluding Archphene itself to prevent a self-import loop. Exact URI/MIME/grant, Share-regression, phone/tablet layout, fatal-log, cleanup, and visually inspected full-device gates pass on the x86_64 emulator and AArch64 Samsung.
- Added a repeated-window sustained-performance gate for the shared Terminal and generated native-Wayland launchers. Its default protocol runs two active and two idle minutes per surface while enforcing per-window ART allocation/object/GC, JNI/copy, input latency, memory, frame, thread, descriptor, process, thermal, and battery ceilings; it writes complete JSON and raw environmental evidence plus full-device screenshots. The exact AArch64 Samsung and x86_64 emulator runs pass with zero GC and zero peak descriptor/thread growth. The gate exposed an emulator-only `sync_file` leak in the legacy CPU native-window post path, so launcher presentation now uses three preallocated `AHardwareBuffer`s with bounded resize generations, `ASurfaceTransaction`, nonblocking reuse, and explicit acquire/release-fence ownership.
- Added exact active-session performance accounting for the shared Terminal and generated Linux launchers. A debug-only authenticated probe measures categorized JNI calls, direct-buffer and explicit Kotlin copy bytes, ART allocations/objects/GC, hardware-input-to-draw/present latency, PSS/RSS, heaps, frames, threads, descriptors, and active UID processes. The budgeted exact-ABI gate writes JSON, compares real full-device frames, and passes on the emulator and Samsung with zero GC. The audit also replaced 32 scalar presentation JNI queries with one direct-buffer snapshot and event flags, reducing the final Samsung Foot transaction from 1,875 calls to 144 without reducing the 8 ms compositor cadence.
- Migrated the complete Foot device workflow from retired per-wrapper debug intents to the manager-owned shared-root/session architecture. A debug-only manager boundary drives bounded Unicode IME while production launchers exercise their real Surface/Binder input, clipboard worker, pointer selection, scrollback, resize, close, cleanup, and relaunch paths. Exact-ABI emulator and Samsung runs pass with inspected full-device captures, and wrapper mouse drags now remain continuous when move events omit `buttonState`.
- Preserved the distinction between absent and empty IME operations at the debug manager-session boundary. Successive Japanese preedit candidates now replace in place without an accidental empty commit clearing them, while exact Japanese/CJK/emoji-modifier/ZWJ commits reach Foot and Bash unchanged. The complete generated-Foot workflow passes again on the exact x86_64 emulator and AArch64 Samsung with inspected full-device captures.
- Made generated-launcher recovery explicit and forward-only after manager registry or signing-state loss. Trusted higher-version wrappers are republished above Android's installed version instead of attempting a forbidden downgrade; untrusted deterministic-package blockers remain visible as retryable failures, survive catalog refresh, and move through Android-confirmed uninstall before a newly signed wrapper is published. A real stale Foot identity completed the full uninstall/install recovery on the emulator.
- Added debug-only StrictMode diagnostics and state-preserving exact-device main-thread I/O, runtime-shutdown, and launcher-thread gates. Manager navigation, Terminal text, Linux appearance, preferred-shell, and storage-onboarding persistence now use one serialized worker and immutable in-memory snapshots where needed; native loading and stale AUR cleanup moved into bootstrap. Service teardown invalidates public handles immediately and performs connection cancellation, tracked-worker draining, synchronization cleanup, lifecycle transitions, and Rust destruction on `ArchpheneShutdown`. Manager-side compositor state/JNI calls are confined to their creating Surface thread while blocking clipboard descriptor I/O is forbidden there. Cold start, preference write/restore/process-death persistence, real package search, dependency resolution, prompt Activity teardown, current generated Foot launch/input/frame/clipboard behavior, fatal logs, and full-device captures pass without an Archphene-owned violation on the x86_64 emulator and AArch64 Samsung.
- Added a state-preserving device performance gate with explicit budgets and JSON evidence for three cold and three retained-process launches, end-to-end UI automation response, PSS/RSS, Java/native heaps, native allocations where exposed, frame p95/jank, views, threads, descriptors, idle children, and fatal logs. Current exact-ABI emulator and Samsung baselines pass with visually inspected full-device captures.
- Extracted runtime fork/exec, descriptor inheritance, process groups, signals, waits, and bounded child-output collection into a reviewed process boundary. The native Android probe now executes a real ABI-specific child by descriptor, verifies its exact output/exit status and invalid-FD rejection, and passes on the x86_64 emulator and AArch64 Samsung.
- Confined Android native-window and Bitmap FFI behind retained-window RAII and bounded locked-pixel views. Every format, dimension, stride, null pointer, and byte count is validated before a Rust slice exists, successful locks are paired with one unlock, and the compositor now denies unsafe code outside its three declared boundary modules.
- Confined compositor descriptor syscalls and Unix ancillary-data layouts to one reviewed wrapper module. Compositor client adoption now crosses from `ParcelFileDescriptor.detachFd()` directly into `OwnedFd`, received `SCM_RIGHTS` descriptors enter RAII immediately, and the complete host suite plus exact x86_64 emulator/AArch64 Samsung probes pass.
- Replaced compositor raw-pointer JNI handles with fixed synchronized slot/generation registries capped at eight core/probe instances and four generated launchers. Exhaustion, stale use, double destroy, and stale-after-reuse fail closed without per-call heap allocation; direct tests and the exact JNI-name Android probe pass on x86_64 and AArch64.
- Enclosed every exported compositor JNI symbol and its Java array/direct-buffer/surface/bitmap conversion in one documented boundary module, and cleared the workspace's strict Clippy findings without adding hot-path aggregates. Clippy with warnings denied, all Rust tests, and optimized x86_64/AArch64 Android compositor builds pass; the remaining syscall/native-window FFI split stays explicitly tracked.
- Added live project-synchronization progress that reports only real actions and distinguishes pushes to Android, pulls into Archphene, deletions on either side, and conflict preservation with running result counts. The complete transaction, cancellation, conflict, and forced-death recovery matrix plus visually inspected full-device progress frames pass on the exact x86_64 emulator and AArch64 Samsung.
- Tightened Rust unsafe-code enforcement: every production crate containing unsafe operations now denies implicit unsafe work inside unsafe functions, and the Builder, Android JNI, process, and storage crates deny unsafe outside their explicit boundary modules. Host checks are warning-free; the compositor still has a separately tracked module-confinement audit.
- Documented the production Android/Linux filesystem boundary, current backup/export routes, grant revocation, destructive uninstall consequences, and the exact compatibility limits of running one shared Arch userspace under Android's kernel, SELinux, seccomp, permissions, and lifecycle.
- Added a versioned generated-launcher capability contract for the production Wayland, input, IME, and clipboard bridge. Rust publishes the exact set, deterministic APK assembly embeds and reverifies it, wrappers fail closed with an actionable repair message before Binder connection, and manager reconciliation updates correctly signed older contracts through normal Android confirmation instead of quarantining them. Existing reviewed launchers migrated and the complete Foot lifecycle/crash gate passed on both exact ABIs.
- Added durable project-sync history and retry UX. Archphene retains a checksum-protected bounded 16-entry log with exact conflict paths, exposes it through a phone-safe History dialog, and changes Sync to Retry after a recorded failure or cancellation. Fixed protocol decoding, mutation journaling, cancellable SAF scanning, Android document verification, transaction ordering, and crash recovery are now cohesive components outside the runtime Service with 18 direct JVM protocol/corruption/recovery tests.
- Added a bounded SAF provider bridge for project mirrors. Read-side listings, metadata, and descriptor opens are cancellable and deadline-driven; a stalled document mutation terminates the manager at 30 seconds so its persisted journal or the next three-way scan recovers the ambiguous provider result.
- Extended project synchronization to recursive directory changes and serial crash-safe Android deletion checkpoints within one Sync. A separately locked native cancellation token now stops long Android/Linux fingerprints without partial publication, and forced-death replacement gates validate both rollback/retry and published-file finalization on physical AArch64 and exact x86_64.
- Added conflict-safe explicit synchronization to Android project mirrors. The initial Rust copy loop records a canonical SHA-256 ancestor under a stable random identity; later scans page a fixed binary three-way plan and transactionally apply regular-file additions, edits, pulls, pushes, and deletions without overwriting simultaneous changes. Android mutations use verified staging/backup renames plus a checksum-protected atomic journal; forced Samsung process death before and after commit proves rollback/replan and cleanup without resurrection.
- Added Android-network-backed private-root DNS and standard Arch CA trust materialization for the shared Linux environment. The checksum-pinned patched glibc runtimes reload the managed resolver inside an already-running process, and verified package maintenance runs the installed `update-ca-trust` adapter through a bounded virtual-root identity.
- Added a safe Shell startup files folder to Archphene Home in Android Files. It exposes only writable `.bashrc` and `.bash_profile` aliases through Rust no-follow opens while keeping arbitrary dotfiles private and preventing create/rename/delete operations. New and reused roots also receive `~/.local/bin` at the front of the shared Linux process PATH.
- Added explicit forward repair for interrupted official package transactions. Rust retains exact verified install inputs or a removal baseline before pacman mutation, refuses cache cleanup while those inputs are required, and clears the intent only after database and final-state validation. The manager distinguishes safe Retry from required Repair after process death.
- Added a Downloads inventory for the manager-owned pacman cache with total disk use, package/version grouping, selectable per-package cleanup, and a separately confirmed Clear all action. Cache inspection and deletion are bounded and fail closed in Rust, paged through JNI, and serialized with package work.

### Fixed

- Recursive AUR builds now checkpoint each independently verified package-base prefix instead of discarding all completed dependency work until the root base finishes. Restart and retry restore only complete dependency-first prefixes bound to the exact reviewed graph and signed closure, reverify their archives and provenance, and resume at the first unfinished base; final installation still requires the complete graph.
- Made the compact AUR presentation, live review, full manager/Builder source, catalog-recovery, package-cache, and storage-usage gates non-installing by default. Supplying APK paths no longer replaces installed apps; `--install-apk` is required, and default runs fail explicitly when the required identities are absent. The presentation gate now supports an already-running manager and the current package-qualified install-script label; its no-network full-device workflow passes on both maintained devices with unchanged package databases. Catalog failure/refresh/restart recovery and package-cache inventory/selective cleanup also pass on both while restoring their exact durable fixtures and preserving every prior cache entry. Storage cleanup additionally restores prior manager running/section state and refuses fixture creation when either app contains retained AUR data. The executable mutation policy now covers 56 optional-install tests.
- Separated APK replacement from sandbox reset in 13 destructive manager and capability tests. `--clean-data` no longer implicitly permits `adb install -r`; tests use the currently installed package unless `--install-apk` is also explicit, and fail with an actionable diagnostic when it is absent. The executable policy now covers 50 optional-install entry points.
- Made six Linux/Android document-flow regressions non-installing by default; replacing the manager now requires explicit `--install-apk`. Open and Share also wait for a visible, actionable Android chooser before capturing evidence, preventing activity-only passes whose screenshot still showed the picker or an unrendered transition. Current installed-manager runs pass on Samsung and the x86_64 emulator with populated full-device chooser frames and no fixture residue.
- Made startup and package-reboot regressions non-installing and running-state-preserving by default, with explicit reboot authorization. Package reboot now archives and exactly restores durable package-job/recovery files, retains the package database/cache, and removes its test backup; default startup and explicitly authorized package-reboot emulator runs pass with inspected full-device evidence.
- Made interrupted-session regressions non-installing and state-preserving by default. Both reject an existing active shell before mutation and restore notification/running state; APK replacement requires `--install-apk`, and the reboot gate rejects before ADB unless `--allow-reboot` is explicit. Process-death and real-reboot emulator paths pass with inspected full-device recovery frames.
- Made all 27 existing optional-install test entry points non-installing by default. APK replacement now requires the explicit `--install-apk` action, the former `--skip-install` remains accepted for compatibility, and development commands that intentionally install a build show the mutation. Default emulator manager and physical Samsung project-tree workflows pass without replacing their APKs; Samsung Terminal remains byte-identical.
- Removed unguarded app-data resets from standalone test defaults. Thirteen manager and test-owned bridge gates now require explicit `--clean-data` before they can call `pm clear`, while existing reset gates retain their named opt-ins and the generated-camera gate retains exact sandbox/permission archive-and-restore behavior. The Terminal project-tree gate now preserves data by default, uses collision-checked per-run aliases and Android folders, and removes only its own mapping, mirror, state, grant, and folder; the physical Samsung state-preserving workflow passes with no test residue.
- Completed the final standalone Bash assertion audit left by the PowerShell migration. Eleven entry points now restore their intended provider-readiness, fresh-probe, ABI-aware catalog, visible-window/touch, fatal-log, runtime-descriptor, local-container, and prior-state checks; destructive legacy APK replacement requires an explicit flag, and the obsolete hand-built KCalc version fixture delegates to the supported manager-generated transaction. Applicable host/container lanes pass, drag/drop passes on both maintained ABIs, and the Samsung passes the ARM, catalog, Mousepad, descriptor, and Android-confirmed KCalc replacement gates with full-device evidence where visual state is asserted. The unavailable 16 KB and absent legacy-fixture lanes remain explicitly unclaimed.
- Accelerated applications that load graphics libraries through `dlopen` now receive the GPU bridge on their first launch. The package profiler streams executables up to 8 MiB through fixed buffers under a 32 MiB catalog budget, accepts only exact NUL-terminated SONAME families, and retains bounded process-map learning as a fallback. Clean current-manager GLMark2 descriptors start virgl immediately and complete the default suite with scores 14 and 12, exit 0, stable correctly fitted hosts, and distinct full-device frames on the x86_64 emulator and AArch64 Samsung.
- Preserved accepted Android composing text when an IME finishes composition in a generated Linux launcher. The wrapper now retains only the latest bounded Wayland preedit, commits that exact text on `finishComposingText()`, and clears it after explicit commit or cancellation instead of discarding it. Stock HeliBoard composition in unmodified Samsung VS Code and Google Keyboard direct commit in unmodified x86_64 Code - OSS pass with exact-ABI builds, scoped logs, and inspected full-device captures.
- Made low-storage package recovery non-destructive. Typed preflight failures now retain exact required/available capacity, Clear cache selects only package downloads outside the failed transaction's freshly resolved verified closure, and a missing/changed catalog conservatively retains the entire cache. State-preserving full-device gates pass on an empty x86_64 emulator cache and keep all 548 pre-existing Samsung archive/signature files byte-identical while removing only synthetic unrelated downloads.
- Recovered interrupted official removals even when pacman's live package record is partially lost. Rust now streams the exact bounded local ownership record into an atomic private snapshot before mutation, binds its canonical SHA-256 into the durable intent, refuses unknown files or tampering, reconstructs the record before asking pacman to remove owned files, and clears all snapshot state only after validated absence. Deterministic process-death, damaged-database, explicit-reinstall, fatal-log, and full-device gates pass on the x86_64 emulator and AArch64 Samsung.
- Proved one Repair can recover multiple partially published pacman local records from the same retained signed closure. Removing both `base` and Foot descriptions now converges through batched database reconstruction and forced reinstall on the x86_64 emulator and AArch64 Samsung, with exact metadata/reasons, launcher inventory, residue cleanup, and full-device UI verified.
- Recovered official installs whose package files and pacman local description were both partially committed. Repair now quarantines only the exact damaged database entry through bounded no-link handling, recreates it from the retained signed archive, preserves the original explicit-reason journal, force-reinstalls the closure, and removes the quarantine only after database/final-state validation. The destructive full-device gate passes on both exact ABIs.
- Made official package Repair converge a partially committed filesystem even when pacman's database already records the current version. Repair now force-reinstalls the complete retained signed closure, while runtime startup and failure refresh pause launcher reconciliation until the mutation is resolved so transiently missing desktop files cannot trigger Android wrapper removal. Exact version/reason, residue cleanup, full-device UI, and executable restoration pass on the x86_64 emulator and AArch64 Samsung.
- Split official package Update from fresh Install at the Rust/JNI boundary so updating an existing dependency no longer promotes it to an explicitly installed package. Package completion also reconciles visible search results immediately instead of retaining a stale “Differs from” version. A reusable signed older-to-newer gate passes `libsysprof-capture` 50.0-2.1 to 50.0-3 on the physical AArch64 Samsung with the dependency reason, database, cache, and full-device UI verified.
- Restored the checksum-pinned JetBrains Mono face to the Rust/Kotlin manager Terminal and anchored every grapheme to its terminal cell. Samsung's device-dependent generic `monospace` alias compressed packed prompts while cursor/input cells remained on the wider grid; exact-ABI full-device captures now show consistent prompt/input spacing on Samsung and the emulator, and the active allocation/latency gate remains zero-GC.
- Kept package and project-sync entry points responsive under real storage. Install, update, remove, repair, and debug transactions now render an accepted preparing state immediately, then write and `fsync` their durable journal on the named package worker before advancing to Queued. Project Sync uses its validated in-memory mapping, retained AUR artifact checks/deletion and network cancellation run off-main, and explicit runtime worker assertions cover package, AUR, storage, synchronization, shell, launcher-publisher, and bootstrap paths. A non-clearing exact-device gate proves visible progress, worker-thread journaling, safe cancellation, clean diagnostics, and full-device presentation on x86_64 and AArch64.
- Preserved exact client raster dimensions for non-tiled/direct Wayland surfaces instead of expanding them to the compositor's default output mode. The complete native compositor probe caught the 4×2-to-320×160 regression and passes again on both maintained ABIs.
- Bounded the native compositor's process-global runtime execution registry. Early cancellation of arbitrary future IDs can no longer grow an unbounded `HashMap`; a fixed 32-entry state machine now fails new starts with `ENOSPC`, consumes cancellation exactly once, and safely reuses removed slots.
- Restored and completed the production Foot lifecycle gate on x86_64. The emulator installs Foot 1.27.0-2 through Archphene's normal 150-package signed transaction and selected-launcher confirmation; manager, wrapper, Foot, and its separate-process-group Bash child retain exact PIDs across Home/resume, deliberate Foot exit −9 is visible, Bash is reaped, and a clean relaunch presents a fresh frame. The gate now asserts the current first-attachment frame contract and retains visually inspected full-device evidence.
- Preserved provider-side edits made during project deletion's final commit window. Archphene rehashes the renamed backup before irreversible removal; a changed backup remains visible, is pulled into the shared Linux project under its conflict name, and is reported in durable sync history. Live-manager and forced-death adversarial cases pass on the exact x86_64 emulator and AArch64 Samsung.
- Completed generic AUR split-package review, build, and atomic installation. RPC results resolve through the canonical package-base snapshot, sibling packages/providers close recursively, the official closure excludes those outputs, SHA-256/SHA-512 evidence stays algorithm-qualified, and every required Builder archive is independently reverified by the manager. The physical Samsung built and installed all six current `dotnet-core-bin` outputs with exact versions and package reasons.
- Preserved logical absolute package symlinks through both fake-chroot builds and root-identity pacman installs. Exact-root legacy physical links remain resolvable without admitting arbitrary Android paths, fixing unmodified `dotnet` while keeping command and desktop discovery fail closed.
- Prevented direct-command results from being hidden by an older interrupted-session warning, and made shell shutdown collect bounded descendants even when tools such as Kestrel create another process group.
- Prevented current Arch GTK file choosers from aborting inside Android when Glycin attempted to launch Bubblewrap. The generic package runtime now publishes checksum-pinned exact-ABI no-Glycin GdkPixbuf/librsvg libraries and a bounded private SVG loader cache. Unmodified Code - OSS and Visual Studio Code open their native chooser, install Red Hat YAML from their real extension galleries, open a YAML document, and activate its providers on the x86_64 emulator and AArch64 Samsung.
- Fixed generic Arch utilities and p11-kit inside the Android path bridge: `statx` now falls back safely after the compatibility filter's `ENOSYS`, contained parent paths and relative symlinks work without permitting root escape, large-file temporary entry points are translated, and AArch64 `dlopen`/`dlmopen` interpose both GLIBC 2.17 and 2.34 imports.
- Preserved external-keyboard Shift/Alt/Ctrl modifiers for terminal cursor, Home/End, editing, paging, and function keys instead of silently reducing them to the unmodified key. The Android path emits standard xterm modifier sequences through its reusable input buffer without per-key allocation; a focus-stable installed Bash fixture verifies exact normal/application Up, `Ctrl+Up`, and `Shift+F5` bytes on the emulator and Samsung.
- Restored terminal SGR styling beyond basic color, bold, underline, and inverse. Faint, italic, strike, and conceal now survive the compact Rust damage protocol and render generically without per-frame allocation; independent reset coverage and the installed `tput`/direct-RGB/DEC-line fixture pass with visually inspected full-device output on the exact x86_64 emulator and AArch64 Samsung.
- Returned terminal identity, healthy-status, and cursor-position queries to Linux processes instead of silently consuming them. Replies use a fixed-capacity Rust ring allocated once with the terminal session and allocation-free thereafter, plus backpressure-aware PTY writes; host pseudo-terminal coverage and exact x86_64 emulator/AArch64 Samsung probes verify Primary DA, ANSI DSR/CPR, DEC origin-relative reports, and clean full-device output.
- Made terminal selection stable across scrollback navigation instead of tying it to transient visible-cell indexes. Rust now publishes an eviction epoch and performs bounded soft-wrap-aware UTF-8 copy across off-screen history/live rows through one reusable buffer; Android renders draggable endpoint handles, supports endpoint crossing and throttled edge autoscroll, and exposes selected state to accessibility. Exact emulator/Samsung gates prove off-screen/back stability, rendered handle interaction, autoscroll, exact paste-back, and full-device visuals.
- Kept Archphene Terminal preedit local and visibly distinct from committed output while reusing one full-editor InputConnection. A deterministic exact-ABI gate replaces an in-progress Japanese candidate, verifies Japanese/CJK/emoji-modifier/ZWJ accessibility content without early PTY delivery, then finishes and commits the exact UTF-8 result to Bash on both the emulator and Samsung with full-device evidence.
- Reflow consecutive soft-wrapped terminal rows as bounded logical lines across both scrollback and the live primary screen without splitting wide Unicode graphemes. Resize pulls a trailing history continuation into the cursor-preserving live viewport and returns only preceding rows to history, while alternate-screen TUIs retain physical-grid semantics. Cached width measurements keep ordinary damage headers from rescanning stored cells, warmed output remains allocation-free, and boundary-specific stable-process portrait-to-landscape gates pass on the exact x86_64 emulator and AArch64 Samsung with full-device evidence.
- Bounded Linux kernel-filesystem visibility without fabricating host telemetry. The glibc bridge now filters `/proc` enumeration through libc and direct `getdents64` so Linux programs see only process entries readable by their Android app UID, while retaining reliable self-process, CPU-topology, safe-device, and private shared-memory paths. Exact x86_64 emulator and AArch64 Samsung probes pass with full-device evidence; Android-denied global metrics such as `/proc/stat` remain unavailable by design.
- Proved durable package-operation recovery across real emulator and Samsung reboots. Work interrupted before mutation now returns at its exact prior progress as Failed with a clear reason and enabled Review action, never a stale Cancel action; the state-preserving gate changes neither the package database nor package cache and tolerates Android-owned post-boot prompts/readiness delays.
- Completed the generic nested-command device matrix on x86_64 and AArch64, covering an installed `/usr/bin` wrapper, `/usr/lib` shebang script, absolute root-internal symlink, and unmodified ELF. The terminal empty-state copy now reflects an already available shared shell, and its shorter command hint remains readable at the emulator's larger text scale.
- Updated the signed-package regression for the manager's sentence-case controls, split package activity fields, persistent section selection, populated cache, and Android-owned launcher install/removal confirmations instead of treating those production behaviors as timeouts.
- Added an unambiguous one-tap “Refresh package catalogs” recovery to durable package activity cards for catalog and trust failures. The signed catalog path now records successful recovery against the exact failed job, survives manager restart, and returns to Review; state-restoring no-network emulator/Samsung gates leave both package databases unchanged.
- Replaced the AUR package review's unbounded evidence wall with a compact summary and six mutually compact, accessible, selectable sections for sources, trust, build environment, digests, recipe, and build logs. A generated no-network fixture proves collapsed defaults, exact expanded evidence, phone scrolling, unchanged pacman state, and full-device presentation on the x86_64 emulator and physical AArch64 Samsung.
- Preserved the manager's selected Packages, Files, or Terminal section across a finished Activity, so reopening Archphene after Back returns to a still-running foreground terminal instead of hiding it behind Packages. Refreshed the sentence-case terminal regression contracts, removed assumptions about Bash being the selected shell, and made Android input injection wait for actual field focus.
- Made cancelled Android launcher installation durable and user-controlled. Android's aborted PackageInstaller result now persists separately from failures, startup reconciliation cannot turn it back into pending publication, and the manager offers explicit Retry/Dismiss actions. Full-device emulator and Samsung gates prove no resubmission after manager restart and exactly one new session after Retry.
- Added reviewed batch publication for packages that introduce several Linux desktop entries. New multi-entry sets remain inert until the user chooses from one checklist; Add selected, Skip all, and Not now are durable, and the package summary reopens dismissed launchers later. The decision crosses JNI as one bounded, generation-checked Rust transaction rather than a series of partially applied choices.
- Completed the generic physical-AArch64 AUR build-to-install boundary for current `visual-studio-code-bin`: official runtime/build dependencies now form one 250-package signed closure, the separate no-network Builder UID provisions and executes the reviewed recipe, Rust rejects hostile or provenance-mismatched output, the manager independently reverifies the copied archive, and pacman installs it with recoverable state and exact postconditions. The Samsung gate discovers `/usr/bin/code` and `code.desktop`, installs its generated Android launcher, proves startup reclaims unusable transient build outputs, and recognizes the installed AUR origin after another restart with a working Remove action; the package screen now keeps only compact relevant progress instead of raw makepkg logs or unrelated terminal cards.
- Restored automatic landscape-sensor presentation for direct-Wayland SDL2/SDL3 applications on the default Android display. This closes the physical Samsung SuperTux portrait coordinate regression without an app-specific offset: manager-generated x86_64 and AArch64 wrappers pass full-device screenshot review with software-cursor hotspots aligned to real taps, both first-run controls activated, stable HOME/resume, and live `1920x1200` tablet resize.
- Extended the generic AT-SPI bridge to GTK 4/libadwaita: sensitive-only controls map to Android enabled state, model-button names follow standard `LABELLED_BY` relations, default actions are limited to semantic control roles, same-bounds focusable descendants receive activation, late names remain current, and destroyed transient controls no longer linger. Current-source GNOME Text Editor passes state-preserving tab, main-menu, Preferences, bounded-tree, SAF edit/writeback/cold-reopen, live/cold theme, Android paste, non-Latin/emoji preedit/commit, Linux copy, exact clipboard readback, and graceful-close gates on x86_64 and manager-owned AArch64.
- Generalized the Android document workflow so unmodified Kate now proves the same SAF import, edit/writeback, collision-safe cold reopen, and DocumentsUI provider path as Mousepad. Added state-preserving Kate gates for bounded File/View/Sessions semantics, dialogs, two actionable tabs, a vertical split, named-session discovery, independent secondary Linux windows, live themes, tablet rotation, and an actual emulator secondary display. Current x86_64 and manager-owned AArch64 Kate 26.04.3-1 pass; the physical workflow restores exact pre-test config/session/feedback hashes.
- Replaced the stale AArch64 signature test, whose deleted fixture paths and named builder container forced physical runs to skip it. The current gate checksum-validates the staged runtime, mounts its Podman package cache read-only, re-verifies every manifest package/signature against the pinned Arch Linux ARM build fingerprint, and rejects missing, unsafe, empty, or partially checked manifests. The legacy compatibility-candidate entry point now targets the maintained emulator and restores a previously stopped manager.
- Hardened the persistent wrapper-signing regression before exercising it on maintained devices. It now requires the tested manager APK to match the installed bytes, preserves version/app-ID/first-install identity across a same-build reinstall, verifies a nonempty v2/v3-signed output, accepts Android's `appId` or legacy `userId` package field, fails explicitly on UI timeouts, and removes its approximately 192 MiB of public/private signing fixtures while restoring prior running state.
- Repaired the standalone PipeWire producer contract, which built successfully but waited forever for an Android camera broker inside its host container. The gate now enables the synthetic frame source, fails with captured producer/core diagnostics if registration does not occur, and directly asserts a numeric standard `Video/Source` node. The AArch64 native-readiness entry point now falls back to the pinned Android-native Podman image when the host SDK lacks NDK 29 instead of requiring an undocumented host install, and manager rejection fixtures now clean their staged APKs and restore prior running state.
- Reworked transient GPU-helper recovery to replace the same-UID virgl helper and restart the Linux runtime once through virpipe before considering software fallback. This preserves the Android host and acceleration on both x86_64 and AArch64 and avoids Samsung's physical-only llvmpipe `SIGILL`; current manager-generated GLMark2 wrappers pass sustained moving-frame and fault-injection gates on both devices.
- Strengthened package ranking and KCalc FD-lifecycle regressions: ranking now proves result order and restores the user's list filter, while descriptor testing warms one-time rotation allocation before enforcing total, SHM, fence, and ashmem bounds with stable processes, rendered output, clean logs, and prior-state restoration.
- Made the remaining repository search, KCalc version selector, physical freeform resize, and Terminal Vulkan standalone gates state-safe and assertion-complete. Catalog tests restore exact manager preferences, freeform no longer clears KCalc data, ADB command delivery is unambiguous, and Vulkan dependencies are provisioned through the verified manager pipeline.
- Made the manager pre-release and pull-to-refresh regressions accessibility-driven and state-safe. Both gates now preserve the user's original manager preferences while validating restart persistence, gesture thresholds, whole-list update completion, and settled list position on x86_64 and physical AArch64.
- Replaced KCalc's compressed-PNG byte comparison with exact semantic calculation and app-region pixel assertions, restored Terminal's nested prompt-collapse check with physical notification-permission preservation, and restored `wev`'s nonzero-runtime rejection. The generic candidate gate now selects the manager's existing direct-Wayland assembly mode; current manager-generated official `wev` wrappers pass the complete input protocol suite on x86_64 and AArch64.
- Restored the local current-source manager self-update fixture with exact APK identity/signature checks, Android-owned confirmation, stable install identity, current app-list reconciliation, and fail-safe restoration of the emulator's original manager APK and install-source policy.
- Restored the runtime-pack gate's catalog parser and unbound legacy-module rejection checks, alongside its existing cross-UID, descriptor-view, lease, and cleanup assertions. The complete trust-and-execution gate passes on x86_64 and physical AArch64.
- Restored the display/theme regressions discarded by the Bash migration: the release matrix again validates bounded manager controls, KCalc viewport fill, process continuity, and compositor logs across phone, tablet, landscape, and docked profiles; the shared toolkit gate now proves stable rendered light/dark/light transitions and live/cold equivalence; and rotation again validates geometry plus visible calculation input. KCalc and Mousepad pass the strengthened gates on x86_64 and physical AArch64.
- Restored the production GitHub self-update regression discarded by the Bash migration. The gate now installs and verifies its requested exact-ABI production baseline, validates published checksums and APK signatures, selects the requested release, requires Android-owned confirmation, proves installed version/signer/UID/first-install continuity, and checks restart reconciliation. The real published x86_64 `v1.0.0` to `v1.0.1` migration passes in an isolated temporary AVD.
- Hardened the real microphone-capture regression and refreshed its current manager-generated `pavucontrol` fixtures on x86_64 and physical AArch64. The gate now checks permission declaration/grant, private Pulse and AAudio startup, bounded capture size, nonzero samples, crash absence, fixture cleanup, and privacy restoration; it also exposed and removed a stale pre-manager-signer Samsung wrapper after preserving its home.
- Retired the obsolete hand-built KCalc update test path in favor of the supported manager-generated wrapper transaction. The replacement gate now verifies Android-owned confirmation, exact installed APK bytes and signer, stable version/UID/first-install identity, manager completion, and post-update KCalc execution on x86_64 and physical AArch64.
- Restored the complete official `wl-clipboard` regression on x86_64 and physical AArch64. Manager-generated `wl-paste` and `wl-copy` wrappers now directly assert exact bidirectional text, live clipboard ownership, focused selection/source protocol, demand-driven Android reads, Android installer updates, and clean runtime output.
- Restored the complete Terminal managed-shell regression on x86_64 and physical AArch64 and fixed the defects it exposed: verified commands can safely re-enter through an immutable APK-owned loader, AArch64 preload entry points carry the `GLIBC_2.17` symbol versions imported by Bash, package queries use Bash-native matching, manager requests no longer depend on blocked Android `date`/`rm`/`mv`/`grep`, and only bounded `sleep` joins the existing internal `cat` bridge. GNU architecture, package inventory, writable/persistent Home, cold restart, signed manager routing, isolation, and SAF transfers now pass.
- Restored the full Android capability-broker regression on current generated KCalc wrappers for x86_64 and physical AArch64 using disposable pinned-NDK probes: permission gating/retry, notification post/withdraw, HTTPS dispatch, unsafe-URI rejection, cross-UID denial, cleanup, and permission restoration now pass.
- Restored the Terminal document-transfer regression on emulator and physical AArch64 with unique self-cleaning fixtures, exact SAF import/export content checks, hidden-home export denial, traversal import denial, and permission-state restoration.
- Restored the Terminal companion isolation regression on emulator and physical AArch64: embedded artifact parity, installed-version parity, distinct UIDs, PTY ownership, signed package-search/result routing, forged-intent denial, and untrusted runtime-provider denial now have direct assertions.
- Restored the package-runtime trust regression on x86_64 and physical AArch64: bundled pacman execution, libalpm closure resolution, live signed download, exact detached-signer continuity, and appended-byte/GPG `BADSIG` rejection now have direct assertions.
- Restored the full manager list/detail Bash regression, including persisted package checks, search clearing, update-only filtering, Android app-settings return, background JobScheduler registration, and state cleanup on emulator and physical AArch64. Pacman update checks now resolve current versions through the architecture-selected repository database, fixing Arch Linux ARM wrappers that previously queried Arch Linux's x86 metadata endpoint and returned HTTP 404.
- Hardened the Bash regression harness after the host-script migration: deterministic emulator provisioning, exact UI matching, safe `set -u` initialization, SIGPIPE-safe image comparison, real non-installing physical runs, cold-launch synchronization, and robust keyboard/menu handling.
- Delivered all debug test intents consistently on both cold and warm manager Activity launches, restoring the Android PackageInstaller update regression when an existing manager task is reused.
- Corrected manager package-detail and Mousepad secondary-window regressions so their success messages are backed by the actual Android settings, popup, and child-window behavior they claim.
- Applied Material You accents to complete semantic GTK selected states without overriding Adwaita base surfaces, and canceled stale delayed IME requests when a popup takes interaction ownership.
- Split the GUI document restart probe into foreground-authorized stages so Android 15 background-activity-launch enforcement remains enabled during the regression.
- Restored the Bash secrets regression's packaged desktop-client coverage with a rebuilt official Arch libsecret/KWallet fixture, direct KWallet D-Bus operations, `kwallet-query`, restart persistence, cleanup, and log-redaction assertions.
- Restored the migrated Mousepad Android-document regression so it again proves the real SAF picker, edit/save writeback, cold reopen, and Archphene DocumentsUI provider on x86_64 and physical AArch64.
- Restored the Mousepad Open-dialog regression with exact InputConnection search text, Android IME retention/dismissal, bounded child-window geometry, accepted result routing, and rendered selection evidence on x86_64 and physical AArch64.
- Fixed GTK file-chooser accessibility by hydrating cache topology with live control geometry, actions, state, and transient children; carrying selected state into Android; and routing generic list-item activation through scaled component centers. Mousepad search results can now be selected and opened entirely through Android framework actions on x86_64 and AArch64.
- Restored the direct camera bridge regression's exact JPEG, I420 stream, private Camera portal, invalid-input, grant, denial, and no-reprompt assertions on x86_64 and physical AArch64.
- Fixed solid-magenta GTK4 camera previews on physical Android GPU drivers by selecting Cairo only for wrappers that declare camera capability. Unmodified Snapshot now passes timestamped PipeWire consumption and foreground pixel inspection on x86_64 and AArch64, and the destructive permission fixture preserves existing Linux homes, preferences, and camera grants.
- Replaced the migrated printing startup smoke with a full XDG-to-Android regression covering PreparePrint, rendered PDF preview, Save as PDF discovery, cancellation cleanup, invalid documents, non-regular descriptors, and runtime-pack binding on x86_64 and AArch64.

### Validation

- Passed the complete conflict-safe project synchronization gate on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 27, 2026: recursive bidirectional additions/edits/deletes, exact conflict retention, post-commit deletion-backup mutation preservation, 256 MiB cancellation in both directions, durable history/retry, manager restart, SAF disconnect retention, and forced death before/after delete and replacement publication all pass with full-device screenshots.
- Passed the physical AArch64 .NET MVP on Android 15 Samsung on July 26, 2026: SDK 10.0.302 installs through the six-output AUR transaction, `dotnet --info`, MVC create/restore/build/run, Android-browser localhost access, and separate-process-group shutdown all pass with scoped checks and full-device screenshots.
- Passed private-root DNS reload and real TLS/Open VSX API access on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 26, 2026. One live process proves success, deliberate unavailable-resolver failure, and restored success; the gate verifies the exact checksum-named patched libc in memory, generated Arch trust bundle, scoped fatal logs, cleanup, and visually inspected full-device screenshots.
- Passed package-cache inventory and selective cleanup on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 26, 2026: real populated caches render with bounded multi-version rows, one selected generated package is removed while its sibling remains, Clear all requires confirmation, every prior cache entry is restored, and the 259/248-package pacman databases remain unchanged. All evidence uses full-device screenshots.
- Passed one-tap package-catalog recovery on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 26, 2026: failure action, completion, Review transition, process-restart persistence, exact journal/preference restoration, full-device presentation, and unchanged 259/248-package databases all pass without network access.
- Passed the compact selectable AUR review presentation on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 26, 2026: summary, collapsed and expanded accessibility states, sources, trust, build environment, digests, recipe, build logs, scrolling, and unchanged 259/248-package pacman databases all pass without network access.
- Passed the state-preserving shared-terminal lifecycle on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 26, 2026: selected Bash/POSIX shell startup, paths and locale, direct input, rotation, Home/Back foreground survival, durable Terminal navigation, status-7 exit, restart, stop, and child reaping all pass with full-device captures.
- Passed current-source GNOME Text Editor GTK 4 accessibility, generic SAF edit/writeback/cold reopen, DocumentsUI provider discovery, stable-process live/cold theme, Android-to-Linux paste, complex UTF-8 InputConnection preedit/commit, Linux selection/copy, exact Android clipboard readback, and graceful-close gates on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 23, 2026. Input and accessibility runs restore the exact pre-test session hash; the input gate also restores the captured Android clipboard.
- Passed current-source Kate 26.04.3-1 SAF edit/save/reopen, menus/dialogs, tabs, vertical split, named session, secondary Linux window, accessibility, and live-theme gates on the API 36 x86_64 emulator and Android 15 AArch64 Samsung on July 23, 2026. The emulator also passed stable tablet portrait/landscape resizing and targeted input on a temporary 1920x1080 Android display; Samsung retained exact pre-test `katerc`, anonymous-session, feedback, manager, and wrapper state.
- Re-verified all 109 package/signature pairs in the current AArch64 runtime against fingerprint `68B3537F39A313B3E574D06777193F152BDBE6A6`, then passed the complete non-installing Samsung physical regression with the signature gate enabled on July 23, 2026. Official `wev` target resolution/download signature verification also passes through libalpm on both maintained repository architectures.
- Passed persistent Android Keystore wrapper signing across byte-identical same-build manager reinstalls on both maintained devices on July 23, 2026. The emulator retained exact manager `1.0.0` bytes, version, app ID, first-install time, and signer `510bace5…b0475`; Samsung retained exact `1.0.2-gpu-recovery-debug` state and signer `a4f902d5…a5c90`. All signing inputs/outputs were removed afterward.
- Passed the host glibc path/exec broker, both manager-native ABI catalogs, AArch64 native readiness through the container fallback, the synthetic PipeWire producer, and the complete unmodified-GStreamer private runtime/cleanup gate on July 23, 2026. Current wrappers also pass audio output, nonzero microphone capture, runtime command isolation, signed-provider authorization, Foot config migration, Terminal prompt collapse, and manager downgrade/checksum rejection on both maintained devices; Samsung microphone privacy and both manager versions were restored.
- Replaced the archived stale Samsung GLMark2 wrapper after preserving its 16 MiB private sandbox, then passed current manager-generated GLMark2 install/launch, sustained distinct virpipe frames, stable process/geometry checks, and rendered same-UID helper replacement on the API 36 emulator and Android 15 Samsung on July 23, 2026. Samsung remains on manager `1.0.2-gpu-recovery-debug`; the emulator's original manager APK was restored byte-for-byte.
- Passed executable-owner repository ranking, multi-term installed-app filtering, warmed KCalc descriptor lifecycle, and Qt/GTK incomplete descriptor-library rejection plus healthy complete-cache launches on the API 36 emulator and Android 15 Samsung on July 23, 2026.
- Passed official package search/add/remove and architecture-adaptive KCalc version pinning on x86_64 and AArch64, non-destructive KCalc physical freeform resize on Samsung, and manager-provisioned unmodified `vulkaninfo` llvmpipe enumeration on both devices on July 23, 2026.
- Passed the state-preserving manager pre-release and pull-to-refresh gates on the API 36 emulator and Android 15 Samsung on July 23, 2026.
- Passed the restored five-profile release display matrix on the API 36 emulator and the strengthened KCalc/Mousepad live-theme plus KCalc rotation gates on both the emulator and Android 15 Samsung on July 23, 2026.
- Passed the full current-source API 36 x86_64 emulator suite and the non-destructive Android 15 AArch64 Samsung suite on July 22, 2026.
- Archived, deliberately migrated, and restored the Samsung KCalc and Mousepad sandboxes under the maintained manager signer. Current-source physical Qt/GTK appearance, interaction, lifecycle, and document-broker core gates pass on July 23, 2026.
- Updated both installed managers and regenerated Mousepad and KCalc through normal manager transactions on x86_64 and AArch64. The focused Open-dialog/IME/accessibility workflow plus the broader Mousepad and KCalc semantic regressions pass on both devices on July 23, 2026.

## 1.0.1 - 2026-07-18

### Added

- Complete on-device package resolution, signature verification, closure staging, wrapper generation, persistent signing, and Android installation for supported x86_64 and AArch64 packages.
- Exact-ABI manager, Terminal, runtime, compositor, and wrapper artifacts for x86_64 and arm64-v8a, including a one-time update path from the published x86_64 `v1.0.0` manager.
- A first-party tabbed Android Terminal with managed Arch CLI packages, verified Bash, durable package requests, foreground sessions, and SAF project trees.
- Android brokers and private desktop adapters for documents, URLs, notifications, audio, microphone, printing, camera/PipeWire, drag-and-drop, accessibility, and encrypted secrets.
- Private AT-SPI2 translation validated with unmodified KCalc/Qt and Mousepad/GTK on x86_64 and physical AArch64.
- Secret Service, libsecret, and KWallet flows validated on x86_64 and physical AArch64.

### Changed

- Replaced application-specific compositor copies with one shared Rust Wayland compositor and metadata-driven Android wrapper host.
- Moved Linux closures into manager-owned content-addressed runtime packs with authenticated providers, Binder-death leases, deduplicated blobs, and process-tree cleanup.
- Added compact stateful package rows, package search/ranking, version selection and pinning, isolated concurrent preparation, durable diagnostics, and self-update progress.
- Added Android-aware Qt/GTK light/dark appearance, font and density controls, IME handling, rotation, popups, dialogs, and secondary-window restoration.

### Security

- Reject incompatible ABIs and 4 KB ELF objects on 16 KB systems before execution.
- Generate package-specific capabilities, permissions, MIME intents, labels, icons, and source/runtime metadata.
- Keep Android PackageInstaller, app UIDs, SELinux, runtime permissions, SAF grants, and Keystore as the security authorities.

### Known limitations

- Package search is broader than tested application compatibility; Qt, GTK, SDL, Electron, Rust-native, XWayland, multimedia, and portal coverage still need expansion.
- Vulkan, zero-copy dmabuf presentation, robust GPU-helper recovery, and sustained vendor desktop-mode validation remain incomplete.
- GrapheneOS Pixel, physical x86_64 Android, and the complete phone/tablet/docked/freeform release matrix remain unvalidated.
