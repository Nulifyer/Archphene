<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.archphene.cameraprobe">
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="36" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <application
        android:label="Archphene Camera Probe"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:debuggable="true"
        android:extractNativeLibs="true">
        <meta-data
            android:name="org.archphene.bridge.capabilities"
            android:value="wayland,input,ime,clipboard,runtime-pack,home-documents,camera" />
        <activity
            android:name="org.archphene.bridge.CameraProbeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
