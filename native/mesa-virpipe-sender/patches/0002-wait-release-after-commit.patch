diff --git a/include/GL/internal/dri_interface.h b/include/GL/internal/dri_interface.h
index 07f38ed..1d28458 100644
--- a/include/GL/internal/dri_interface.h
+++ b/include/GL/internal/dri_interface.h
@@ -665,6 +665,8 @@ struct __DRIswrastLoaderExtensionRec {
                                      uint32_t helper_generation,
                                      uint32_t resource_id,
                                      uint64_t fence_sequence,
+                                     void (*finish)(void *),
+                                     void *finish_data,
                                      void *loaderPrivate);
 };
 
diff --git a/src/egl/drivers/dri2/platform_wayland.c b/src/egl/drivers/dri2/platform_wayland.c
index 175c756..ba2a182 100644
--- a/src/egl/drivers/dri2/platform_wayland.c
+++ b/src/egl/drivers/dri2/platform_wayland.c
@@ -3080,6 +3080,8 @@ dri2_wl_swrast_present_resource(struct dri_drawable *draw,
                                 uint32_t helper_generation,
                                 uint32_t resource_id,
                                 uint64_t fence_sequence,
+                                void (*finish)(void *),
+                                void *finish_data,
                                 void *loaderPrivate)
 {
    struct dri2_egl_surface *dri2_surf = loaderPrivate;
@@ -3089,7 +3091,7 @@ dri2_wl_swrast_present_resource(struct dri_drawable *draw,
       return 0;
    if (!resource_id && !fence_sequence)
       return 1;
-   if (!resource_id || !fence_sequence)
+   if (!resource_id || !fence_sequence || !finish || !finish_data)
       return 0;
 
    org_archphene_gpu_surface_v1_set_resource(
@@ -3161,6 +3163,8 @@ dri2_wl_swrast_swap_buffers_with_damage(_EGLDisplay *disp, _EGLSurface *draw,
    }
 
    dri2_wl_swrast_commit_backbuffer(dri2_surf);
+   if (direct)
+      driSWRastFinishPresent(dri2_surf->dri_drawable);
    return EGL_TRUE;
 }
 
diff --git a/src/gallium/frontends/dri/dri_drawable.h b/src/gallium/frontends/dri/dri_drawable.h
index 4605c8a..42402eb 100644
--- a/src/gallium/frontends/dri/dri_drawable.h
+++ b/src/gallium/frontends/dri/dri_drawable.h
@@ -63,6 +63,8 @@ struct dri_drawable
 
    struct pipe_fence_handle *throttle_fence;
    bool flushing; /* prevents recursion in dri_flush */
+   void (*finish_present)(void *);
+   void *finish_present_data;
 
    /**
     * Private data from the loader.  We just hold on to it and pass
diff --git a/src/gallium/frontends/dri/dri_util.c b/src/gallium/frontends/dri/dri_util.c
index 0d02d3f..b6a7251 100644
--- a/src/gallium/frontends/dri/dri_util.c
+++ b/src/gallium/frontends/dri/dri_util.c
@@ -863,6 +863,18 @@ driSWRastQueryBufferAge(struct dri_drawable *drawable)
    return drawable->buffer_age;
 }
 
+void
+driSWRastFinishPresent(struct dri_drawable *drawable)
+{
+   void (*finish)(void *) = drawable->finish_present;
+   void *finish_data = drawable->finish_present_data;
+
+   drawable->finish_present = NULL;
+   drawable->finish_present_data = NULL;
+   if (finish)
+      finish(finish_data);
+}
+
 /*
  * Note: the first match is returned, which is important for formats like
  * __DRI_IMAGE_FORMAT_R8 which maps to both MESA_FORMAT_{R,L}_UNORM8
diff --git a/src/gallium/frontends/dri/dri_util.h b/src/gallium/frontends/dri/dri_util.h
index 9671367..808064d 100644
--- a/src/gallium/frontends/dri/dri_util.h
+++ b/src/gallium/frontends/dri/dri_util.h
@@ -300,6 +300,9 @@ dri_query_compatible_render_only_device_fd(int kms_only_fd);
 PUBLIC int
 driSWRastQueryBufferAge(struct dri_drawable *drawable);
 
+PUBLIC void
+driSWRastFinishPresent(struct dri_drawable *drawable);
+
 PUBLIC void
 dri2_set_in_fence_fd(struct dri_image *img, int fd);
 
diff --git a/src/gallium/frontends/dri/drisw.c b/src/gallium/frontends/dri/drisw.c
index 139b99c..25049ad 100644
--- a/src/gallium/frontends/dri/drisw.c
+++ b/src/gallium/frontends/dri/drisw.c
@@ -191,15 +191,26 @@ static bool
 drisw_present_resource(struct dri_drawable *drawable,
                        uint32_t helper_generation,
                        uint32_t resource_id,
-                       uint64_t fence_sequence)
+                       uint64_t fence_sequence,
+                       void (*finish)(void *),
+                       void *finish_data)
 {
    const __DRIswrastLoaderExtension *loader = drawable->screen->swrast_loader;
 
    if (loader->base.version < 7 || !loader->presentResource)
       return false;
+   if (finish && drawable->finish_present)
+      return false;
 
-   return loader->presentResource(drawable, helper_generation, resource_id,
-                                  fence_sequence, drawable->loaderPrivate);
+   if (!loader->presentResource(drawable, helper_generation, resource_id,
+                                fence_sequence, finish, finish_data,
+                                drawable->loaderPrivate))
+      return false;
+   if (finish) {
+      drawable->finish_present = finish;
+      drawable->finish_present_data = finish_data;
+   }
+   return true;
 }
 
 static inline void
diff --git a/src/gallium/include/frontend/drisw_api.h b/src/gallium/include/frontend/drisw_api.h
index 1995095..d7c1fd6 100644
--- a/src/gallium/include/frontend/drisw_api.h
+++ b/src/gallium/include/frontend/drisw_api.h
@@ -25,7 +25,9 @@ struct drisw_loader_funcs
    bool (*present_resource) (struct dri_drawable *dri_drawable,
                              uint32_t helper_generation,
                              uint32_t resource_id,
-                             uint64_t fence_sequence);
+                             uint64_t fence_sequence,
+                             void (*finish)(void *),
+                             void *finish_data);
 };
 
 #endif
diff --git a/src/gallium/include/frontend/sw_winsys.h b/src/gallium/include/frontend/sw_winsys.h
index bdc8685..7421808 100644
--- a/src/gallium/include/frontend/sw_winsys.h
+++ b/src/gallium/include/frontend/sw_winsys.h
@@ -144,7 +144,9 @@ struct sw_winsys
                                       void *context_private,
                                       uint32_t helper_generation,
                                       uint32_t resource_id,
-                                      uint64_t fence_sequence );
+                                      uint64_t fence_sequence,
+                                      void (*finish)(void *),
+                                      void *finish_data );
 
    void 
    (*displaytarget_destroy)( struct sw_winsys *ws, 
diff --git a/src/gallium/include/mesa_interface.h b/src/gallium/include/mesa_interface.h
index f5b7048..ff4344a 100644
--- a/src/gallium/include/mesa_interface.h
+++ b/src/gallium/include/mesa_interface.h
@@ -232,6 +232,8 @@ typedef struct {
                                      uint32_t helper_generation,
                                      uint32_t resource_id,
                                      uint64_t fence_sequence,
+                                     void (*finish)(void *),
+                                     void *finish_data,
                                      void *loaderPrivate);
 } __DRIswrastLoaderExtension;
 
diff --git a/src/gallium/targets/dri/dri.sym.in b/src/gallium/targets/dri/dri.sym.in
index d987668..0318661 100644
--- a/src/gallium/targets/dri/dri.sym.in
+++ b/src/gallium/targets/dri/dri.sym.in
@@ -60,0 +61 @@
+		driSWRastFinishPresent;
diff --git a/src/gallium/winsys/sw/dri/dri_sw_winsys.c b/src/gallium/winsys/sw/dri/dri_sw_winsys.c
index cb6f1f4..2c7850e 100644
--- a/src/gallium/winsys/sw/dri/dri_sw_winsys.c
+++ b/src/gallium/winsys/sw/dri/dri_sw_winsys.c
@@ -95,13 +95,16 @@ dri_sw_displaytarget_present_resource(struct sw_winsys *winsys,
                                       void *context_private,
                                       uint32_t helper_generation,
                                       uint32_t resource_id,
-                                      uint64_t fence_sequence)
+                                      uint64_t fence_sequence,
+                                      void (*finish)(void *),
+                                      void *finish_data)
 {
    struct dri_sw_winsys *ws = dri_sw_winsys(winsys);
 
    return ws->lf->present_resource &&
           ws->lf->present_resource(context_private, helper_generation,
-                                   resource_id, fence_sequence);
+                                   resource_id, fence_sequence, finish,
+                                   finish_data);
 }
 
 
diff --git a/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.c b/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.c
index 4c9987d..7e264fc 100644
--- a/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.c
+++ b/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.c
@@ -66,7 +66,7 @@ archphene_resource_is_presentable(struct virgl_vtest_winsys *vtws,
    return vtws->archphene_present_enabled && map_front_private &&
           vtws->sws->displaytarget_present_resource(
              vtws->sws, (void *)map_front_private,
-             vtws->archphene_helper_generation, 0, 0) &&
+             vtws->archphene_helper_generation, 0, 0, NULL, NULL) &&
           target == PIPE_TEXTURE_2D &&
           (format == PIPE_FORMAT_R8G8B8A8_UNORM ||
            format == PIPE_FORMAT_R8G8B8X8_UNORM ||
@@ -131,6 +131,19 @@ virgl_vtest_transfer_put(struct virgl_winsys *vws,
    return 0;
 }
 
+static void
+archphene_finish_present(void *data)
+{
+   struct virgl_hw_res *res = data;
+
+   if (!res || !res->owner || !res->archphene_outstanding_sequence)
+      return;
+   if (!virgl_vtest_send_archphene_wait_release(
+          res->owner, res->res_handle, res->archphene_outstanding_sequence))
+      fprintf(stderr, "Archphene release response was invalid\n");
+   res->archphene_outstanding_sequence = 0;
+}
+
 static int
 virgl_vtest_transfer_get_internal(struct virgl_winsys *vws,
                                   struct virgl_hw_res *res,
@@ -330,6 +343,7 @@ virgl_vtest_winsys_resource_create(struct virgl_winsys *vws,
    res = CALLOC_STRUCT(virgl_hw_res);
    if (!res)
       return NULL;
+   res->owner = vtws;
 
    if (!archphene_presentable &&
        bind & (VIRGL_BIND_DISPLAY_TARGET | VIRGL_BIND_SCANOUT)) {
@@ -734,18 +748,12 @@ static void virgl_vtest_flush_frontbuffer(struct virgl_winsys *vws,
    if (res->archphene_presentable) {
       uint64_t sequence;
 
-      if (res->archphene_outstanding_sequence &&
-          !virgl_vtest_send_archphene_wait_release(
-             vtws, res->res_handle, res->archphene_outstanding_sequence)) {
-         fprintf(stderr, "Archphene release response was invalid\n");
-         return;
-      }
-      res->archphene_outstanding_sequence = 0;
       sequence = virgl_vtest_send_archphene_present(vtws, res->res_handle);
       if (!sequence || !vtws->sws->displaytarget_present_resource ||
           !vtws->sws->displaytarget_present_resource(
              vtws->sws, winsys_drawable_handle,
-             vtws->archphene_helper_generation, res->res_handle, sequence)) {
+             vtws->archphene_helper_generation, res->res_handle, sequence,
+             archphene_finish_present, res)) {
          fprintf(stderr, "Archphene resource identity was not accepted\n");
          return;
       }
diff --git a/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.h b/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.h
index 2da8083..b2c3e39 100644
--- a/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.h
+++ b/src/gallium/winsys/virgl/vtest/virgl_vtest_winsys.h
@@ -56,6 +56,7 @@ struct virgl_vtest_winsys {
 };
 
 struct virgl_hw_res {
+   struct virgl_vtest_winsys *owner;
    struct pipe_reference reference;
    uint32_t res_handle;
    int num_cs_references;
