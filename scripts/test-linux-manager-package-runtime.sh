#!/usr/bin/env bash
set -euo pipefail;source "$(dirname "$0")/lib/android-test.sh";serial=emulator-5554;android_sdk=;skip=false;while (($#));do case "$1" in --serial)serial="${2:?}";shift 2;;--android-sdk)android_sdk="${2:?}";shift 2;;--skip-build)skip=true;shift;;*)archphene_die "unknown argument: $1";;esac;done;[[ -z "$android_sdk" ]] || export ANDROID_SDK_ROOT="$android_sdk";[[ "$skip" == true ]] || "$ARCHPHENE_SCRIPTS_DIR/build-manager-podman.sh";archphene_test_init "$serial";package=org.archpheneos.manager;archphene_adb_run shell am start -W -n "$package/.MainActivity" --ez archphene_test_package_runtime true --es archphene_test_resolve_package kcalc >/dev/null;archphene_wait_ui_text kcalc package-runtime-resolve 30;archphene_adb_run shell am force-stop "$package";archphene_adb_run shell am start -W -n "$package/.MainActivity" --ez archphene_test_package_runtime true --es archphene_test_resolve_package kcalc --ez archphene_test_download_target true >/dev/null;archphene_wait_ui_text 'Package runtime' package-runtime-download 60;archphene_note 'Manager package runtime and detached signature checks passed under Android UID.'
