# Archphene TODO

Updated: 2026-07-29

This file is the remaining prioritized work, not a history of completed tests. Validated behavior belongs in `docs/project-status.md`, `docs/compatibility-matrix.md`, and `research/experiments/`.

## Product target

Archphene should feel like one normal, user-owned Arch Linux installation inside the Archphene Android app:

- Pacman and AUR packages share one filesystem, home, package database, toolchain, and process environment.
- Graphical Linux packages appear individually in Android's app drawer through thin launcher wrappers, but run inside the shared Arch environment.
- CLI tools installed by one package are immediately available to every terminal and graphical application.
- Android remains responsible for app installation confirmation, permissions, lifecycle, display/input, and access to Android files.
- Archphene generically adapts Wayland, Qt, GTK, SDL, Electron, and terminal applications; it does not ship application-specific source patches.

The first daily-use acceptance target is VS Code with `dotnet-sdk`: create an MVC project in the integrated terminal, debug it with breakpoints, run it, and open the local web application in Android's browser.

## P0 - Greenfield Rust + Kotlin implementation

- [x] Choose the production language boundary.
  - Rust owns the shared Arch runtime, package engine, process supervision, storage synchronization, compositor, input state, rendering, and persistent operation state.
  - Kotlin owns the Android application shell: activities, services, lifecycle, permissions, PackageInstaller, SAF/DocumentsProvider, IME, accessibility, notifications, and system UI.
  - Legacy Java/C/Rust prototypes remain as reference until replacements pass their gates; preserving installed prototype state is not required.
- [x] Establish the production source tree without adding new features to `prototypes/`:

  ```text
  android/app/                 Kotlin Android shell
  crates/archphene-core/       platform-independent Rust domain/runtime core
  crates/archphene-android/    small Android/JNI adapter
  crates/archphene-*/          bounded feature crates added only as needed
  tests/                       host contracts and cross-boundary fixtures
  ```

- [x] Pin reproducible Rust, Kotlin, Android Gradle Plugin, Gradle, JDK, SDK, and NDK versions; build offline from verified caches after initial provisioning.
  - [x] Start with Rust 1.88, Kotlin built into AGP 9.3, Gradle 9.6.1, the installed JDK 26, SDK/Build Tools 36, and NDK 29. Use JDK 17 only if a measured compatibility failure requires AGP's documented minimum/default JVM.
  - [x] Commit the verified Gradle wrapper and dependency checksums, exact Rust toolchain and Android revisions, immutable native-container base, and a non-downloading local toolchain gate.
- [x] Build the first vertical slice.
  - [x] Kotlin Activity binds to one Archphene runtime Service.
  - [x] The Service owns exactly one Rust runtime handle and survives Activity recreation.
  - [x] Rust exposes a versioned ABI and fixed-format status snapshot.
  - [x] Kotlin reuses direct buffers for batched commands/events and never transfers rendered frames as Java arrays or Bitmaps.
  - [x] Clean shutdown is idempotent and releases every native handle; the base owns no descriptors, worker threads, or child processes yet.
  - [x] Host unit tests, allocation gate, Rust Android cross-builds, Kotlin compilation, APK packaging/alignment, emulator installation, and Samsung installation pass.
  - [x] Full-device emulator and Samsung gates prove bounded touch batching, visible insets, Activity recreation, HOME/resume continuity, and normal Back shutdown while restoring screen and rotation state.
- [ ] Add capabilities in test-gated vertical slices; do not port code merely because it existed before.
  - [x] Shared private Arch root and bootstrap
    - [x] Create and version a conventional private root under Android app storage with bounded paths and required modes.
    - [x] Reject unsafe layout entries and unknown versions; repair known directory modes on reuse.
    - [x] Bootstrap off Android's main thread and publish readiness through the fixed native snapshot.
    - [x] Host safety tests plus clean/reused-root gates pass on the emulator and Samsung using full-device screenshots.
  - [x] Package catalog/search and persistent job state
    - [x] Add a Rust-owned, fixed-size persistent operation journal with bounded fields and legal state transitions.
    - [x] Atomically publish journal updates, reject corruption/symlinks, recover interrupted work explicitly, and avoid warmed in-memory heap allocation.
    - [x] Bootstrap and reuse the empty journal on the emulator and Samsung; report readiness through the native snapshot.
    - [x] Package the verified pacman runtime as content-addressed exact-ABI native payloads, validate its signed-APK manifest, and execute the real pacman binary through its patched glibc loader.
    - [x] Emit 38 MiB x86_64 and 36 MiB arm64-v8a compressed debug APKs instead of a 168 MB universal test artifact; Android extracts the executable runtime and signed repository keyring to device filesystem paths at installation.
    - [x] Pass clean/reused-root, lifecycle, input, log, and full-device visual gates with real pacman execution on the emulator and Samsung.
    - [x] Connect authoritative repository catalogs and expose real search results.
      - [x] Android's HTTPS stack transports bytes from Rust-selected, exact-ABI official endpoints through one bounded file descriptor; Rust owns temporary-file safety, size limits, sync, atomic publication, modes, and readiness.
      - [x] Rust executes bounded read-only pacman searches off the UI thread, validates and normalizes result fields, caps output, and treats empty pacman exit 1 as a normal no-results state.
      - [x] Clean catalog refresh, package search, process-death reuse, scoped logs, and full-device screenshots pass with `dotnet-sdk` on x86_64 and `btop` on Samsung AArch64. The official ARM repositories currently have no `dotnet-sdk` result.
    - [x] Resolve an exact package and its real dependency closure without mutating the root.
      - [x] Pacman emits repository, package, version, archive, exact HTTPS URL, and download size through a fixed response; Rust rejects untrusted endpoints, unsafe fields, duplicates, missing targets, oversized closures, and malformed output.
      - [x] Kotlin renders the target, package count, bounded download size, and closure off the main thread. Process-death reuse, scoped logs, and full-device screenshots pass for the 33-package `dotnet-sdk` x86_64 closure and 9-package `btop` AArch64 closure.
    - [x] Queue real signed-package operations from the Kotlin UI and render every durable phase immediately.
      - [x] The UI persists Queued, Resolving, Downloading, Verifying, Publishing, Complete, and Failed transitions before rendering them; installation is also rendered explicitly, and the latest result survives Activity and manager process death.
      - [x] Android transports exact official archives and detached signatures through Rust-owned bounded descriptors. Rust enforces resolved sizes, atomic mode-0600 cache publication, signed name/version/architecture identity, the packaged Arch keyring, and the pinned Arch Linux ARM build signer.
      - [x] Current `btop` closures install on the emulator and Samsung; deliberate cache tampering is rejected, redownloaded, and reverified on both, with scoped logs and full-device screenshots.
  - [ ] Pacman install/update/remove
    - [x] Install a bounded exact official package closure into the shared Arch root only after re-verifying every cached signature and package identity.
    - [x] Adapt the generic runtime to Android app-UID ownership, SELinux hard-link denial, app seccomp's blocked `fchmodat2`, and the explicit-loader environment without modifying individual packages.
    - [x] Commit dependency-ordered package transactions durably, recover a stale database lock and explicit install reasons after interruption, and prove the requested package through pacman's local database.
    - [x] Pass clean full-device `btop` install, cache-tamper, process-death, executable, and package-database gates on the x86_64 emulator and AArch64 Samsung.
    - [x] Query exact installed versions, preserve explicit/dependency install reasons, reconcile a current signed package, and conservatively remove only when pacman accepts the non-cascading plan.
    - [x] Pass clean full-device install, verify/tamper recovery, remove, absence, verified-cache reinstall, database-validity, and process-death gates on both targets.
    - [x] Preflight the entire re-verified closure and every installed-package removal before mutation.
      - [x] Require pacman's archive print plan to contain every verified closure name/version exactly once and reject missing, duplicate, changed-version, or unknown archive entries. Cache-only full-device gates pass on both targets.
      - [x] Simulate the accepted conflict/replacement transaction against a bounded mode-0700 private copy of the local package database. Rust requires the result to contain only unchanged installed records plus exact verified archive additions/upgrades, derives every removal, deletes the preview on all exits and runtime restart, and Kotlin blocks a nonempty removal plan before the commit boundary with an explicit non-mutating diagnostic. Host hostile/positive contracts and ordinary changed-dependency updates pass on both exact-ABI devices with no retained preview state.
      - [x] Present every planned removal in a blocking Cancel/Replace review, bind the exact resolution to a single-use native authorization, re-simulate immediately before mutation, atomically snapshot and hash every removed pacman ownership record into the durable intent, and accept conflicts only with pacman's exact replacement bit. Rust restores damaged/missing retained records before forward repair and cleans bounded orphan snapshots only when no mutation exists. Signed official `curl` replacing the device-test `wcurl` baseline passes normal manager flow, exact database/residue/fatal-log checks, and visually inspected full-device light/dark review/completion on the emulator and Samsung.
    - [x] Commit the complete prepared archive set through one normal pacman transaction without `--nodeps` or blanket overwrite; persist and recover explicit install reasons across manager death. Cache-only remove/reinstall and restart-recovery gates pass on both targets.
    - [x] Resolve, download, size-check, and commit the same exact target closure; inspect verified `.PKGINFO` installed sizes and reject low storage before mutation with the required and available bytes. A full-device x86_64 `dotnet-sdk` gate rejects a 757 MiB requirement with 454 MiB available, retains its Review job, and leaves no package entries or database lock.
    - [x] Allow cancellation while work is queued, resolving, downloading, or verifying; disconnect an active transfer, delete partial payloads, persist a terminal Cancelled result, and disable cancellation before pacman mutation. Cache-only cancellation and normal-commit regressions pass on both targets.
    - [x] Publish a bounded mode-0600 mutation intent immediately before official install/update/remove, retain exact verified install inputs or a private checksummed copy of the exact removal database baseline across `SIGKILL`, distinguish pre-mutation Retry from in-mutation Repair, protect retained inputs from cache cleanup, and converge an explicit repair before clearing the intent. A deterministic `SIGKILL` with a missing live `strace` database description restores pacman's ownership record before removal; database/executable absence, private-state cleanup, explicit reinstall, logs, and full-device visual gates pass on the emulator and Samsung.
    - [x] Prove a real older-to-newer repository update, including changed dependencies and replacements, on both targets.
      - [x] Replay signed `libsysprof-capture` 50.0-2.1 to 50.0-3 on the physical Samsung through the normal manager flow. The update preserves its dependency install reason, immediately reconciles the result row, leaves no database lock or partial payload, and passes a reusable full-device gate.
      - [x] Replay the same gate on x86_64 with a deliberately supplied official Arch Linux Archive input. The gate prepares and validates the APK-bound package trust cache, verifies `libsysprof-capture 50.0-2` through the packaged `gpgv` and generated keybox with an exact `VALIDSIG`, uses pacman only to establish that signed dependency baseline, and updates it through the normal manager flow to 50.0-3. Full-device review/completion, immediate row reconciliation, retained `%REASON%=1`, exact local database version, no lock, and no partial payload pass on the emulator. The reusable gate now navigates explicitly to Packages instead of assuming it was the user's last page.
      - [x] Prove a signed changed-dependency update on x86_64: archived `btop` 1.4.3-3 (`gcc-libs`, `glibc`) updates through the normal manager flow to 1.4.7-1 (`glibc`, `hicolor-icon-theme`, `libgcc`, `libstdc++`). The reusable gate reads both verified `.PKGINFO` files through the APK's exact `bsdtar`, requires every new dependency's local database record, preserves dependency reason, and passes inspected full-device review/completion.
      - [x] Prove a signed changed-dependency update on physical AArch64: `angle-grinder` 0.19.6-2 (`glibc`, `libgcc`) updates through the normal manager flow to 0.19.6-3 with the new `jemalloc` dependency. The review shows the exact three-package closure, and verified metadata, installed dependency state, reason preservation, cleanup, logs, and inspected full-device completion all pass.
      - [x] Prove a reviewed package rename/replacement on both exact ABIs. A controlled installed `wcurl 0.0-1` conflict baseline is replaced by the current signed official `curl 8.21.0-1` through the ordinary manager Install path. Both devices show the exact removal and shared-environment warning, retain Cancel until explicit Replace, commit the signed closure, reconcile immediately, and leave only curl with no preview, journal, repair snapshot, lock, partial payload, or fatal log.
    - [ ] Complete official-package scriptlets and whole-operation AUR recovery.
      - [x] Run Arch's installed `update-ca-trust` tooling through a bounded virtual-root maintenance identity after verified package mutation and on runtime preparation. Validate the Mozilla source and generated bundle without following an escape, retain normal package ownership, and pass real TLS on both exact ABIs. This reviewed trust adapter participates in the bounded generic hook bridge below.
      - [x] Replace uncontrolled libalpm hook execution with a bounded private `HookDir`: validate at most 1,024 current system/local hook entries, override each exact `.hook` with a mode-0700 `/dev/null` symlink, refresh the set before every package mutation, and reject substitution or unsafe entries. After official install/remove/repair/rollback, run only root-contained trust, GIO, GLib schema, fontconfig, GDK-Pixbuf, GTK input-module, desktop MIME, shared MIME, and dconf maintenance adapters whose command and subsystem directory are installed; retain the durable mutation journal until every applicable adapter succeeds. A real Samsung adapter failure remained visibly repairable and then converged after the path fix. The reusable gate proves 24 isolated hooks on x86_64 and 29 on AArch64, no uncontrolled libalpm hook execution, fresh applicable caches, exact dependency cleanup, no transaction residue, and inspected full-device completion on both targets.
      - [x] Preserve libalpm's native dependency-ordered per-package `pre`/mutation/`post` lifecycle for verified official and exactly reviewed AUR scripts. Pacman alone receives the conventional fake-root `PATH=/usr/bin:/bin`, so unmodified scripts resolve installed commands through the existing loader/path bridge instead of the invalid Android-host absolute path; other packaged tools retain their private path. The private `HookDir` still suppresses generic uncontrolled hooks. Real signed `fontconfig` reinstall/repair executes `post_upgrade` and `vercmp` without the prior missing-command/arithmetic failures on both exact ABIs, while ordinary rollback still restores the exact signed prior archive and removes forward additions.
      - [ ] Add one signed older/newer exact-ABI rollback gate whose `.INSTALL` has observable reverse upgrade/removal behavior. Native libalpm already selects the restored archive's upgrade script and the installed forward package's removal script in normal order; this remains a test-coverage item rather than a separate execution implementation.
      - [x] Inject a partially committed official install with pacman's database already current but the installed Foot executable missing. Repair force-reinstalls the complete retained signed closure instead of allowing `--needed` to skip it, preserves the exact version and explicit install reason, clears all transaction residue, and passes through the normal UI on the emulator and Samsung. Launcher reconciliation remains paused while the mutation is pending so transient missing desktop files cannot prompt removal of otherwise valid Android launchers.
      - [x] Inject a missing Foot executable and missing pacman local `desc` record together. Repair moves only the exact damaged `name-version` entry into a bounded private quarantine, reconstructs its database record from the retained signed archive with `--dbonly`, force-reinstalls the closure, preserves the original explicit-reason intent, validates the restored `desc`/`files`/`mtree`, and removes the quarantine only after success. The full-device gate passes on both exact ABIs.
      - [x] Repeat the database-partial gate with both `base` and Foot local records missing from the same retained closure. Batched signed database reconstruction, forced reinstall, inventory/launcher return, reason preservation, exact metadata, residue cleanup, and full-device presentation pass on the emulator and Samsung.
      - [x] Inject removal-side pacman database loss while installed files remain. Before mutation Rust streams only the exact bounded local ownership record into an atomic private snapshot and binds its canonical SHA-256 into the durable intent. Repair refuses tampering, reconstructs the record, lets pacman remove its owned files, validates absence, and clears the snapshot only before the final intent. Deterministic process-death, explicit reinstall/reason, fatal-log, and full-device gates pass on both exact ABIs.
      - [x] Inject manager `SIGKILL` after an accepted replacement's exact database snapshot and mutation intent are durable but before pacman starts. A runtime-scoped, single-use, debug-gated hold leaves production transaction semantics unchanged; restart exposes the durable 97% Repair state, restores the retained `wcurl` record, revalidates the authorized plan, completes signed official `curl`, and clears every snapshot, intent, preview, temporary record, and database lock. The reusable gate passes with inspected full-device light/dark review, interruption, and completion frames on the x86_64 emulator and AArch64 Samsung.
      - [x] Retain a complete rollback capability before official mutation: every prior signed archive/signature is reverified and bound by exact name, version, ABI, size, SHA-256, and install reason, while every previously absent closure package is recorded. After deterministic manager death following Pacman's newer-version commit, the UI offers distinct Repair and Roll back actions. Rollback idempotently restores the complete prior closure and removes new additions without `--nodeps`; signed `libsysprof-capture` 50.0-3 → 50.0-2 passes on x86_64, while AArch64 restores `angle-grinder` 0.19.6-2 and removes newly introduced `jemalloc`. Both preserve dependency reasons, clear all durable residue, and pass inspected full-device light/dark gates.
      - [x] Make dependency-orphan cleanup an explicit reviewed removal choice. Rust derives Pacman's exact recursive plan, independently rejects explicit packages in the cleanup tail, retains the single-use user choice, re-simulates immediately before mutation, snapshots every exact local ownership record, and repairs the complete authorized removal after process death. The phone dialog offers Cancel, Keep dependencies, and Remove all with the exact names/versions and shared-environment warning. Real `angle-grinder`/`jemalloc` gates prove package-only preservation on the emulator, cleanup on Samsung, deterministic multi-package repair on the emulator, exact database postconditions, residue cleanup, and inspected full-device light/dark presentation.
      - [x] Prove retry-to-completion under real storage pressure. A bounded emulator-only gate primes a signed closure through the public UI, allocates recognized unrelated cache artifacts until the app filesystem has 48 MiB free, and observes the real 70 MiB preflight rejection before mutation. Clear cache removes only those unrelated artifacts, retains the verified closure, Review enables the exact durable Retry, and `angle-grinder` plus `jemalloc` install normally with no transaction residue. Cleanup traps and baseline restoration pass, full-device failure/recovery/completion frames are inspected, and the physical Samsung passes the matching typed recovery presentation without filling its personal storage.
  - [x] Terminal/PTY and shared command environment
    - [x] Execute one installed ELF command without a shell through the verified loader and generic path bridge, with bounded names, arguments, output, time, process group, environment, working directory, and symlink resolution.
    - [x] Pass clean `btop --version` execution, scoped-log, and full-device UI gates on the x86_64 emulator and AArch64 Samsung after signed install/remove/reinstall cycles.
    - [x] Support package-owned scripts only through conventional `/usr/bin` or `/bin` shebang interpreters that resolve to installed ELF programs inside the same root; reject Android-host and recursive script interpreters.
    - [x] Install Bash through the normal signed package flow and pass a warning-free root-contained script/argument/output gate with full-device screenshots on both targets.
    - [x] Build the conventional interactive shared shell environment, locale policy, startup files, and user-selected command/path behavior without escaping into Android's `/system/bin`.
      - [x] Keep package-installed Bash interactive in a controlling PTY while Archphene owns line editing; disable Bash's redundant Readline path, which Android app seccomp kills while idle.
      - [x] Publish conventional `/home/archphene`, `/tmp`, XDG, and `/usr` PATH values; map `getcwd` back into the Linux root; activate the installed `C.utf8` data; and create user-owned `.bashrc`, `.bash_profile`, `.zshrc`, and Fish `config.fish` defaults once without overwriting edits.
      - [x] Pass root-contained multibyte script plus interactive prompt, HOME, PWD, PATH, UTF-8 charmap, Activity recreation, scoped-log, and full-device gates on both targets.
      - [x] Discover declared, safely installed Bash, POSIX-shell, Zsh, and Fish adapters in Rust; expose a bounded Kotlin selector; persist and migrate the stable choice through process death; refresh it after package mutation; hide it during a session; and pass exact-ABI PTY, scoped-log, and full-device gates on both targets.
      - [x] Add reviewed startup adapters for installed Zsh (`zsh -i`) and Fish (`fish --interactive`). Both are accepted only when the exact safe executable is installed and declared in `/etc/shells`; stock packages installed through Archphene launch, persist, stop, and render on the x86_64 emulator and AArch64 Samsung.
      - [x] Expose only `.bashrc`, `.bash_profile`, `.zshrc`, and Fish `config.fish` through a read/write, non-creating virtual DocumentsProvider folder backed by Rust descriptor-relative no-follow opens; keep all other dotfiles private. Create `~/.local/bin` safely and put it first in the shared process PATH so user commands work consistently in terminals and graphical apps. Host safety/lint gates and real Android Files workflows pass on both exact-ABI targets.
      - [x] Provide a bounded, sandbox-scoped Linux `/proc`, `/sys`, and `/dev` compatibility view where Android SELinux hides host pseudo-files: libc directory APIs and direct `getdents64` now omit every numeric process entry whose status the app UID cannot read, while reliable self-process, CPU-topology, safe-device, and private shared-memory paths remain available. The bridge does not bypass SELinux or fabricate unavailable global telemetry. Host contracts, sealed x86_64/AArch64 bridges, exact-ABI device probes, scoped logs, and full-device screenshots pass.
      - [x] Export the private POSIX shared-memory bridge under both old and current AArch64 glibc symbol versions. Stock ARM `wev` imported `shm_open@GLIBC_2.34` and initially bypassed the `GLIBC_2.17`-only interposer; the sealed bridge now publishes both `shm_open`/`shm_unlink` versions, the rebuild gate checks them, host path/SHM tests pass, and unmodified `wev` opens from Code's shared Samsung root instead of failing on absent `/dev/shm`.
    - [x] Add a four-slot generation-checked PTY registry with controlling-terminal setup, bounded nonblocking direct-buffer I/O, resize, deterministic close, and process-group kill/reap on runtime destruction.
    - [x] Pass package-installed Bash PTY open, 24×80 to 40×120 resize, bidirectional marker, close/reap, scoped-log, and full-device UI gates on both targets.
    - [x] Add user-controlled long-lived session ownership, backpressure queues, cancellation, exit status, Activity/process-death policy, and reboot recovery.
      - [x] Make the Service own a user-started package-installed Bash session across Activity recreation, with bounded fixed input/output rings, reusable direct I/O buffers, explicit stop, process-group reap, and preserved signed exit status.
      - [x] Pass two-command, rotation/rebind, retained-output, `exit 7`, restart/stop, child-reap, scoped-log, and full-device screenshot gates on the x86_64 emulator and AArch64 Samsung.
      - [x] Replace the 100 ms Kotlin polling loop with a Rust-owned `poll(2)` pump, fixed per-session wake channel, write-readiness backpressure, and coarse 16 KiB read batches; pass the full lifecycle gate on both targets.
      - [x] Keep a user-started shell alive across Home, Back, and task removal in a special-use foreground Service with a visible low-priority notification, open-app route, explicit Stop action, and notification permission requested at the user action.
      - [x] Prove identical manager/Linux process ownership, retained output, foreground state, notification title/Stop route, cleanup, and full-device visuals across Home and Back on both targets.
      - [x] Atomically publish a bounded Rust-owned active-session marker, remove it on clean close, surface abrupt manager death as an interrupted session, and require explicit restart rather than pretending a lost PTY was resumed.
      - [x] Pass same-UID `SIGKILL`, interrupted-state, retained-Home restart, clean-marker removal, and full-device screenshot gates on both targets.
      - [x] Validate the same interruption/restart policy across a real emulator and physical-device reboot, including durable marker retention, full-device visuals, explicit restart, and clean-marker removal.
    - [x] Replace the diagnostic command panel with production terminal rendering, scrollback, selection, IME, hardware keyboard, clipboard, and accessibility.
      - [x] Establish a dependency-free Rust terminal-state core with bounded grids, strict streaming UTF-8, delayed VT autowrap, cursor/erase/scroll-region/basic SGR handling, bounded control strings, resize preservation, dirty-row tracking, and a warmed zero-allocation gate.
      - [x] Connect every exact PTY read and resize to session-owned Rust terminal state, then expose bounded, versioned coarse dirty-row snapshots through one direct-buffer JNI call without per-cell JNI traffic or warmed heap allocation.
      - [x] Consume terminal snapshots in a frame-paced Android renderer with one Service-owned reusable direct buffer, dimension-bound primitive cell storage, revision-gated JNI reads, and cached RenderNode rows that are re-recorded only when damaged.
      - [x] Size the PTY from measured scaled automatic monospace cells, give active sessions the available phone/tablet viewport, preserve cursor-adjacent rows when shrinking, and reconstruct a fresh Activity from one explicit full snapshot after rotation, Home, or Back.
      - [x] Expose a bounded terminal accessibility snapshot only when Android queries the node, keeping the normal frame path free of synthesized terminal strings.
      - [x] Route terminal focus through a full-editor Android `InputConnection` with local preedit display, bounded UTF-8 commit/delete delivery, and direct hardware Enter/Backspace/Tab/Escape/navigation/function/Ctrl/Alt handling without per-keystroke byte-array allocation.
      - [x] Prove direct text, Backspace correction, Enter execution, PTY delivery, rendering, accessibility, IME attachment, lifecycle, and full-device visuals with exact-ABI APKs on the emulator and Samsung.
      - [x] Preallocate bounded primary and alternate grids, implement DEC `47`/`1047`/`1049` screen switching plus cursor visibility/save/restore, resize both grids, and preserve warmed zero-allocation screen changes.
      - [x] Prove installed `tput` alternate-screen clearing, content, discard, exact primary restoration, scoped logs, and full-device visuals on both exact-ABI targets.
      - [x] Publish application cursor/keypad, bracketed-paste, newline, and backarrow modes through the existing damage flags; select static mode-correct Android hardware and IME sequences without extra JNI or per-key allocation.
      - [x] Prove exact normal `ESC [ A` and application `ESC O A` hardware Up sequences through installed `tput`, Bash byte capture, accessibility, scoped logs, and full-device screenshots on both ABIs.
      - [x] Preserve Shift/Alt/Ctrl modifiers for external-keyboard cursor, Home/End, editing, paging, and F1–F12 keys using allocation-free xterm modifier sequences. One focus-stable installed Bash fixture proves exact normal/application Up, `Ctrl+Up`, and `Shift+F5` bytes with full-device screenshots on both ABIs.
      - [x] Implement bounded insert/delete/erase character and line operations, region scroll/reverse-index, repeat, insert mode, extended cursor movement/save/restore, and fixed-capacity programmable tab stops.
      - [x] Exercise the editing controls in the warmed zero-allocation gate, then prove installed `tput` ICH/DCH/IL/DL behavior through exact-ABI PTYs, accessibility, scoped logs, and visually inspected full-device screenshots on both targets.
      - [x] Preserve full-byte foreground/background indexes, render the fixed 256-color palette, and parse indexed SGR through the coarse damage/JNI path.
      - [x] Consume G0/G1 character-set designation correctly, render DEC special-graphics line drawing, and prove installed `tput` colors, background reset, line drawing, accessibility, scoped logs, and full-device visuals on both exact-ABI targets.
      - [x] Version the coarse damage protocol to 16-byte cells, preserve indexed colors or exact 24-bit RGB without palette quantization, and keep attributes compacted into the Android foreground array rather than adding another maximum-grid allocation.
      - [x] Prove distinct indexed/direct-RGB wire values, warmed zero allocation, exact `#123456` on `#abcdef`, unchanged 256-color/DEC rendering, broad PTY behavior, scoped logs, and visually inspected full-device screenshots on both ABIs.
      - [x] Add bounded Android clipboard paste through a long-press touch menu, `Ctrl+Shift+V`, InputConnection context action, and accessibility action; preserve literal UTF-8/newlines and wrap it only when the terminal publishes bracketed-paste mode.
      - [x] Prove normal and exact `ESC [ 200 ~`/`ESC [ 201 ~` bracketed bytes through Android clipboard, touch/hardware input, Bash capture, accessibility, scoped logs, and full-device screenshots on both ABIs.
      - [x] Follow Android's scaled 16sp baseline by default and add persistent bounded 10–32sp terminal sizing through pinch, long-press controls, and hardware `Ctrl+-`/`Ctrl++`/`Ctrl+0`; defer metric re-recording and PTY resize until pinch completion.
      - [x] Prove Auto, explicit 20sp, hardware/touch controls, process-restart persistence, reset, scoped logs, and visually inspected full-device screenshots on both exact-ABI targets.
      - [x] Restore the checksum-pinned no-ligature JetBrains Mono asset to the Rust/Kotlin manager Terminal and paint every grapheme at its exact grid coordinate. This removes Samsung's proportional generic-`monospace` prompt/input drift without per-glyph heap allocation; exact-ABI active gates pass with zero GC and inspected full-device output on both targets.
      - [x] Pin `unicode-width` and `unicode-segmentation`, retain at most 16 Unicode scalar values per grapheme, and keep streaming width/segmentation plus protocol publication allocation-free after warm-up.
      - [x] Version terminal damage to bounded grapheme cells; render combining text, CJK, flags, emoji modifiers, and ZWJ families through reusable primitive/scratch storage while preserving one/two-column geometry and normalizing wide cells across edits and resize.
      - [x] Implement DEC origin and autowrap modes, then prove Unicode, exact last-column placement, margin-relative cursor positioning, accessibility, scoped logs, and full-device visuals on both exact-ABI targets.
      - [x] Add a preallocated 4 MiB/4,096-line primary-screen scrollback ring with bounded compact grapheme cells; exclude alternate-screen and partial-margin scrolling and preserve warmed zero-allocation output.
      - [x] Version damage with bounded history/viewport state, hide the cursor outside the live viewport, retain the viewed position while new rows arrive, and expose history through touch, mouse wheel, Shift+PageUp/PageDown, and Android accessibility actions.
      - [x] Prove visible history, return to live output, touch, wheel, hardware-page, accessibility, scoped-log, and full-device behavior on the exact-ABI emulator and Samsung builds; rerun broad PTY, color, editing, and Unicode gates on both.
      - [x] Add bounded visible-viewport word selection with touch-drag extension, exact cell highlighting, 2 KiB grapheme/newline copy, long-press Copy, `Ctrl+Shift+C`, InputConnection Copy, and accessibility Copy; clear stale selection on output, resize, scroll, or input.
      - [x] Prove long-press word selection, visible highlighting, Android Copy, exact clipboard paste-back, existing Paste, scroll gestures, scoped logs, and full-device visuals on both exact-ABI targets.
      - [x] Make overlong grapheme behavior explicit and bounded: retain at most 16 scalars, replace the truncated tail visibly, discard subsequent zero-width extenders, and resume at the next printable cell.
      - [x] Join consecutive soft-wrapped rows into bounded logical lines and reflow them across scrollback and the live primary screen without splitting wide graphemes. Resizing pulls a trailing history continuation into the live screen, returns only rows above the cursor-preserving viewport to history, preserves hard breaks, and leaves alternate-screen TUIs on physical-grid semantics. Width measurements are cached outside resize/line-extension work, warmed output remains allocation-free, and boundary-specific stable-process portrait-to-landscape gates pass on exact x86_64 and AArch64 devices with full-device screenshots.
      - [x] Keep one Activity-owned full-editor InputConnection, expose local preedit separately from committed terminal accessibility text, replace in-progress Japanese candidates without PTY delivery, and commit exact Japanese/CJK/emoji-modifier/ZWJ UTF-8 only on finish. Exact-ABI emulator and Samsung gates pass with full-device preedit/committed screenshots.
      - [x] Anchor selection endpoints in the combined history/live document, publish a scrollback-origin epoch so eviction invalidates stale ranges, and copy bounded off-screen UTF-8 through one reusable native buffer with soft-wrap-aware newlines. Render touch-sized endpoint handles, support crossing endpoint drags and throttled edge autoscroll, and expose selected state to accessibility. Exact-ABI device gates prove long-press, off-screen/back stability, autoscroll, rendered-handle discovery/drag, exact paste-back, and full-device visuals.
      - [x] Return Primary DA, ANSI status/cursor DSR, DEC private cursor reports, and legacy DECID through a fixed-capacity terminal reply ring and backpressure-aware PTY master writes. Unit tests cover ordering and origin-relative coordinates without warmed allocation, a real host PTY proves the reply reaches the child input stream, and unmodified Bash/POSIX-shell probes receive exact replies on the emulator and Samsung with full-device evidence.
      - [x] Preserve and render generic SGR bold, faint, italic, underline, strike, inverse, and conceal attributes with independent resets and no per-frame allocation. The installed `tput`/direct-RGB/DEC-line fixture and full-device visual inspection pass on the exact x86_64 emulator and AArch64 Samsung builds.
      - [x] Parse bounded CSI prefixes/intermediates without executing truncated or malformed parameter lists; implement xterm saved-line erase, Secondary DA, character-size reporting, and ANSI/DEC mode queries through the fixed reply ring. The warmed zero-allocation gate and real Bash PTY probes pass on both exact ABIs.
      - [x] Complete remaining xterm controls/modes and richer terminal accessibility before calling the surface production-ready.
        - [x] Honor xterm background-color erase for character/line/display erasure, edits, and scroll-created cells while resetting foreground and attributes. Host unit, warmed zero-allocation, lint, and exact x86_64/AArch64 build gates pass.
        - [x] Publish coalesced terminal text-change and scroll accessibility events, expose the cursor or visible terminal selection as UTF-16 offsets, accept bounded Android accessibility selection, and include nonblank content below the cursor. Android 10 compatibility, lint, unit, and exact-ABI build gates pass.
        - [x] Implement terminfo-used DEC soft reset without clearing visible content, reset the saved cursor and update-affecting modes, and publish/query reverse-screen mode through the fixed damage flags. Rust unit/allocation/clippy, Android lint/unit, and exact x86_64/AArch64 build gates pass.
        - [x] Implement bounded OSC 4 dynamic-palette set/query and OSC 104 reset. Keep ordinary indexed cells unchanged on the damage wire, publish only overridden indexes as direct RGB, invalidate existing rows on palette changes, and reject malformed/overlong commands without partial mutation or warmed allocation. Rust unit/clippy and exact x86_64/AArch64 build gates pass.
        - [x] Implement DECLRMM/DECSLRM horizontal margins without confusing `CSI s` cursor save. Bound origin-relative addressing/reporting, wrapping, tabs, edits, wide graphemes, and rectangular vertical scrolling while preserving outside columns and excluding partial-width scrolls from history. Mode/query, reset, resize, alternate-screen, unit, warmed zero-allocation, clippy, lint, exact x86_64/AArch64 build, and APK-install gates pass.
        - [x] Implement bounded xterm DECSCUSR block/underline/bar cursor selection and steady/blinking variants plus terminfo-used DEC private mode 12 blink control/query through reserved damage flags. Android resets the 500 ms blink phase on cursor activity, re-records only affected rows, and suspends timing while detached, hidden, or unfocused. Rust unit/allocation/clippy, Android lint/unit, exact x86_64/AArch64 build, APK-install, cold-launch, scoped-log, and full-device manager gates pass.
        - [x] Implement mutually exclusive X10, VT200, button-motion, and any-motion mouse tracking; focus in/out; and normal, UTF-8, SGR, URXVT, and SGR-pixel coordinate encodings. Publish/query the modes through bounded flags, generate reports through a reusable byte array, coalesce motion to changed cells/pixels, retain Shift selection/scrollback override, and reset all modes on terminal reset. Rust unit/allocation/clippy, exact Kotlin encoder tests, Android lint/unit, exact x86_64/AArch64 build, APK-install, cold-launch, scoped-log, and full-device manager gates pass.
        - [x] Implement DEC private synchronized-output mode 2026 with fixed-state generation tracking, withheld damage without consuming dirty rows, atomic full publication on release, and a 250 ms crash/malformed-client fail-safe. Android continues polling only while a synchronized frame is pending so timeout publication does not depend on later PTY output. Rust unit/allocation/clippy, Android lint/unit, exact x86_64/AArch64 build, APK-install, cold-launch, scoped-log, and full-device manager gates pass.
        - [x] Prove background-color erase, cursor style/blink/color, blinking text, synchronized frame atomicity/timeout, TUI touch/mouse/wheel, focus transitions, hardware eight-bit Meta input, and terminal-to-Android clipboard writes through installed `tput`/Bash, then exercise selection/event accessibility with the current exact-ABI APKs on the emulator and Samsung. One state-preserving production-terminal gate now passes against package-installed Bash/`tput` on the exact x86_64 emulator and AArch64 Samsung. Rapid full-device frame sequences contain only the complete prior or released synchronized screen, while a separate held frame publishes through the 250 ms fail-safe. Both targets pass exact SGR mouse reports for touch, mouse, and wheel; Home/resume focus reports; hardware Alt+A in eight-bit Meta mode; OSC 52 Android clipboard write and paste-back; background erase; blinking text; and red blinking bar-cursor visuals. Google TalkBack passes the exact production View on the emulator; a debug-only public-framework accessibility service avoids changing Samsung's vendor tutorial state while proving real accessibility focus, selected text, word traversal, and exact focus/selection/traversal events there. Every accessibility setting is restored after the gate.
        - [x] Preserve SGR 5/6 blinking text and SGR 25 reset through damage protocol v7, compact scrollback, retained Android rows, and the existing lifecycle-safe 500 ms callback. Redraw only rows containing blinking cells, pause/reveal while hidden/unfocused or when Android animations are disabled, keep `Cell` at 76 bytes, and retain warmed zero allocation. Full workspace test/clippy, Android unit/lint, exact x86_64/AArch64 build/install, cold-launch, scoped-log, and full-device manager gates pass.
        - [x] Implement bounded OSC 12 cursor-color set/query and OSC 112 reset with exact RGB replies and no warmed allocation. Publish the current color in the versioned damage header and use it for Android block, bar, underline, and IME-composition cursors without per-frame allocation. Full workspace test/clippy, Android unit/lint, exact x86_64/AArch64 build/install, cold-launch, scoped-log, and inspected full-device manager gates pass; live shell visual proof remains in the consolidated device gate above.
        - [x] Implement queryable/resettable xterm DEC private mode 1034 through a reserved damage flag. Android retains the normal ESC-prefix Meta behavior while disabled, emits valid UTF-8 for high-bit ASCII/control values while enabled, and falls back to ESC-prefixed UTF-8 for non-ASCII keys. Parser/damage remains warmed-allocation-free; exact encoder tests, full workspace test/clippy, Android unit/lint, exact x86_64/AArch64 build/install, cold-launch, scoped-log, and inspected full-device manager gates pass.
        - [x] Implement bounded write-only OSC 52 clipboard integration. Strict selector, Base64, UTF-8, and 2 KiB limits fail closed; queries receive no reply; fixed native/JNI buffers carry only the newest pending write to a coalesced main-thread Android clipboard update without content logging or a redundant raw PTY mirror. Host unit/allocation/real-PTY tests, full workspace test/clippy, Android unit/lint, exact x86_64/AArch64 build/install, cold-launch, scoped-log, and full-device manager gates pass; live clipboard proof remains in the consolidated shell-dependent gate above.
        - [x] Complete the material advertised `xterm-256color` contract audit. Cursor, palette, mouse/focus, synchronized output, eight-bit Meta, and bounded write-only clipboard behavior are implemented; title/title-stack, hyperlink, printer, and window operations remain deliberate no-ops where they do not affect visible correctness or a safe Android interaction.
        - [x] Add character, word, and line movement-granularity actions with exact traversal events and selection extension, backed by retained locale iterators and Unicode boundary tests. A debug-only exact damage-protocol fixture lets an installed vendor screen reader exercise the production terminal View without a managed shell. Google TalkBack binds, accessibility-focuses the terminal, retains focus through next-item navigation, and produces full-device focus-border evidence on the emulator; the Samsung lane fails closed until its preinstalled TalkBack first-run tutorial is completed by the user.
      - [x] Replace the clipped temporary diagnostic strip during active sessions with the measured full-height terminal surface; retain emulator and Samsung full-device portrait/landscape/rebind evidence.
      - [x] Remove the obsolete command-entry and Run/Send controls from the manager Terminal. Before launch it shows shell selection and Start; while active it reserves only a 52 dp status/Stop row, with visually inspected full-device exact-ABI captures.
      - [x] Migrate affected terminal regressions to the real focus/InputConnection/hardware-Enter path and retain noninteractive backend-command coverage behind a bounded debug-only receiver. The production manifest exposes neither receiver nor a duplicate command UI; exact-ABI command, input-mode, navigation, and full-device gates pass.
  - [ ] Wayland compositor, presentation, and lifecycle
    - [x] Run a real package-installed native Wayland client through the authenticated generated launcher, private manager-owned socket, shared Arch root, and manager-owned process group on physical AArch64.
    - [x] Separate Android physical pixels from Wayland logical coordinates, advertise integer and fractional output scale, tile the primary toplevel, preserve client-resolution subsurface rasters, reuse the steady output canvas, and fit the launcher around Android system bars and the visible IME. Foot passes crisp full-device inspection at 1080x2202 portrait, 1080x1343 with the Samsung keyboard, and 2241x978 landscape.
    - [ ] Replace remaining SHM snapshot/copy work with damage-driven retained buffers and HardwareBuffer/dmabuf paths before claiming sustained high-frame-rate or Vulkan production performance.
      - [x] Replace legacy CPU `ANativeWindow_lock`/post output with three preallocated `AHardwareBuffer`s submitted through `ASurfaceTransaction`; bound resize generations, never block on an unsignaled reuse fence, transfer acquire-fence ownership to Android, and explicitly close every returned release fence. The sustained gate found and removed an emulator-only one-`sync_file`-per-frame leak: Foot now holds flat at 157 Samsung and 133 emulator descriptors across active and idle windows. Full SHM damage elimination and direct client dmabuf import remain open.
      - [x] Retain the exact prior committed SHM raster as the semantic baseline and reread only the clipped union of declared surface/buffer damage. Map surface damage back through every buffer transform and integer scale, preserve untouched pixels, and fall back to a full snapshot for first/incompatible/undeclared or viewport-ambiguous commits. Direct partial-read and fallback tests pass; both Android-native ABIs build cleanly, the current x86_64 Foot performance gate remains within memory/FD/JNI budgets with zero GC and an 81 ms input-to-present sample, and the exact AArch64 compositor probe passes on Samsung with inspected full-device evidence.
      - [x] Eliminate the full retained-raster clone on ordinary damaged commits. Read each bounded damage region into one per-`wl_buffer` reusable scratch allocation, publish it atomically into lock-protected Rust frame storage, and reuse the same `Arc` raster; truncated SHM leaves the prior frame byte-exact. Synchronized subsurfaces retain a detached copy until their parent commits. Pointer-identity, detached-snapshot, hostile-short-read, transform, and compositor tests pass; the rebuilt x86_64 Foot gate stays within the production five-process/memory/FD/JNI budgets with zero GC and a 98 ms input sample, and the rebuilt AArch64 probe passes with inspected full-device evidence. Removing synchronized-detachment copies and importing client dmabufs remain open.
    - [x] Fix the production launcher resize/input regression exposed by VS Code on Samsung: translate Activity-window input into the inset Surface, preserve at least 432 logical pixels on phone short edges, require matching fresh client frames after IME resize, and validate crisp 1080-wide full-device pixels plus exact visible touch targets.
    - [x] Repeat the real-client density, live-resize, lifecycle, and visual gate on the x86_64 emulator. Current Code-OSS evidence covers exact full-device pixels, IME/full-height resize, hidden-IME cold launch, graceful Wayland close, and a stopped single-task wrapper reopening into a newly attached session.
  - [ ] Pointer, touch, keyboard, IME, clipboard, and drag-and-drop
    - [x] Bridge authenticated `zwp_text_input_v3` state to a wrapper-owned Android `InputConnection`, including bounded UTF-8 surrounding text, preedit/commit, deletion, editor actions, content hints/purpose, keyboard show/hide, and deterministic detach. Real Samsung Foot accepts text from the Samsung IME.
    - [x] Preserve operation presence across the debug manager-session IME boundary so successive preedit-only updates replace rather than clear or accumulate. Current generated Foot launchers show only the final Japanese candidate and commit exact Japanese/CJK/emoji-modifier/ZWJ UTF-8 to Bash on the exact x86_64 emulator and AArch64 Samsung; the complete clipboard, selection, scrollback, resize, close, force-stop, and cold-relaunch workflow passes with inspected full-device captures.
    - [x] Transform Android touch and pointer coordinates from the physical Surface into the logical Wayland viewport so input and rendered content use the same density model.
    - [x] Bridge capability-scoped Android `DragEvent` and Wayland data-device copy flows for bounded plain text and `text/uri-list`. Import granted Android files through the conflict-safe document broker and export only visible-home files with exact temporary grants; protocol, denial/grant, cancellation, cleanup, and current dual-ABI device gates pass.
    - [x] Bridge `zwp_relative_pointer_v1` and persistent/one-shot `zwp_locked_pointer_v1` through authenticated Binder protocol v5 to lifecycle-safe Android pointer capture. Relative deltas remain in logical Wayland coordinates, absolute motion stops while captured, and host/user/surface loss releases capture deterministically. A real Wayland client proves lock/motion/unlock events, full workspace and Kotlin gates pass, and exact x86_64/AArch64 compositor probes pass with inspected full-device evidence.
      - [x] Copy initial lock regions, intersect them with committed surface input, and double-buffer later `set_region` updates through direct and synchronized-subsurface commits. The real client gate proves pending updates do not alter capture, an excluding commit unlocks, and a null-region commit allows a persistent lock to reactivate without weakening explicit user escape.
      - [x] Activate null/full-surface and single-rectangle `zwp_confined_pointer_v1` regions through Android capture so the hardware cursor cannot visibly escape the Wayland pointer. Integrate raw relative deltas into one virtual position, clip it allocation-free to the committed region/input/surface intersection, emit standard relative plus confined absolute motion, and release deterministically on lifecycle or explicit user escape. A real client gate proves confine/motion/boundary/unconfine behavior.
    - [x] Complete the embedded US hardware-key map for ordinary desktop input: shifted digits, punctuation, brackets, quotes, slash/question, Caps Lock, Super, and Menu now preserve Android modifier state through Wayland. Samsung VS Code's real integrated terminal accepts the `/`, `-`, `.`, and `:` required to create, build, run, and open an ASP.NET Core MVC project; the compositor keymap compiler and 64 native tests cover the regression.
    - [ ] Add real-user non-Latin IME coverage beyond the deterministic debug boundary, non-text clipboard formats, and launcher accessibility semantics.
      - [x] Preserve a real Android IME's accepted composing text when it calls `finishComposingText()`. The generic launcher now commits the latest bounded Wayland preedit instead of clearing it: Samsung HeliBoard visibly retains its underlined Latin candidate after keyboard dismissal in current VS Code, while Google Keyboard's direct-commit path reaches current Code - OSS on x86_64. Exact-ABI builds, unit/lint, scoped logs, and full-device captures pass without application-specific changes.
      - [x] Advertise `wp_cursor_shape_manager_v1` v2, validate focused-pointer enter serials, map every standard cursor-shape-v1 value to an Android system `PointerIcon`, and publish repeated same-size changes through authenticated allocation-free Binder protocol v6. Real Code - OSS and VS Code clients select the standard arrow through current manager-generated launchers on the exact x86_64 emulator and AArch64 Samsung; host protocol tests prove text/crosshair transitions and all 36 shape mappings, and exact-ABI build, lint, unit, compositor-probe, scoped-log, and inspected full-device gates pass.
      - [x] Preserve legacy `wl_pointer.set_cursor` SHM bitmaps across the manager-owned wrapper boundary. Binder protocol v7 copies only changed compositor-retained ARGB cursors, caps them at 256×256/65,536 pixels, validates dimensions/configuration/hotspots on both sides, and retains Android framework ownership instead of recycling a bitmap still referenced by `ViewRootImpl`. Stock Qt 5 `qt5ct` publishes repeated 24×24 arrow cursor surfaces with exact 3,1 hotspots through current generated launchers on the x86_64 emulator and AArch64 Samsung; Samsung additionally proves a 24×24 pointing-hand cursor with hotspot 11,12. Both wrappers survive rapid repeated updates that exposed the original recycled-bitmap crash, with scoped logs and inspected full-device screenshots.
      - [x] Generalize confinement to compound add/subtract regions without ambiguous clipping or pointer-hot-path allocation. Region construction is capped at 64 ordered operations; a fixed 516-edge stack buffer partitions each relative-motion segment and clamps at the first outside interval, including subtractive holes and disconnected islands whose endpoint is allowed again. A real host Wayland client proves the discontinuous case, while exact x86_64/AArch64 native probes, full-device screenshots, scoped logs, and the complete workspace gates pass.
  - [x] Android file integration and `/mnt/android`
    - [x] Expose visible regular files/directories from shared `/home/archphene` through a scoped Android `DocumentsProvider`; reject dotfiles, symlinks, traversal, spoofing controls, replacement rename, and root mutation through a Rust directory-descriptor broker.
    - [x] Import one Android document from the system picker, Open With, or Share directly into `~/Downloads` through a bounded Rust descriptor transaction; fsync before non-replacing publication, recover interrupted staging, number collisions, and retain coarse status across manager restart.
    - [x] Persist exactly one user-selected Android folder capability with explicit Connect/Change/Remove UX, read/write versus read-only status, process-restart validation, safe replacement ordering, and revoked-grant recovery.
    - [x] Materialize one connected Android tree as an initial atomic, non-replacing `~/Projects/<folder>` snapshot. Stream provider descriptors into Rust-owned bounded staging, preserve nested folders and dotfiles, reject unsafe paths/symlinks, recover stale staging, cancel through the Rust chunk loop with complete cleanup/retry, retain the published project after grant removal, and pass exact recursive/restart/full-device gates on both targets.
    - [x] Evolve the initial snapshot into a conflict-safe synchronized POSIX mirror, add scoped drag-and-drop and exports, and show live synchronization/conflict status without pretending `/mnt/android` is a live SAF mount.
      - [x] Define an allocation-free three-way decision engine: one-sided edits/deletes propagate, identical changes converge, and concurrent edits, edit/delete races, unequal new files, and incompatible type changes preserve a conflict.
      - [x] Define and atomically persist a canonical versioned baseline manifest with a stable mapping ID, sorted unique safe paths, fixed fingerprints, strict entry/content/encoded-size limits, stale-temp recovery, symlink/substitution/corruption rejection, and round-trip tests.
      - [x] Populate the initial manifest during the same bounded 32 KiB copy loop with exact SHA-256 fingerprints, a cryptographically random stable mapping ID, atomic private persistence, restart/disconnect retention, and an independent physical-device parser/digest gate.
      - [x] Scan both sides into bounded Rust manifests, page one fixed binary three-way plan, atomically pull through private Rust staging, publish Android file updates through verified staging/backup renames, propagate exact file deletions in both directions, and retain deterministic Android conflict copies without overwriting either original.
      - [x] Recover Android mutations through a bounded checksum-protected atomic journal. Pre-commit process death restores the verified backup and replans; post-commit death finishes backup removal without resurrecting deleted data. The physical Samsung gate kills the manager at both phases.
      - [x] Repeat no-op, bidirectional additions/edits/deletes, conflict retention, restart/disconnect, and forced pre/post-commit recovery on the exact x86_64 emulator build using full-device captures.
      - [x] Propagate directory additions/deletions in both directions. Revalidate and remove only empty Android directories, and serially checkpoint multiple Android file removals within one user-visible Sync so every irreversible deletion retains its own crash-safe baseline boundary.
      - [x] Cancel long Android and Linux fingerprints through a separately locked fixed-size native token registry. Exact 256 MiB cancellation gates on both targets leave no partial files, and forced manager death before and after Android file replacement publication proves restore/retry and finalize-without-rollback behavior.
      - [x] Route initial mirror and synchronization SAF access through one bounded provider bridge. Listing, metadata, and descriptor opens use Android cancellation signals plus a 30-second deadline; document mutations use the same deadline watchdog and terminate the manager on timeout so the persisted journal or the next three-way scan resolves the ambiguous result instead of continuing an uncertain transaction.
      - [x] Add a checksum-protected bounded 16-entry synchronization history with explicit conflict paths, process-restart persistence, a visible History action, and Retry after failure/cancellation. Direct corruption/round-trip tests and exact full-device AArch64 Samsung/x86_64 emulator gates pass.
      - [x] Rehash each committed Android deletion backup immediately before irreversible removal. If the provider changes it after baseline commit, retain the visible backup as an explicit conflict and pull it into Linux instead of erasing it; live-manager and forced-death adversarial gates pass on both exact ABIs.
      - [x] Publish live action-only synchronization progress with explicit push, pull, delete, and conflict phases plus running pulled/pushed/conflict counts. The complete exact-ABI transaction/recovery matrix and visually inspected full-device progress frames pass on the AArch64 Samsung and x86_64 emulator.
      - [x] Move the Android tree scan, fixed-plan decoder, mutation journal, Android document operations, recovery executor, and transaction coordinator out of the already-large runtime Service into cohesive bounded components with direct protocol/journal/recovery tests; keep the Service responsible for lifecycle and foreground ownership.
        - [x] Extract strict fixed-plan/fingerprint/request decoding, the checksum-protected mutation journal/store, and bounded cancellable SAF tree scanning. Twelve direct JVM protocol/journal/history tests, Android lint, exact builds, and the complete synchronization/recovery gate pass on both maintained ABIs.
        - [x] Extract ordered Android/Linux mutation coordination, bounded Android document verification, and crash recovery execution. Six direct recovery-policy tests cover every committed/interrupted/ambiguous state; provider/I/O failures are no longer mistaken for fingerprint mismatches, and the full forced-death matrix passes again on both exact ABIs.
  - [ ] Launcher wrapper generation and runtime-service binding
    - [x] Discover launchable shared-root desktop entries through a bounded Rust parser: accept safe structured `Exec` arguments without a shell, validate executable containment and modes, isolate malformed/symlink entries, cap files/bytes/results, page one immutable snapshot through coarse JNI, and surface scan status in the manager. Exact-ABI cold/restart and light/dark full-device gates pass on the emulator and Samsung.
    - [x] Reconcile complete discovery snapshots into a checksum-protected atomic manager-owned registry. Derive bounded pacman ownership, stable collision-checked Android identities, full launch/icon inputs, desired/published/pending generations, retryable build/install/removal states, and safe process-death/external-package reconciliation. Refuse incomplete catalogs and unsafe/corrupt paths. Host lifecycle tests plus exact-ABI cold-restart, cleanup, logs, and full-device gates pass on the emulator and Samsung.
    - [x] Resolve package icon names and absolute paths through bounded hicolor/pixmaps lookup plus fake-root symlink expansion, fingerprint icon bytes in the v2 registry, and place validated package PNGs in generated Android wrappers with an Archphene fallback. Preserve v1 Android identities during migration and fail closed on path, mode, size, format, dimension, digest, or race mismatches. Host lifecycle tests and a real Foot migration/app-drawer/launch gate pass on Samsung.
    - [ ] Derive reviewed Android capability declarations from the verified package closure and expose actionable per-launcher compatibility failures.
      - [x] Version the already implemented Wayland/input/IME/clipboard launcher contract end to end. Rust publishes the exact bounded set, deterministic APK assembly embeds and reverifies it, wrappers refuse incompatible metadata before Binder connection with an actionable repair message, and manager restart reconciliation treats a correctly signed older contract as stale rather than hostile. Normal Android-confirmed migration plus the complete Foot lifecycle/crash matrix pass on both exact ABIs.
      - [x] Carry non-dangerous document and HTTP(S) OpenURI capabilities through complete manager brokers. The V3 launcher contract binds each request to the authenticated visible wrapper, rejects non-HTTP schemes, credentials, malformed ports, controls, and oversized URIs, and opens Android's browser without file grants or dangerous permissions. Repeatable exact-ABI gates reject `file://` and visibly open Chrome/Brave; a Samsung .NET 10 minimal web app proves Android reaches the service on Archphene's loopback.
      - [ ] Add optional drag-and-drop, notification, audio, camera, printing, secrets, and accessibility declarations only alongside their complete manager brokers; derive the available set from verified package/runtime evidence and obtain explicit user review for dangerous Android permissions.
    - [x] Generate deterministic minimized thin launcher APKs, patch bounded binary-manifest identities, sign with one non-exportable RSA-3072 Android Keystore key using APK v3, and reverify signer/package/version/metadata plus the complete entry set and content digests before handoff.
    - [x] Remove repository-wide VCS/dirty-state metadata from the staged launcher-template bytes so unrelated manager commits cannot cause needless user-confirmed wrapper updates. The release template omits `META-INF/version-control-info.textproto`, and a dedicated gate requires byte-identical SHA-256 across forced full rebuilds.
    - [x] Stream only the verified APK digest into normal `PackageInstaller` sessions, preflight Android's unknown-source approval without stranding work, retain per-launcher install/uninstall confirmation, reconcile verified packages after process death, abandon and requeue interrupted sessions, quarantine identity conflicts, and remove wrappers without touching shared Linux package or user data. Exact fresh-install, denied-permission recovery, cold-restart, launch, and removal gates pass on the Android 16 x86_64 emulator and Android 15 AArch64 Samsung using full-device captures.
    - [x] Launch Android-owned install/update confirmation only while the manager Activity is visible, abandon manager-owned sessions made stale by a new template, and gate the visible-Activity poll on known pending work so idle frames do not allocate publisher threads. A Samsung repair gate updated all five wrappers through five explicit confirmations and returned to zero pending changes.
    - [ ] Run manager-owned Surface/input/lifecycle sessions against authenticated registry descriptors.
      - [x] Authenticate every wrapper Binder transaction from the kernel caller UID, exact installed package, persistent wrapper signer, bounded manifest identity, current Rust registry descriptor, generation, and exact launcher-template digest.
      - [x] Use a bounded versioned Binder protocol with a generation-checked session, client death token, cold-runtime retry, and real cross-process `Surface` attach/replace/detach/close ownership. Reject an untrusted manager-UID probe and malformed protocol input.
      - [x] Detect manager-signed wrappers built from an older template, advance their Android version monotonically, and republish them through normal user-confirmed PackageInstaller updates instead of quarantining them.
      - [x] Pass cold manager start, light/dark full-device Surface presentation, exact physical dimensions, and Binder-death cleanup on the AArch64 Samsung. Repeat this gate on the x86_64 emulator when it is attached again.
      - [x] Recover an already-open wrapper after manager APK replacement by discarding the dead producer's `SurfaceView` exactly once and attaching a fresh Surface to the restarted manager. Samsung Code reconnects, reauthorizes, and presents a full-device frame without manual force-stop or a retained `BufferQueue` connection.
      - [x] Stage the Rust Wayland compositor as a reproducible dual-ABI production library, bind one private manager-owned socket per authenticated session, present committed SHM frames directly through `ANativeWindow`, and remove the socket deterministically after close or wrapper death.
      - [x] Add a bounded generation-checked manager process registry that reauthorizes the exact published descriptor, expands only structured desktop arguments, launches the exact installed `/usr/bin` command in its own process group, and supplies the private Wayland plus standard Qt/GTK/SDL environment without a shell.
      - [x] Submit bounded touch, primary-pointer, and hardware-key batches through one authenticated Binder transaction and one coarse direct-buffer JNI call. Samsung evidence covers the complete wrapper-to-compositor transport, fixed hardware-key mapping, HOME/resume session retention, Back cleanup, and a full-device fail-closed launch diagnostic.
      - [x] Drain merged stdout/stderr into one fixed 16 KiB tail ring, poll signed process exit state outside the frame hot path, terminate remaining process-group descendants when the leader exits, and report bounded start/stop/crash state through an authenticated one-way callback to a wrapper-owned Android overlay while native code remains the exclusive Surface pixel owner. A restored no-download Samsung fixture proves real ELF launch, captured loader diagnostics, exit 127, visible status, socket cleanup, and exact fixture restoration.
      - [x] Prove diagnostic-state portrait/landscape Surface replacement, normal Android reflow without a stretched retained buffer, same-session HOME/resume continuity, Back close, and socket cleanup in visually inspected full-device Samsung captures.
      - [x] Extend the fixed launcher input protocol with five mouse buttons, bounded horizontal/vertical axes, key repeat, Android modifier state, host focus transitions, and deterministic release/cancel cleanup. Validate records independently in Kotlin and Rust, cap active touches, cover the native mappings with 48 compositor tests, and pass a full-device Samsung HOME/resume/rotation/injected-input lifecycle gate without malformed-input rejection or a crash.
      - [x] Bridge focused-wrapper Android text clipboard and Wayland `wl_data_device` selections through bounded authenticated Binder messages. Limit text to 16,384 UTF-16 units/64 KiB UTF-8, cap queued transfers, perform nonblocking pipe I/O on a dedicated worker with two-second deadlines, reject invalid UTF-8, suppress stale/echoed revisions, defer Linux publication across focus loss, and clear descriptors deterministically. The 48-test native suite, full Samsung Wayland probe, production wrapper Unicode Binder gate, full-device capture, Back cleanup, and fatal-log check pass.
      - [x] Prove package-installed Foot connects from the shared AArch64 Arch root, resolves generic Unix sockets, user identity, Bash, runtime libraries, and Android system fonts, and presents a crisp density-aware client/subsurface tree. Full-device Samsung evidence covers safe system/IME insets, Samsung IME text, and live portrait/keyboard/landscape Surface replacement.
      - [x] Repeat the production Foot client gate on the x86_64 emulator, then add HOME/resume, deliberate crash status, and descendant-cleanup variants on both targets.
        - [x] On Samsung, Home detaches the Surface without closing session 1; the same manager and wrapper PIDs survive, resume reattaches the Surface, and the retained Foot frame remains readable in a full-device capture.
        - [x] On Samsung, deliberately killing the real Foot leader reports visible exit `-9`, reaps its Bash descendant even though that child owns a separate process group, and permits a clean fresh-client relaunch. Stopped sessions no longer silently restart after an IME-driven Surface resize. A repeatable gate retains full-device evidence, rejects in-session relaunch, and checks fatal logs.
        - [x] Repeat all production-client lifecycle variants on x86_64. Foot 1.27.0-2 installs through the normal 150-package signed transaction and selected-launcher confirmation; exact PID continuity across Home/resume, visible exit `-9`, separate-group Bash reap, clean relaunch, scoped logs, and visually inspected full-device captures pass.
      - [x] Carry relative-pointer motion and pointer-capture state through the generated production launcher contract. The wrapper requests Android capture only after a standard Wayland lock activates and reports user/system capture loss back to the compositor.
      - [ ] Add real-user non-Latin IME, non-text clipboard formats, and accessibility semantics to the production launcher path.
        - [x] Match Android composition-finish semantics in the generated launcher: retain only the latest accepted bounded preedit, commit it exactly on finish, and clear it after explicit commit or cancellation. Real HeliBoard composition passes in unmodified Samsung VS Code; Google Keyboard direct commit passes in unmodified emulator Code - OSS.
        - [x] Carry all standard cursor-shape-v1 cursors through the current manager-owned compositor and generated launcher as validated Android system icons without bitmap allocation. Code - OSS and VS Code exercise the production Binder v6 boundary on both exact-ABI targets; protocol tests cover semantic text/crosshair changes and every standard shape.
        - [x] Carry bounded legacy cursor-surface ARGB pixels and hotspots through authenticated Binder v7. Current stock Qt 5 clients publish repeated 24×24 arrow frames with exact 3,1 hotspots on both devices, while Samsung additionally proves a pointing-hand frame with hotspot 11,12, without a wrapper crash or app-specific patch.
  - [ ] Qt, GTK, native Wayland, SDL, Electron, and XWayland adaptation
  - [ ] Audio, camera, printing, notifications, URLs, secrets, and accessibility
  - [ ] GPU acceleration, external displays, and secondary windows
    - [x] Port the pinned dual-ABI virglrenderer helper into the Rust/Kotlin manager. Bind startup to A2 registry-derived OpenGL topology, carry only one bounded socket through G5, reauthorize and verify the Unix socket in Rust, select virpipe or llvmpipe without arbitrary environment injection, and own helper/socket cleanup per session. Exact-ABI Qt 5 helper launches, readable full-device frames, clean close, fatal-log review, and bounded stale-socket recovery after manager death pass on emulator and Samsung.
    - [x] Recover a current shared-root graphical session generically after GPU-helper loss. Killing the exact helper twice restarts Qt 5 once with a replacement virpipe helper and then once with explicit llvmpipe; both stages reconnect, publish fresh full-device frames, retain no helper in software mode, and emit no fatal logs on the emulator or Samsung.
    - [x] Repeat the real renderer and scene gates with a current shared-root OpenGL benchmark on both devices. Official unmodified GLMark2 now launches through a clean current manager descriptor with virgl on its first run, completes 32/33 logged default scene variants with positive scores and exit 0, retains stable correctly fitted Android hosts, and passes distinct full-device frame, geometry, software-fallback, and fatal-log checks on the emulator and Samsung.
  - [ ] AUR builds and the VS Code + .NET acceptance workflow
    - [x] Bridge XDG OpenURI from a current generated Code/VS Code launcher into Android's browser through the authenticated wrapper. On Samsung, create, restore, build, and run a stock .NET 10 minimal web project inside the shared Arch root and visibly load its `Hello World!` response at `127.0.0.1:54321` in Brave.
    - [ ] Complete the user-driven acceptance flow in VS Code: create a new MVC project from the integrated terminal, run it under the editor debugger with a breakpoint, and open the rendered site through the production OpenURI path.
      - [x] Complete the unmodified integrated-terminal and browser portion on physical AArch64. Current VS Code creates a stock .NET 10 MVC project, restores and builds it, runs Kestrel at `127.0.0.1:5263`, and invokes stock `xdg-open`; the verified generic adapter reaches the private OpenURI portal and visibly loads the rendered MVC home page in Brave. Full-device evidence and the wrapper log are retained, and the exact fixture/server are removed afterward.
      - [ ] Install and validate an explicitly user-approved C# debugger extension, set a breakpoint in the generated MVC project, and prove pause/inspect/continue. No C# extension is currently installed, and Archphene will not install one without approval.
- [ ] Delete legacy implementation source only after every retained capability has equivalent tests and the user approves removal.

### Performance and safety rules

- [ ] Keep JNI narrow, versioned, and coarse-grained; no per-pixel, per-object, or avoidable per-input-event JNI chatter.
  - [x] Collapse generated-launcher presentation diagnostics from 32 scalar JNI queries into one 128-byte direct-buffer snapshot, return clipboard/IME/presentation readiness in the existing dispatch flags, and suppress unchanged text-input commits. A one-second Foot transaction fell from 1,875 compositor JNI calls to 144 on Samsung and 133 on the emulator while preserving the 8 ms dispatch cadence, exact key batches, clipboard/IME behavior, and visible frame.
- [ ] Pass file descriptors, direct buffers, shared memory, `ANativeWindow`, and HardwareBuffer handles instead of copying payloads.
- [ ] Preallocate bounded queues and reusable scratch buffers for input, frame metadata, logs, package progress, and bridge messages.
- [ ] Do not allocate or free heap objects in frame, pointer-motion, touch-motion, audio-callback, or compositor dispatch hot paths after warm-up.
  - [x] Remove temporary `Vec<WlPointer>` collection from absolute, relative, confined, button, wheel, leave, and popup-return pointer delivery. Resource filtering now streams over the retained seat list; confinement clipping uses fixed bisection state.
- [ ] Put explicit limits on queues, strings, paths, manifests, documents, windows, processes, descriptors, and package operations; apply backpressure rather than unbounded growth.
- [x] Confine Rust `unsafe` to reviewed FFI/syscall modules with safe wrappers, ownership documentation, null/alignment/length validation, and targeted tests.
  - [x] Enforce `unsafe_op_in_unsafe_fn` in every production crate that contains unsafe code, deny unsafe outside the existing Android JNI, Builder JNI, process-syscall, and storage-syscall modules, and keep the remaining compositor-wide confinement work explicit.
  - [x] Enclose every exported compositor JNI symbol and its Java array/direct-buffer/surface/bitmap conversion in one documented boundary module. A warning-free strict workspace Clippy gate, all 260 Rust tests, and optimized x86_64/AArch64 Android builds pass; the later handle-registry and graphics slices removed its raw native handles while legacy runtime-process syscalls remain open.
  - [x] Confine compositor descriptor syscalls, memfd/pipe construction, nonblocking I/O, and `SCM_RIGHTS` message layouts to one reviewed wrapper module. The safe core now adopts `OwnedFd` rather than manufacturing ownership from an integer, every received descriptor enters RAII immediately, and the exact native probe passes on both Android ABIs.
  - [x] Confine `ANativeWindow` and Android Bitmap FFI to one reviewed boundary with retained-window RAII, serialized cross-thread ownership, exact lock/unlock pairing, and bounded format/dimension/stride/null/byte validation before any pixel slice exists. The compositor crate now denies unsafe code outside its explicit JNI, syscall, and Android graphics modules; all 260 tests, strict Clippy, optimized dual-ABI builds, and both exact device probes pass.
  - [x] Extract the legacy runtime launcher's fork/exec, inherited-descriptor, process-group, signal, wait, and child-output operations into one reviewed process boundary with validated manifest/status inputs. Extend the exact Android probe with a real ABI-specific descriptor-executed child plus invalid-FD rejection; both maintained devices pass.
- [x] Keep global JNI handle state in bounded synchronized registries while capability state stays in its owned runtime. Every manager, compositor-core, and generated-launcher handle now has slot/generation validation, deterministic destruction, stale/double-destroy rejection, and fixed exhaustion behavior without per-call heap allocation.
  - [x] Replace all compositor `Box::into_raw`/pointer-cast handles with fixed registries capped at eight core/probe and four generated-launcher instances. Direct host stale/reuse tests and an exact JNI-name Android probe exercise exhaustion, stale use, double destroy, and replacement generation on both exact ABIs.
  - [x] Replace the compositor runtime execution/cancellation `HashMap` with a fixed 32-entry state registry. Unknown early-cancel IDs can no longer grow process-global memory; duplicate, full, cancellation, process registration, removal, and slot-reuse transitions have direct tests.
- [x] Keep blocking I/O, package work, and filesystem synchronization off Android's main thread and compositor/render threads.
  - [x] Enable debug-only StrictMode diagnostics, move manager navigation/terminal/appearance preference reads and writes to one serialized worker with an immutable in-memory snapshot, and move native runtime loading plus stale AUR cache cleanup into bootstrap. A state-preserving cold-start, preference write/restore/restart, real package search, and dependency-resolution gate reports zero Archphene-owned violations or fatal events on the exact x86_64 emulator and AArch64 Samsung; Samsung lifecycle violations remain explicitly reported framework-only (eight across the current two-start gate).
  - [x] Move preferred-shell and storage-onboarding persistence onto the serialized preference worker. Invalidate public runtime handles immediately during service destruction, then disconnect network work, signal and drain tracked workers, cancel/abort synchronization, transition lifecycle, and destroy Rust state on a bounded `ArchpheneShutdown` worker. The repeatable exact-device gate leaves the Activity foreground in 48 ms on the emulator and 99 ms on Samsung, confirms the named shutdown thread, and rejects Archphene StrictMode/fatal events.
  - [x] Finish the static/native audit of launcher Binder entry points, compositor dispatch/presentation, and active package/synchronization/session paths; add explicit thread assertions where StrictMode cannot observe native work.
    - [x] Bind every compositor state/JNI operation to the creating `ArchpheneLauncherSurface` thread and reject potentially blocking clipboard descriptor reads/writes on that owner. The current generated Foot launcher passes authenticated Surface attachment, input, Wayland frames, an Android-to-Linux transfer on `ArchpheneLauncherClipboard`, fatal-log checks, and visually inspected full-device captures on both exact ABIs.
    - [x] Assert that every manager session transaction and generated-launcher callback is dispatched on a Binder worker before parsing or touching session state; UI changes remain explicitly posted to the owning Android handler.
    - [x] Cache the validated project mapping used by Sync instead of rereading preferences on its caller, move durable package-job writes/fsync, AUR artifact checks/deletion, and network cancellation off the Activity thread, and assert every package, AUR, storage, synchronization, shell, launcher-publisher, and bootstrap worker before native or filesystem work. A non-clearing exact-device gate proves immediate accepted/Queued progress, durable journaling on `ArchphenePackagePhases`, safe cancellation, no Archphene StrictMode/fatal events, and visually inspected full-device UI on the emulator and Samsung.
- [x] Replace the legacy Foot workflow's removed per-wrapper debug-intent hooks with debug-only manager-session IME control while keeping generated launchers free of test intents. The current manager-owned shared root and Linux process now pass real wrapper Surface/Binder input, Android/Linux clipboard, pointer selection, scrollback, live resize, graceful close, force-stop cleanup, and cold relaunch on the exact x86_64 emulator and AArch64 Samsung builds. Full-device screenshots are retained and visually inspected; the run also fixed drag motion from Android mouse sources that omit `buttonState` on move events.
- [x] Measure cold/warm startup, RSS/PSS, Java/Kotlin allocations and GC, native allocations, JNI calls, copied bytes, frame time, input latency, descriptors, threads, and child processes.
  - [x] Add a state-preserving exact-device baseline for three cold and three retained-process launches, UI-automation response, PSS/RSS and Java/native heap PSS, exposed native-allocation counts, frames/jank/p95, views, threads, descriptors, idle child processes, fatal logs, JSON output, and full-device screenshots. Current x86_64 emulator/AArch64 Samsung medians are 298/484 ms cold and 14/53 ms hot; both stay inside explicit budgets.
  - [x] Instrument opt-in debug counters for exact Terminal/compositor JNI calls, direct-buffer input/output bytes, JNI array copies, and explicit Kotlin payload copies; sample ART allocation/object/GC/blocking-GC deltas and raw hardware-event-to-Terminal-draw or Wayland-present latency without release-build allocations. The bounded active-session gate records PSS/RSS, heaps, frames, threads, descriptors, and UID processes, enforces explicit budgets, writes JSON, compares rendered frames, and retains visually inspected full-device captures. On the current exact builds, Samsung Terminal/Foot measure 10/145 JNI calls and 13/110 ms latency; the emulator measures 10/131 calls and 8/99 ms. All four samples report zero GC.
- [ ] Add allocation-count and steady-state soak gates; performance regressions fail CI rather than becoming documentation notes.
  - [x] Fail one-second active Terminal and Foot transactions on ART allocation/object, JNI-call, input-latency, PSS/RSS, Java/native heap, thread, descriptor, process, and Android-frame ceilings on both exact ABIs.
  - [x] Add a default repeated-window protocol with four 30-second hardware-key and four 30-second idle windows for each of Terminal and Foot. Every window independently gates ART bytes/objects/GC, JNI/copies, latency, memory, frames, threads, descriptors, UID processes, fatal logs, battery, and thermal state; aggregate JSON rejects peak and final drift. The exact Samsung/emulator runs pass all 16 windows with zero GC and zero peak descriptor/thread growth, 30/154 ms and 18/84 ms Terminal/Foot latency p95, maximum thermal status zero, and visually inspected full-device captures.
  - [ ] Run the exact device soak automatically in a maintained CI/device-farm lane; the executable gate is currently local because no physical-device CI runner is configured.
- [x] Use release builds, R8, baseline profiles, stripped native libraries, panic-abort, and LTO only after debug diagnostics and tests remain adequate.
  - [x] Keep ART profiles startup-only for the manager, isolated Builder, and generated launcher rather than compiling broad application graphs. The release gate builds all three through R8, requires compiled `baseline.prof`/`baseline.profm` payloads, verifies stripped Rust libraries for both Android ABIs, and binds Cargo release output to one codegen unit, ThinLTO, panic-abort, and symbol stripping.

### Architecture gates

- [x] Define one Archphene-owned private Arch root with conventional `/usr`, `/etc`, `/var`, `/opt`, `/home`, and `/tmp` semantics.
- [x] Define how thin launcher APKs bind to the shared runtime service while Linux processes, packages, and files remain in that environment: a separately exported, versioned Binder authenticates the kernel-supplied caller UID, installed generated-wrapper signer, and bounded descriptor registry; the wrapper supplies an Android Surface, input batches, capability results, and a death token while the manager UID owns the Linux process and shared root.
- [ ] Preserve Android launcher entries, icons, intents, windows, notifications, and lifecycle without duplicating Linux roots.
  - [x] Use a deterministic thin `MAIN`/`LAUNCHER` APK as each graphical application's primary Android identity. Current Mousepad and btop wrappers resolve from the app drawer on both maintained devices, retain stable icons/packages/tasks, and remain approximately 91/87 KiB because all Linux packages, processes, and files stay in the shared manager-owned root. A 1×1 widget is unsuitable as the primary identity because it is user-placed and absent from the app drawer; future widgets or shortcuts may route to the same wrapper as optional conveniences.
  - [ ] Complete the remaining per-launcher Android intent and notification brokers and their declaration/review policy without adding Linux state to wrappers.
- [ ] Define supervision, background execution, daemons, resource limits, crash recovery, and shutdown.
- [ ] Define trust for pacman, AUR builds, hooks, arbitrary executables, runtime content, and launcher signing.
  - [x] Pin and seal the official pacman runtime, exact repository endpoints, Arch keyring, and bounded official signer trust used by the current install path.
  - [ ] Define the remaining AUR, hook/scriptlet, arbitrary executable, and launcher-signing policies.
- [x] Document that packages inside the shared Arch environment intentionally share one Linux trust domain.
- [x] Wipe the emulator and Samsung prototype installations only when the new base APK is ready; retain source and any explicitly requested evidence.

## P0 - Android and Linux file integration

- [x] Present Android-accessible storage inside Linux under:

  ```text
  /mnt/android/downloads
  /mnt/android/documents
  /mnt/android/pictures
  /mnt/android/media
  /mnt/android/shared
  ```

  - [x] Use Android's Storage Access Framework for user-selected folders and persist grants across restarts.
  - [x] Keep familiar private home directories such as `~/Downloads`, `~/Documents`, `~/Pictures`, `~/Media`, and `~/Shared`, and publish fail-closed managed aliases for them under `/mnt/android`.
  - [x] Explain in the Files UI that Archphene Home is the Android-visible side of these directories and that a connected Android folder is copied into `~/Projects` as a snapshot, not exposed as a live mount. Exact root-link tests, both-device DocumentsProvider security/CRUD gates, and a full-device Samsung Foot/Bash read through `/mnt/android/shared` pass.
  - [x] Clearly show disconnected/unavailable, connecting, revoked, read-only, mirroring, cancelling, completed, and failed folder states instead of silently failing. Current-source connect/replace/restart/revoke/reconnect/read-only/disconnect and cancel/cleanup/retry/mirror gates pass with full-device evidence on both targets; Connect now opens at primary device storage even after Share used Archphene Home.
  - [x] Add live action-only pull/push/delete progress and explicit conflict states to the conflict-safe synchronized mirror, with durable exact conflict paths in History. The complete transaction/recovery matrix and full-device progress frames pass on both exact ABIs.
  - [x] Keep working project trees in private `~/Projects` POSIX storage and treat each selected SAF tree as an explicitly synchronized Android endpoint, never as a mount.
  - [x] Keep package databases, build/runtime state, symlinks, executables, sockets, and other POSIX-dependent data in the manager's private Arch root; expose only reviewed regular home documents through the provider.
- [x] Add a first-run storage flow that explains private Linux storage and the optional Android-folder snapshot, lets the user choose a folder or skip without granting broad file access, persists either choice across restart, and leaves Connect/Change available later. Pass semantic, picker-cancellation, no-repeat, scoped-log, and visually inspected full-device gates on the exact-ABI emulator and Samsung.
- [x] Expose visible files in shared `/home/archphene` through a `DocumentsProvider` so Android Files, pickers, share sheets, browsers, and other apps can open and save them.
  - [x] Use `MANAGE_DOCUMENTS` plus Android URI grants rather than broad storage permission; keep dotfiles, symlinks, package/runtime trees, and unsupported file types private.
  - [x] Pass exact framework create/read/write/non-replacing-rename/delete, child, collision, traversal, bidi-spoof, and symlink gates plus visually inspected full-device DocumentsUI browsing on the emulator and Samsung.
- [x] Support Android-to-Archphene import through Open With, Share, drag-and-drop, and file-picker flows.
  - [x] Stream a single picker, Open With, or Share document descriptor into `~/Downloads` without copying bytes through JNI/Kotlin; preserve exact bytes, publish atomically, number collisions, recover stale staging, and bound imports at 16 GiB.
  - [x] Keep the Service-owned operation/status through Activity recreation, consume incoming intents once, persist completed/failed/interrupted status, and bound the pending Activity queue to one batch of at most 32 URIs.
  - [x] Pass exact ACTION_VIEW/ACTION_SEND content, duplicate-name, process-restart status, system-picker launch, fatal-log, cleanup, and visually inspected full-device gates on the emulator and Samsung.
  - [x] Accept up to 32 deduplicated documents from a multi-select picker or `ACTION_SEND_MULTIPLE`, import them in selection order through reused direct JNI buffers and the existing atomic Rust transaction, preserve collision numbering, continue after per-document failures with honest partial status, and consume the incoming batch once. Exact-byte, duplicate-stream, restart, picker-cancellation, fatal-log, and visually inspected full-device gates pass on both exact ABIs.
  - [x] Show the active document and live copied-byte count, allow chunk-boundary cancellation from the Files page, and discard the private staging file without publishing a partial destination. Exact slow-copy cancellation, filesystem cleanup, scoped logs, and visually inspected full-device progress/completion states pass on both exact ABIs.
  - [x] Bound provider metadata queries and descriptor opens with Android cancellation plus fail-safe manager recovery when a provider ignores cancellation. Apply a sliding 30-second native idle deadline to descriptor reads while checking cancellation every 100 ms. Cooperative open timeout, ignored-cancellation process recovery, stalled pipe rollback, paced stream success, exact retry, scoped logs, and inspected full-device states pass on both exact ABIs.
  - [x] Use `ACTION_OPEN_DOCUMENT_TREE` for manager folder connection and Linux folder choosers, materialize bounded private POSIX projects, and synchronize connected trees through the crash-safe three-way conflict engine. Capability-scoped wrapper drag-and-drop imports granted Android URI lists through the conflict-safe document session. Current drag/drop gates and the complete exact-APK mirror/recovery matrix pass on both exact ABIs.
- [x] Support Archphene-to-Android open, save, export, and share flows.
  - [x] Add a manager Share action that opens Android's picker at Archphene Home, accepts only a bounded regular document from Archphene's own provider, excludes the manager self-import loop, and sends one MIME-typed content URI with read permission and no write permission through the system chooser. Exact-URI, scoped-log, cleanup, and visually inspected full-device gates pass on the emulator and Samsung; an independent Messages target receives the temporary cross-UID read grant without sending the fixture.
  - [x] Add a manager Open action that starts at Archphene Home, accepts only a bounded regular document from Archphene's own provider, excludes the manager self-import loop, and offers the MIME-typed document to Android viewers through `ACTION_VIEW` with read permission and no write permission. Exact-URI, MIME, grant, cleanup, fatal-log, phone/tablet layout, and visually inspected full-device gates pass on the emulator and Samsung.
  - [x] Add single-file Export: select a bounded regular file from Archphene Home, choose an Android `ACTION_CREATE_DOCUMENT` destination, and stream the exact source and destination descriptors through Rust's fixed 32 KiB buffer without Kotlin/JNI file bytes. Cancellation creates no target; exact bytes, unchanged source, durable completion status, cleanup, StrictMode/fatal logs, and visually inspected phone/wide full-device gates pass on the emulator and Samsung.
  - [x] Add bounded multiple-file Share: select up to 32 regular Archphene Home documents, deduplicate exact URIs, negotiate an exact/family/general common MIME type, and send `ACTION_SEND_MULTIPLE` with read permission and no write permission. Unit MIME-policy checks plus exact two-URI, cleanup, fatal-log, single-share-regression, and visually inspected full-device gates pass on the emulator and Samsung.
  - [x] Add Export byte/percent progress, chunk-boundary cancellation, recovery-safe persistent destination grants, and process-death cleanup of nonempty partial Android outputs. Cancellation and restart delete the incomplete destination, providers without persistable write access fail before mutation, and full-device progress/cancel/recovery plus normal completion/restart gates pass on both exact ABIs.
  - [x] Persist honest Open/Share chooser-handoff and chooser-launch-failure status without claiming an external app completed the action. Single Open success, forced failure, single Share, and two-file Share survive manager process restart on both exact ABIs; exact URI/grant checks, fatal-log checks, and inspected full-device frames pass.
  - [x] Add Linux desktop-portal Save As integration so unmodified GTK 3 desktop applications can request Android's `ACTION_CREATE_DOCUMENT` flow through the shared bridge. A private per-launcher D-Bus portal, authenticated versioned Binder descriptor handoff, checksum-pinned GTK compatibility module, and generic credential bridge keep Android URIs/grants outside Linux while mirroring stable saves through a bounded app-private file. Cancellation, exact bytes, untrusted-caller rejection, rapid relaunch, crash-staging recovery, fatal logs, and full-device stock Mousepad gates pass on the x86_64 emulator and AArch64 Samsung.
- [x] Keep broad Android storage permissions out of every production component. Archphene currently has no demonstrated workflow that requires an advanced `MANAGE_EXTERNAL_STORAGE` mode: SAF, scoped URI grants, the DocumentsProvider, explicit import/export, and synchronized project mirrors cover the supported boundary. A CI source contract rejects `MANAGE_EXTERNAL_STORAGE`, `READ_EXTERNAL_STORAGE`, and `WRITE_EXTERNAL_STORAGE`; packaged manager/Builder/launcher artifacts and every installed Archphene manager/generated launcher on the emulator and Samsung pass the same permission audit.
- [ ] Test grant creation, persistence, revocation, rename, deletion, conflicts, large trees, offline providers, uninstall behavior, and malicious paths.

## P0 - Package system and shared Arch behavior

- [ ] Complete pacman transaction semantics against the shared Arch root.
  - [x] Re-verify signed archives immediately before mutation and commit the complete prepared closure through one normal pacman transaction.
  - [x] Preflight the complete verified closure with normal pacman dependency/conflict/replacement checks and require an exact name/version plan before mutation.
  - [x] Recover a bounded stale database lock and durable explicit-install-reason intent after interruption.
  - [x] Preserve pacman install reasons, validate the local database after mutation, and support conservative non-cascading removal with exact postcondition checks.
  - [x] Cancel safely before the commit boundary, including active-transfer disconnect, partial-file cleanup, durable Cancelled state, and exact installed/cache postconditions.
  - [x] Support large bounded dependency closures and transaction transcripts without widening every command response: a reusable dedicated 256 KiB direct resolution buffer carries the current 198-package x86_64 Code closure, package transactions have a separate bounded 4 MiB diagnostic cap, ordinary failures remain capped at 16 KiB, and detached-signature status parsing accepts non-UTF-8 human diagnostics while requiring exact GnuPG status tokens.
  - [x] Classify preflight storage failures with exact required/available capacity, expose a durable Clear cache recovery action, and remove only cached packages outside the failed transaction's freshly resolved verified closure. If that closure cannot be resolved, retain the complete cache. State-preserving full-device gates pass with an empty x86_64 emulator cache and all 548 pre-existing archive/signature files byte-identical on the AArch64 Samsung.
  - [ ] Complete the signed script-bearing rollback coverage for native official-package scriptlets; forward install/update/repair execution, exact rollback selection, bounded hook adapters, orphan cleanup, and real-storage retry are implemented.
- [ ] Add a bounded AUR workflow suitable for packages such as `visual-studio-code-bin`.
  - [x] Parse bounded AUR v5 metadata and one pinned cgit snapshot without executing it; require exact request/base/version/AArch64 agreement, reject unsafe or oversized tar content, record the cgit commit and snapshot SHA-256, pair every selected source with its strongest supported SHA-256/SHA-512 checksum or explicit `SKIP`, verify local snapshot source bytes and every required output's install-script presence, and enumerate dependencies plus visible PKGBUILD functions. Common package-base `provides` metadata is inherited only for an unambiguous single output; host fixtures and the live current `visual-studio-code-bin` AArch64 snapshot pass.
  - [x] Resolve split-package RPC results through their canonical package-base snapshot, retain the exact requested split identity, recognize both hyphenated and normalized package functions, and carry SHA-512 declarations through native cache verification and the current v5 review wire while deriving independent SHA-256 Builder evidence. The live current `dotnet-sdk-bin` review and its 225 MiB ARM64 SDK source pass on Samsung with full-device evidence.
  - [x] Fetch the exact AUR RPC and Rust-approved cgit snapshot over pinned Android HTTPS, carry the bounded review over a versioned binary JNI wire, and render its community trust warning, maintainer, commit, snapshot digest, architecture-selected sources and checksums, dependencies, build functions, required install scripts, and exact PKGBUILD. A live full-device Samsung gate confirms the official Install action remains disabled and the pacman database is unchanged.
  - [x] Resolve snapshot-local versus direct-HTTPS versus unsupported sources, require bounded safe cache names and SHA-256 or SHA-512 for the supported remote path, follow at most five credential-free HTTPS redirects, stream through a Rust-owned private-cache descriptor, and have Rust re-read and hash the completed file before promotion. Tampered cache entries are discarded; verified entries are rehashed and reused. Physical Samsung gates verify the current Code SHA-256 source and current .NET SDK SHA-512 source and record full-device evidence.
  - [x] Establish a separate Android build identity before executing community code. Stock Samsung denies the manager both user namespaces (`unshare -Ur`) and mount/network namespace creation, while an Android isolated process has no network or manager-data access but cannot create a normal descriptor-granted build workspace. Use one hidden, same-signer Archphene Builder companion—not one APK per Linux package—with a distinct ordinary UID, private storage, no `INTERNET` permission, a signature-guarded explicit Binder, and descriptor-only output. A live Samsung gate proves distinct UIDs, signer continuity, no launcher entry, `untrusted_app` SELinux confinement, private workspace ownership, manager-data denial, and manager inability to read builder-private state.
  - [x] Retain the exact Rust-reviewed snapshot atomically beside verified remote sources, rehash both before opening read-only descriptors, and stage them through the explicit Binder into the Builder's private storage. The Builder independently bounds names/counts/sizes, requires regular descriptors, hashes and atomically publishes every input plus a canonical manifest, and reuses only reverified bytes. The Samsung gate independently hashes the staged 211 MiB Code source, validates the snapshot/source manifest, keeps Install disabled, preserves all 36 pacman entries, and captures the resumed full device.
  - [x] Resolve one bounded official build-environment transaction from the complete `base-devel` package plus every reviewed AUR runtime `depends`, `makedepends`, and `checkdepends`, stripping only validated version operators and failing closed when a target is unavailable from the official repositories. Resolve against an ephemeral manager-owned database with current sync catalogs and no installed-package state so an empty Builder root receives the complete closure. The current Code AArch64 candidate requires 250 official packages and 321,419,288 archive bytes.
  - [x] Download every exact build-environment archive and detached signature into the manager cache, require the pinned architecture signer plus exact package name/version/architecture metadata, retain the exact Rust resolution, and reverify the complete closure before reporting success. The final Samsung cache-reuse gate reverified all 250 Code build packages and removed its ephemeral resolution database.
  - [x] Canonicalize the verified closure as a bounded Rust manifest containing every exact repository/name/version/URL, archive size/SHA-256, and detached-signature size/SHA-256; validate it against the retained resolution in Kotlin, expose its overall SHA-256 for approval/provenance, and permit descriptor reopening only while that retained closure remains verified. The Samsung gate parses all 250 entries and renders the closure digest.
  - [x] Transfer the retained closure to the separate Builder UID in bounded eight-package Binder batches. Rust duplicates the read-only descriptors, uses no-follow directory-FD operations to reset and populate Builder-private storage, streams through fixed scratch buffers, rehashes every archive/signature pair, and atomically publishes the exact manifest only after complete reverification. Host traversal/symlink tests and the Samsung gate cover all 250 pairs, the 321,419,288 archive bytes, and the matching whole-manifest digest.
  - [x] Provision a fresh Builder-private Arch root from the exact published closure without invoking Android host extractors. Rust scans every signed XZ/Zstandard package before mutation, rejects escapes and unsupported entries, rehashes each archive again before extraction, resets more than the former 4,096-entry cleanup limit through no-follow directory-FD traversal, materializes hard links that Android app storage forbids, syncs the filesystem, and atomically publishes a closure-bound root manifest. Host hostile-path/recovery tests and the final Samsung gate prove all 250 packages, 66,878 verified archive entries, 1,744,478,772 expanded bytes, and executable `bash`/`makepkg`/`fakeroot`.
  - [x] Stage a minimal content-addressed Builder runtime from the already verified dual-ABI loader/path bridge, independently validate its manifest, size, digest, mode, and native-path containment, and execute the provisioned root's unmodified `makepkg --version` through it. Eleven host Builder tests cover valid preparation and tamper rejection; the cold Samsung gate executes `makepkg (pacman) 7.1.0` as Builder UID 10345 while preserving all 36 shared-root packages.
  - [x] Prepare and execute the exact reviewed recipe in the disposable Builder root without Android network permission. Rust validates all staged recipe/source and closure digests again, resets the recipe through no-follow directory descriptors, kills stale same-UID processes, runs unmodified `makepkg` in a bounded process group with timeout/log/cancel support, and reaps it on completion. The Samsung gate builds current Code AArch64, records a nonzero 1,079,048,720-byte installed size, and proves `.BUILDINFO` exactly names all 250 signed build packages.
  - [x] Extend the generic glibc bridge for the real build toolchain rather than package-specific workarounds: versioned ARM stat/statx exports, directory-FD-relative traversal, temporary-file rename, fakeroot identity/message queues, optional Landlock denial, hard-link copying, fake-root canonicalization including fortified `realpath`, and the standard `alpm` package-manager identity. Host probes and the physical Code build cover these paths.
    - [x] Support current x86_64 systemd under Android's seccomp profile without inventing filesystem identity: translate `fstatat64`, fall back from blocked `openat2`, and recover the real legacy `STATX_MNT_ID` from bounded descriptor metadata when `statx` is trapped. The native exec/SHM/path probes pass, and the emulator provisions the current 170-package signed build root, completes systemd scriptlets, and builds/verifies the unmodified `pfetch` AUR package.
  - [x] Show source, PKGBUILD, maintainer, signatures/checksums, build steps, permissions, and disk impact before installation. The compact review keeps six mutually exclusive expandable/selectable sections for sources/licenses, community/shared-environment trust and Android permission status, isolated build/download/root cost, exact digests, dependencies/functions/install script/PKGBUILD, and bounded logs. Once Builder output is independently verified, the final pre-Install view replaces the build action with disabled Built, enables Install, and shows the exact archive and installed sizes plus package digest. A no-network ready-to-install fixture now asserts the complete evidence contract and full-device light/dark presentation on both exact ABIs while preserving the emulator's 152-package and Samsung's 151-package databases.
  - [x] Provision a minimal verified Arch build root in the Builder's private storage, execute the approved build without Android network permission, and return verifiable package output through a manager-owned descriptor.
    - [x] Keep every reusable Builder workspace behind no-follow directory-FD/openat-style Rust operations, kill and reap every prior Builder descendant, and treat all Builder-writable output as hostile after each recipe.
      - [x] Move reviewed snapshot/source publication out of Kotlin `File`/`Files` operations into a bounded Rust session. Rust resets both hostile v2 state and the legacy 211 MiB Kotlin workspace without following links, streams and hashes every regular descriptor through a fixed buffer, reverifies all staged bytes before atomically publishing the canonical manifest, and passes hostile-symlink/tamper host tests plus the physical Samsung upgrade gate.
      - [x] Before reusable state is opened on Android, scan bounded `/proc` entries for the Builder's unique UID, bind each candidate to its kernel start time to avoid PID-reuse signaling, kill every other same-UID process, and fail closed until none remains runnable. A Samsung gate creates an orphaned same-UID process before the service starts and proves it is terminated without affecting the manager.
      - [x] Reset and repopulate the disposable recipe with no-follow Rust operations, re-run same-UID cleanup before execution, and retain/cancel/reap the active bounded build process group.
      - [x] Safely enumerate hostile post-build output, reject links/substitutions and unexpected archives, verify exact `.PKGINFO` and `.BUILDINFO` metadata/provenance in Rust, and copy only the selected verified package through a manager-owned descriptor.
    - [x] Report distinct download, closure staging, root scan/extraction, recipe build, and output verification progress. The physical Samsung gate visibly advances through the current 250-package Code closure in bounded eight-package scan/extraction batches, reports the separate root-reset transition, retains one compact cancellable activity card, and separately renders Builder descriptor copy and manager verification of the 212 MiB output before completion.
      - [x] Render closure staging, root scanning, batched root extraction, runtime validation, recipe preparation, live bounded build logs, and cancellation.
      - [x] Make the initial whole-closure scan granular/cancellable and add descriptor-copy/output-verification progress. Rust retains the bounded scan cursor without mutating the root; a full-device Samsung run cancelled cleanly at 200/250, while the final exact-ABI run scanned 66,877 entries/1,744,486,351 expanded bytes and crossed the new separate root-preparation transaction successfully.
  - [x] Run builds as the separate unprivileged Builder UID and clearly communicate that installed Arch/AUR packages join the shared Linux trust domain after manager verification and installation.
  - [x] Independently reverify copied Builder output under the manager UID, retain it in a content-addressed manager cache, install its signed official runtime dependencies and reviewed local archive through a recoverable pacman transaction, and require exact installed-version and database postconditions. The full Samsung gate built and installed `visual-studio-code-bin` generically, retained SHA-256 `51e44c87e8ffbe9b7f3c441bfad6ab8e2fdff1d9f0402d0fa27b94d9a11d3c5c`, and discovered `/usr/bin/code` plus `code.desktop`.
  - [x] Remove bounded regular transient Builder-output copies on manager startup. A physical restart reclaimed six stale outputs and reduced the manager cache from 1.2 GiB to 3.5 KiB without touching the installed package or durable content-addressed cache; subsequently implemented durable capabilities use only independently reverified content-addressed outputs.
  - [x] Recognize installed AUR packages after manager restart from the bounded no-follow pacman local record and its `none` validation origin, show an honest disabled Installed action, and enable conservative Remove without requiring the transient build review. The exact Samsung UI now resolves `aur/visual-studio-code-bin 1.130.0-1` after restart instead of reporting an official-repository failure.
  - [ ] Finish whole-operation recursive AUR recovery and signed script-bearing reverse rollback coverage.
    - [x] Reuse a reviewed package-base capability for each uniquely bound required split output instead of requiring the output name to equal the originally requested split. Ambiguous duplicate output names across reviewed bases still fail closed; Rust unit coverage passes, and the physical Samsung independently verifies all six current `dotnet-core-bin` outputs through the normal Builder-to-manager descriptor boundary.
    - [x] Finish fail-before-mutation handling for unreviewed AUR file ownership conflicts. Rust bounds and canonicalizes every incoming archive file list and rejects a path already owned by a package outside the incoming transaction; direct traversal, duplicate, and foreign-owner tests pass. On the physical Samsung, a controlled `dotnet-runtime` record owned `/usr/bin/dotnet` while all six current `dotnet-core-bin` records were staged aside without moving their files. The normal restored `dotnet-sdk-bin` capability reached the Rust preflight and failed with the exact owner before publishing a mutation journal. A typed native result keeps that preflight distinct from a post-commit failure, and the full-device card now names `usr/bin/dotnet`, `dotnet-runtime`, and “No Linux packages were changed” instead of hiding the diagnostic behind a generic refresh message. The SDK binary and all 12 staged `desc`/`files` records remained byte-identical across both physical runs, after which the fixture was removed, every real record was restored byte-for-byte, the temporary backup was removed, and the Manager returned to the 241-package baseline. Full-device failure evidence and scoped native logs were inspected.
    - [x] Add a reviewed AUR replacement path that shows every exact package removal and shared-environment consequence, requires explicit authorization, re-simulates immediately before mutation, and uses the durable replacement snapshot instead of silently overwriting ownership. Kotlin now performs a strictly decoded non-mutating native plan call before entering the commit boundary, keeps Cancel available, and requires explicit Replace for nonempty removals; Rust binds the complete content-addressed split-output set and exact replacement plan into a single-use authorization, then re-verifies archives, ownership, and the plan at commit. A controlled physical Samsung `dotnet-runtime` → six-package `dotnet-sdk-bin` gate rendered the full-device replacement dialog while all 725 live database files stayed byte-identical through Cancel, with no mutation journal published.
    - [x] Recover retained official-dependency transactions started by an AUR operation from the mutation journal's own package identity rather than assuming the stale outer AUR job identity. A bounded Kotlin/JNI decoder now discovers any retained install/remove journal, rejects malformed or operation-conflicting state, adopts the exact transaction target, and makes Repair/Roll back visible at startup even with an empty package search. The physical Samsung upgraded across the prior failure, recovered `libgcc` instead of the stale `dotnet-sdk-bin` job, rendered the persistent 35% recovery card, repaired the transaction, and cleared both mutation and install-reason journals. A follow-up gate fixed dependency transactions to journal a concrete package from their own resolution rather than the later AUR target.
    - [x] Move final AUR archive installation onto the same durable mutation journal, rollback archive, replacement snapshot, postcondition, process-death repair, and low-storage retry machinery as official packages. The journal binds the selected request, every exact split archive identity/version/size/SHA-256/install-script digest/reason, every checksummed replacement record, exact retained prior AUR archives, and freshly absent outputs. Repair rehashes retained archives, restores and re-simulates replacements, rechecks foreign ownership, and validates exact postconditions; Roll back re-verifies the exact prior capability, restores lifecycle authorization and install reasons, reinstalls replaced archives, and removes only recorded fresh outputs before cleanup. The physical Samsung six-package .NET gate survives the retained forward-repair boundary and returns byte-for-byte to its 241-package baseline. Fresh-package `pfetch` process-death rollback passes on both the Samsung and x86_64 emulator with visible Repair/Roll back choices and no remaining journal, lock, or package record. An emulator-only real-storage gate rejects the verified AUR transaction with 48 MiB free before mutation, clears only unrelated pressure cache files while retaining the 170-package verified build closure, restores the exact durable Review/Retry flow, and installs normally; Samsung exercises the matching typed recovery presentation without consuming personal storage.
    - [x] Define the bounded cross-package-base graph authority in Rust: at most 32 separately reviewed package bases and 256 dependency edges, official providers preferred, exact required split outputs and declared `provides` retained, versioned dependency expressions preserved for later pacman validation, deterministic dependency-first ordering, and fail-closed missing/ambiguous providers, duplicate bases, cycles, limits, and disconnected reviews. A versioned bounded Rust/JNI wire now carries that exact dependency-first plan to Android, whose independent parser rejects duplicate/unknown/out-of-order edges and changed requirements instead of reconstructing graph authority in Kotlin. Direct graph and full Rust/Android gates pass; sequential isolated builds, exact provider-version validation, and one final install transaction remain.
    - [x] Partition the complete build-target set into official-repository and unresolved AUR candidates without treating arbitrary pacman failures as absence. One fresh resolution database handles the bounded operation, the normal all-official path uses one batch, mixed failures bisect to exact C-locale `target not found` leaves, and the combined official set is resolved again to detect conflicts. A bounded versioned Rust/JNI/Kotlin wire carries the exact official closure and unresolved names separately. Unit gates cover exact/malformed diagnostics, mixed targets, final conflicts, and aggregate multi-review dependency collection; the physical Samsung passed the new wire with Code's 250-package/321,420,916-byte closure, staged/provisioned all 66,877 entries, and returned to the visually inspected ready-to-build state.
    - [x] Recursively discover and separately review every unresolved AUR package base before downloading or executing its sources. Exact AUR names are preferred; otherwise a bounded Rust parser accepts only RPC v5 `by=provides` results that actually declare the requested virtual package, preserves ambiguity for explicit choice, and rejects malformed/mismatched responses. Android retains at most 32 exact reviews, replans until the graph is connected, accepts pacman-validated official virtual providers without weakening exact user-package resolution, and renders the combined sources, trust, commit/digests, lifecycle scripts, dependencies, and PKGBUILDs. The physical Samsung reviewed current `pamac-aur` plus `libpamac-aur`, resolved their official closure, and passed full-device combined-evidence inspection.
    - [x] Execute the exact dependency-first graph as separately reviewed isolated builds. The Builder resets a no-follow private dependency staging area, copies and hashes graph-edge-bounded manager-provided archives, independently verifies exact `.PKGINFO`/`.BUILDINFO` identities and bounded archive structure, publishes a canonical digest-bound manifest, installs only into the disposable next-base root, and asks pacman `-T` to validate the preserved sorted version requirements. Android verifies every graph source in Rust-authorized dependency-first order, orchestrates each isolated package base, rehashes every dependency manifest and output at the Builder and manager boundaries, and commits all selected graph outputs through one final pacman transaction. On the physical Samsung, current `libpamac-aur 11.7.4-2` and `pamac-aur 11.7.5-1` built as two isolated bases against an exact 339-package/443,742,032-byte signed official closure; pacman's validated `pacman` provider satisfied `libalpm.so=16`, both local database records survived Manager replacement/restart, and the generated Add/Remove Software launcher installed through Android PackageInstaller. The run also found and fixed nested GPGME execution of sealed runtime-command symlinks without weakening signature preflight; the exact signed `libnotify` preflight passes through the packaged bridge after restart.
      - [x] Persist and restore every independently verified graph-base output as one mode-0600 graph capability so Manager death or a safe retry does not require repeating completed dependency builds. The capability binds the canonical Rust graph digest, exact selected package/ABI, signed official closure digest, every base's package/version/review digest, and every content-addressed output identity/size/SHA-256. Restoration reopens every archive and independently revalidates `.PKGINFO`, `.BUILDINFO`, dependency provenance, install scripts, metrics, and current graph order before enabling installation. The physical Samsung built current `libpamac-aur` → `pamac-aur`, retained a 1,318-byte capability, replaced/restarted the Manager, reconstructed the exact reviews and 339-package closure, and returned directly to disabled Built plus enabled Update with “restored verified build · ready to install”; no recipe build ran after restart. This gate also raised the stale persisted-provenance bound from 256 to the actual bounded 512-official-plus-256-AUR model after the real 339-package closure correctly failed closed.
      - [x] Keep a freshly provisioned Builder root for every separately untrusted package base under the current single-Builder-UID boundary. A physical Samsung same-UID write probe demonstrated that a completed recipe can mutate the prepared root's `/usr`; owner read-only modes or hardlinks therefore cannot preserve a trusted lower tree, while user and mount namespaces are unavailable on the production device. Reuse remains limited to manager-owned signed archives and independently verified outputs. Sharing prepared roots is prohibited until a distinct UID, kernel-enforced immutable lower layer, or equivalent mount boundary exists.
        - [x] Restore a current independently verified graph capability before binding or provisioning the Builder. The Manager now uses the freshly reverified signed-closure digest directly, keeps cache misses on the unchanged isolated build path, and permits the restored output to install without transient Builder state. On Samsung the 339-package closure restored current `libpamac-aur` → `pamac-aur` with no Builder service binding or staging/scanning/provisioning, then completed the real same-version `pamac-aur 11.7.5-1` transaction.
      - [x] Replace repeated generic Verify labels with explicit graph stages—dependency review, source/closure verification, isolated base build, and final install—and expose bounded completed/total progress for each. The compact phone action advances through AUR → Prepare → Sources → Build → Built, with exact base/package counters in the live status and the existing durable transaction progress for final installation. Full-device Samsung evidence covers Prepare, one reviewed dependency base, Sources, 133/339 and 327/339 closure verification, restored Built/Update, and completed installation.
      - [x] Make safe build concurrency adaptive instead of the fixed two-worker cap while retaining conservative phone thermals and memory defaults. A pure bounded policy selects one to four workers from available processors, free memory, Android low-memory state, and thermal severity; the selected value is fixed in the reviewed Builder report and independently bounded again across Binder/JNI. Make, Cargo, CMake, and the verified Ninja wrapper receive the same value. The physical Samsung selected Auto · 4, rendered it in the full-device build evidence, published an exact `ninja -j4` wrapper, started current `libpamac-aur` through the new protocol, and cancelled/reaped the build normally.
    - [x] Persist the verified built-package capability as a bounded Rust-owned atomic manifest plus mode-0600 content-addressed outputs. Restoration requires the exact reviewed package/commit, source digest, signed official closure digest, output identity/order/size/digest, and a fresh independent Builder-output verification; mismatches or mutation fail closed. After manager death, a physical Samsung reverified the current Code review and all 250 closure packages, restored the 212 MiB output without rebuilding, and rendered disabled Built plus enabled Install in a visually inspected full-device capture.
    - [x] Treat required sibling outputs from one split PKGBUILD as one reviewed build/install transaction. Resolve sibling providers recursively, exclude those outputs from the official pacman closure, independently reverify every required Builder archive, and install the complete set in one pacman transaction with the selected package explicit and sibling outputs dependencies. The physical Samsung built and atomically installed all six current `dotnet-core-bin` outputs; exact package versions/reasons, SDK SHA-256 `38b29e8c763c33bd649c3f05fbfb6b08275b1911efafea396455b045755abcb7`, and host SHA-256 `4d7a6efe0637a261842fc2c792914ccad314ecc48ac0c505b3ac127c259dc79b` pass.
    - [x] Bind lifecycle scripts to exact reviewed build outputs instead of globally suppressing them. Rust records the effective `.install` path and bytes separately for every required split output, review wire v5 displays every script, and the manager rejects any built archive whose `.INSTALL` presence or SHA-256 differs. Before pacman mutation it atomically publishes exact package/version/archive/script capabilities in mode-0600 manager state outside the shared Arch root; reviewed AUR install/upgrade/removal may run those scripts, while absent, changed, or unreviewed removal scripts fail closed. Controlled install/upgrade/remove fixtures pass through the production fake-root bridge, and physical Samsung Code install, manager death, authorized removal, capability pruning, scoped logs, and full-device UI pass. Official scriptlets now retain native libalpm ordering through the root command path, and bounded root-contained hook adapters pass both exact ABIs; a signed script-bearing reverse rollback gate remains open.
- [ ] Make installed commands, libraries, desktop files, MIME handlers, fonts, themes, and services immediately discoverable across all Linux apps.
  - [x] Make Android's root-owned `/system/fonts` a read-only host resource for every managed Linux process. Preserve absolute reads and contained directory-FD-relative enumeration without exposing other `/system` paths or mapping the directory into the writable Arch root. Fontconfig now validates a nonempty system cache and resolves `Droid Sans Mono` on both exact ABIs; Foot launches, survives Home/resume, reports crash status, and relaunches on the font-package-free emulator as well as Samsung, with inspected full-device captures.
- [x] Generate, update, reconcile, and remove Android launcher wrappers from desktop entries without deleting shared package or user state incorrectly.
- [x] Derive every launcher owner's package from pacman's bounded local `%FILES%` records, publish launchers only for explicitly installed owners, and fail closed on ambiguous multi-owner desktop paths. Direct hostile/ambiguity tests and the real Code closure prove that unrelated dependency desktop entries do not become Android launchers.
- [ ] Handle package upgrades, downgrades, replacements, hooks, interrupted transactions, rollback, orphan cleanup, and low-storage failures.
  - [x] Complete low-storage diagnosis and retry after recovery for both official and AUR transactions. Exact preflight failures occur before mutation, cache cleanup protects the operation's verified closure and fails closed when it cannot establish one, and Review restores only the exact durable Retry. Real constrained-storage completion passes on the emulator; Samsung covers the same bounded recovery presentation without intentionally filling personal storage.
- [x] Use one generic command policy instead of publishing package-specific exceptions: PATH lookup resolves installed shared-root `/usr/bin` commands, while exact absolute execution may resolve a verified package-owned executable anywhere inside the shared Arch root. Android host commands and root escapes remain unavailable.
- [x] Complete the verified runtime model for package-owned `/usr/lib/<app>` trees, data, scripts, and valid symlinks.
  - [x] Resolve regular nested executables through the verified loader only when their canonical path remains inside the shared root, their executable mode is present, and they are not group/world writable. Host tests cover real ELF execution, spawn, access, escape, and writable-file rejection; a full-device Samsung gate proves a package-owned `/usr/bin` Bash wrapper launching an unmodified ELF from `/usr/lib`.
  - [x] Resolve relative and fake-root absolute symlinks without interpreting them against Android's host root; cap expansion at 40 links and reject loops or traversal above the shared root.
  - [x] Execute bounded nested shebang programs through one installed ELF interpreter, preserve the optional single kernel shebang argument, and reject recursive/non-ELF interpreters. Host exec/spawn probes and the Samsung `/usr/bin` wrapper → `/usr/lib` script → absolute symlink → ELF gate pass.
  - [x] Remove the global `/usr/lib/pulseaudio` loader-path exception. Application-private library directories are now discovered from each launched ELF closure instead of being mixed into every Linux process.
  - [x] Preserve normal `/proc/self/exe` semantics across the explicit glibc loader: publish the verified real target on initial launch and replace stale values on every nested `exec`/`posix_spawn`. Host direct/spawn probes and the current Samsung Foot client pass with the refreshed sealed bridge.
  - [x] Generalize verified absolute/private RUNPATH translation without globally mixing unrelated application-private libraries. Rust initial launch and the exact-ABI C exec/spawn bridge walk at most 256 reachable ELF objects and 256 dependencies per object, export at most 64 canonical root-contained non-world-writable absolute RUNPATH/RPATH directories, keep `$ORIGIN` paths scoped to dependency traversal, and ignore unrelated private trees. A deterministic host fixture proves transitive absolute RUNPATH resolution; interactive Bash → Foot full-device gates and screenshot inspection pass on x86_64 and AArch64.
  - [x] Repeat the nested executable/symlink/script device gate on the x86_64 emulator. Exact-ABI manager builds now pass the complete `/usr/bin` wrapper → `/usr/lib` script → absolute root-internal symlink → ELF chain, real Bash output, fixture cleanup, fatal-log check, and visually inspected full-device capture on both targets.
- [x] Cache unchanged verified closure analysis so repeat installs do not rescan large package archives.
- [x] Cache unchanged wrapper-generation inputs so launcher reconciliation does not repeat large package-tree work. The Rust runtime retains the bounded desktop/ownership/integration catalog after its authoritative package-tree scan; launcher-only installer, review, retry, dismissal, and reconciliation transitions page that exact catalog without scanning packages again, while every package mutation still forces a fresh scan. The physical Samsung bootstrap log proves one 151-package rescan followed by one cached four-entry replay with the same examined/rejected/truncated evidence and a correct full-device app list.
- [ ] Complete and validate the separate 16 KB-aligned x86_64 package/runtime strategy before enabling transactions there.

## P0 - Manager UX and reliability

- [x] Make every install/update/remove operation appear in the app list immediately with persistent state and progress.
  - [x] The current exact-package details flow immediately shows persistent resolve, download, verify, install, complete, and failure state.
  - [x] Replace the ambiguous transaction strip with a stable recent-activity card backed by the existing bounded Rust journal fields: package, operation, state, exact progress, message, static progress track, and state-driven Cancel. Prove durable Complete across manager restart and durable Failed on both exact ABIs with debug-only native journal fixtures, full-device screenshots, and no package downloads.
  - [x] Build the actual installed-package list from one bounded Rust snapshot of pacman's local database; page the cached snapshot through coarse JNI, publish one revisioned Binder object, and render recycled Android rows with exact version and explicit/dependency reason. Prove 67-package multi-page light/dark, mode-switch, scroll, restart, scoped-log, and full-device behavior on the emulator and Samsung without downloads.
  - [x] Build the structured available-package list from Rust's bounded real-pacman search response; publish one revisioned Binder snapshot, render recycled repository/name/version/description rows, route row selection to details, restore the visible query across recreation, and overlay matching durable operation state. Pass generated no-network pacman-catalog light/dark, selection, recreation, failed-row, scoped-log, and full-device gates on both ABIs.
  - [x] Prove a live newly queued operation appears in its matching or appended package row before the worker advances, then complete retry/recovery actions. A generated no-network catalog and one-shot debug-only worker gate prove the real durable Queued row/card, safe Cancel before cache/network/mutation, cold-restored Cancelled/Review, and exact retry gating on both ABIs with visually inspected full-device screenshots. Install/Remove also disable immediately when the visible package field no longer matches the freshly resolved exact package.
  - [x] Show queued, resolving, downloading, verifying, building, publishing, installing, awaiting Android confirmation, completed, failed, and cancelled states. Append the launcher phases without renumbering persisted v1 states, enforce legal forward transitions in Rust, and pass a no-network live durable-state presentation gate with exact progress, cancellation boundary, cold restart, scoped logs, and visually inspected full-device screenshots on both ABIs. This proves manager presentation; the real launcher builder/PackageInstaller pipeline remains separately tracked.
  - [x] Keep state correct across rotation, backgrounding, process death, reboot, and manager restart.
    - [x] Retain bootstrap, catalog, package, command, storage, and shell work when the Activity finishes or its task is dismissed; promote user-started long work to one foreground Service notification, stop only after unobserved work is idle, and pass exact-ABI Recents-dismissal/cold-reopen gates on the emulator and Samsung.
    - [x] Interrupt a real durable pre-mutation package phase with a device reboot, recover it as Failed at the exact prior progress with an enabled Review action and no stale Cancel, and preserve both package database and cache contents. The state-preserving full-device gate passes on the emulator and Samsung, including Samsung's post-boot USB prompt and delayed-service behavior.
  - [x] Provide actionable diagnostics, retry controls, and package-scoped failure isolation.
    - [x] Replace the dead disabled Cancel control with no action for terminal success and a state-driven Review action for durable Failed/Cancelled jobs. Review prefills the exact package and performs a fresh signed-repository/installed-state resolution before enabling any retry or removal; it never replays stale transaction metadata.
    - [x] Keep long package names, phase labels, and the Review/Cancel touch target readable in the fixed-height activity card by stacking name and phase in a flexible column; verify full-device captures on the emulator and Samsung.
    - [x] Add explicit Retry only after the exact durable failure/cancellation revision resolves successfully against current catalogs and installed state. A subsequent failure creates a new revision and requires Review again; catalog refresh locks Review, Retry, and Remove consistently with the executor.
    - [x] Classify network, storage, trust, changed-state, catalog, generic, and post-mutation failures into bounded actionable guidance; show up to three readable lines and verify settled full-device captures for every class on the emulator and Samsung.
    - [x] After an install/remove failure once mutation has begun, refresh the installed-package and shell snapshots before offering Review. If refresh fails, require a manager restart; if the durable journal update also fails, publish a local terminal state rather than leaving the UI stuck on Installing.
    - [x] Add a one-tap Clear cache recovery for storage failures. Kotlin resolves and protects the current verified closure, conservatively protects the complete cache if that resolution fails, and sends only unrelated package names through the bounded selected-cleanup Rust/JNI boundary. Rust validates the manager-owned cache fail-closed, deletes only selected recognized archives/signatures/partials, syncs the directory, and reports reclaimed bytes. The handled result is bound to the persistent job identity, survives process restart, and retains foreground work through Recents dismissal; state-preserving exact-ABI gates cover empty and populated caches without changing pre-existing verified downloads.
    - [x] Add package-specific cache selection and disk-use controls, one-tap catalog refresh from the activity card, and a verified repair/rollback workflow for interrupted or partial pacman transactions. Downloads shows total and per-package/version bytes with multi-select cleanup and separately confirmed Clear all; Files reports package-download storage; durable catalog/trust failures expose Refresh package catalogs; retained mutations expose Repair and, when an exact prior signed closure exists, Roll back.
      - [x] Add one-tap signed catalog refresh for catalog/trust failures directly on the durable activity card. Successful recovery is keyed to the exact job, persists across process restart, and returns to Review; a state-restoring no-network gate passes with full-device captures and unchanged 259/248-package databases on both exact ABIs.
      - [x] Add a Downloads inventory with total disk use, version-grouped package rows, multi-select cleanup, and a separately confirmed Clear all action. Rust scans the bounded manager-owned pacman cache fail-closed before deletion, JNI pages a retained snapshot, and cleanup is serialized with package/catalog work. State-preserving exact-APK gates on the emulator and Samsung remove only a selected generated package, preserve every prior cache entry, leave the 259/248-package databases unchanged, and pass full-device visual review.
  - [x] Show a state-driven Cancel action only before Linux package mutation and retain the durable Cancelled result across manager restart.
- [x] Make the current package details and operation content responsive and scrollable; full-device emulator and Samsung audits show the complete `btop` closure with state-driven Install/Update/Verify and Remove actions.
- [x] Profile and fix slow first launch after boot; retain the visible runtime-loading state while avoiding unchanged verification-keyring rebuilds.
  - [x] Cache the derived GPG keybox only for the exact immutable packaged keyring/ownertrust identity, reject unsafe or oversized cache entries, and prove real signed-package verification after reuse.
  - [x] Gate steady cold-process readiness below 1,000 ms and post-boot readiness below 1,500 ms on both targets; final steady reuse is 237 ms on the emulator and 222 ms on Samsung, with post-boot reuse at 442 ms and 1,222 ms respectively.
- [ ] Perform a complete UX pass over discovery, package details, installation, launcher creation, updates, storage, permissions, setup, settings, and recovery.
  - [x] Establish a dependency-free light/dark visual baseline for the greenfield manager: use theme resources instead of hardcoded dark panels, sentence-case native actions, readable selector states, and a user-facing readiness header while retaining debug runtime evidence outside the visible text. Pass reversible full-device light/dark and lifecycle gates on the emulator and Samsung.
  - [x] Replace the dense single-screen scaffold with focused Packages, Files, and Terminal navigation; preserve the selected section across Activity recreation; keep an active shell alive when navigating away; and pass full-device portrait/landscape, onboarding, lifecycle, and persisted-folder gates on the emulator and Samsung.
  - [x] Add an allocation-stable 840 dp adaptive branch with a persistent navigation rail, two-column package workspace, side-by-side file actions, and a maximized terminal surface. Pass reversible accessibility-geometry and full-device visual gates at tablet and external-display-sized configurations, then prove restoration to the phone branch and rerun Samsung phone navigation.
  - [ ] Validate the adaptive branch on a real Samsung DeX or physical external display, including live display moves, keyboard/mouse focus, window resizing, and density changes; the current gate safely simulates the Android configurations on the emulator but is not physical-display evidence.
  - [x] Audit the populated installed-package list and durable terminal Complete/Failed cards in the new navigation on both exact ABIs, including light/dark full-device views, pagination, and manager restart.
  - [x] Audit populated available search rows, row-to-details selection, query/result recreation, and matching durable Failed state on both exact ABIs with generated local pacman databases.
  - [x] Audit full populated details, every live durable transaction phase, post-review Retry/recovery, and an active terminal in the new navigation. The final state-preserving shell gate found and fixed a real Back/reopen regression: the foreground shell survived but the finished Activity forgot that Terminal was selected. The manager now durably restores its last section, and current selected-shell, rotation, Home, Back, exit, restart, and reap flows pass with visually reviewed full-device captures on the emulator and Samsung.
  - [x] Suppress unrelated terminal activity cards while a package/AUR workflow is selected and retain only its compact relevant completion state, so an old transaction cannot obscure a phone-sized review.
  - [x] Replace the AUR review's long plain-text evidence dump with a compact summary and mutually compact expandable source, trust, build-environment, digest, and recipe sections while retaining exact accessible text and selectable/copyable values. A bounded no-network fixture proves collapsed defaults, expanded accessibility state, exact evidence, scrolling, and unchanged pacman state with visually reviewed full-device captures on both exact ABIs.
  - [x] Add expandable/copyable AUR logs. The default product view uses one compact live phase line, a verified output summary, and a clear Build/Install/Verify next action without exposing the raw makepkg transcript; the same emulator/Samsung gate proves the transcript remains available on demand.
  - [x] Make launcher publication a reviewed, recoverable workflow instead of an automatic series of Android prompts.
    - [x] Stop automatically resubmitting user-cancelled Android launcher installations after manager restart. Persist distinct Cancelled and Dismissed states, require explicit Retry/Dismiss, and verify cancel/restart/retry/install with full-device screenshots and scoped logs on the emulator and Samsung.
    - [x] Hold two or more newly discovered desktop entries in one durable review batch, default them selected, support Skip all/Not now/Add selected, and expose dismissed launchers through the package-screen status for later re-enablement. Prove the atomic Rust transition and real generated-wrapper flows on the emulator and Samsung, then remove every temporary package record, desktop entry, and wrapper.
    - [x] Recover deterministic launcher identities after manager registry/signing-state loss without silently downgrading or overwriting an Android package. Trusted higher-generation wrappers advance the desired generation; signer-blocking wrappers remain visible as failures until explicit Retry, which preserves the removal state across catalog refresh, opens Android's uninstall confirmation, and republishes only after confirmed removal. The real emulator Foot identity recovered through full-device uninstall/install prompts and returned to a trusted Current state.
- [ ] Review Obtainium's source, license, screenshots, app-list structure, and update progress UI. Adapt suitable open-source patterns to Archphene's compact list, spinner, and richer phase strings without copying blindly.
- [x] Ensure search results distinguish graphical apps, CLI tools, libraries, installed packages, available updates, AUR results, unsupported packages, and compatibility status.
  - [x] Preserve pacman's authoritative installed marker across the bounded Rust/JNI snapshot and show Official, Installed, or the exact differing installed version without name/description heuristics. Current, differing, and available fixtures pass light/dark full-device emulator inspection; the physical Samsung shows its real installed Foot package while preserving all 255 installed packages.
  - [x] Derive GUI, CLI, library, and compatibility classes from verified package file/capability metadata; integrate AUR results and a version-order-safe update state without guessing from names or descriptions.
    - [x] Derive bounded Graphical, CLI, Library, System, and Data classes from installed pacman file-ownership records, keep missing/unverified candidate metadata explicitly Not analyzed, and use pacman/libalpm ordering to distinguish updates from locally newer versions. The manager refuses an automatic downgrade. Multi-page installed and four-state local-search fixtures pass exact-ABI light/dark full-device visual gates on the emulator and Samsung.
    - [x] Derive package-specific bridge compatibility/unsupported diagnostics from the verified candidate closure and launcher capability review rather than treating a file class as a support claim.
      - [x] Stream every signed cached archive in the freshly re-resolved closure before package mutation; bound paths, links, entry count, expanded bytes, and per-entry bytes; derive the exact target class; and reject runtime ELF ABI, architecture-any native content, malformed ELF, actual Android page-size alignment, and unsupported command-format blockers. Search/details remain explicitly Not analyzed until those signed bytes exist, Bridge eligible explicitly means not validated, and blockers name the exact closure package. A successful review creates a single-use in-memory capability bound to the exact resolution digest; install/update must consume it before pacman mutation and still reverifies every archive. Rust hostile/ABI/page/compression/capability tests plus real signed `btop` install/tamper/remove/reinstall/Terminal gates pass on the emulator and Samsung across 139/138-package closures and 1,628/1,624 native ELF files.
      - [x] Cache unchanged closure reviews under a bounded content address derived from the exact resolution, ABI, actual page size, immutable keyring/owner-trust identity, and every archive/signature byte. Canonical checksummed mode-0600 records are published atomically, corrupt derived records are discarded, changed package/signature/trust inputs miss, and at most 1,024 entries survive. Cache-hit install/tamper/remove/reinstall gates pass on both exact ABIs.
      - [x] Make large first-pass scans granular and cancellable. Rust checks one shared token at every 64 KiB content-hash chunk, decompressor read, archive-entry boundary, package verification boundary, and capability-publication boundary. Details exposes the live review and Cancel action; install/update uses the same lock-free JNI cancellation path, and service shutdown cancels before joining workers. Cold 139/138-package exact-ABI gates cancel before mutation, restore the package database/cache unchanged, and pass inspected full-device light/dark captures on the emulator and Samsung.
      - [x] Combine the archive result with installed desktop-source/executable ownership, the exact broker-capability contract, and current generated-wrapper publication state. `Terminal=true` desktop entries remain shared-Terminal commands instead of impossible Android wrappers. Real Foot installs on the emulator and Samsung recover stale signer conflicts through the reviewed manager flow, publish three trusted launchers, survive manager restart, report Ready, and launch the native-Wayland terminal from a full-device gate.
      - [x] Make the generic GTK settings preload self-contained instead of assuming every graphical executable already exports GLib. The x86_64 and AArch64 modules now explicitly link GIO/GObject/GModule/GLib; the ARM build uses a checksum-pinned Arch Linux ARM sysroot. The Foot gate first reproduced the unresolved `g_object_unref` failure in a non-GTK process and then proved clean Wayland mapping on both devices.
      - [x] Add signed dependency-owned Qt Wayland runtime companions generically during package resolution. A closure containing or an existing root owning `qt5-base`/`qt6-base` adds `qt5-wayland`/`qt6-wayland` unless already present, so desktop entries never require an application-specific patch or a manual optional-dependency guess. A clean emulator `qt5ct` review visibly includes `qt5-wayland` in the 23-package closure and installs both; a migrated Samsung root was repaired through the same manager UI before the stock Qt 5 launch gate.
      - [x] Derive and expose a bounded static topology from each exact installed ELF executable and its recursively resolved dependency graph without reading whole binaries into memory. Package details distinguish Qt 5/6, GTK 3/4, SDL 2/3, native Wayland, X11 linkage, OpenGL/EGL, and Vulkan; scripts remain explicitly unresolved. Hostile/truncated ELF tests pass, and real current Foot installations report Native Wayland on the emulator and Samsung.
      - [x] Merge observed process-image topology for scripts, `dlopen`/plugin-only dependencies, Electron/Chromium, and other runtime-loaded hybrids without treating the result as a support claim. The manager scans only each supervised process group on a two-second startup warm-up that backs off to a 30-second steady cadence, streams maps through fixed stack buffers, verifies Android `/data/user/0` and `/data/data` root aliases by device/inode identity, recognizes exact loaded toolkit/protocol/graphics SONAMEs plus observed Chromium process roles, and atomically persists accumulated evidence against the exact launcher generation. Registry v4 migration, stale-generation rejection, descriptor-change reset, hostile map/root tests, and explicit static/observed/partial provenance pass; real Foot launches persist Native Wayland observations on the emulator and Samsung.
    - [x] Integrate reviewed AUR results into the same source, class, installed-version, update, and compatibility presentation. The bounded search snapshot now retains matching official rows and appends exactly one reviewed AUR row with explicit source, exact candidate and installed versions, installed file class, and `Candidate: Not analyzed` provenance. Pacman's own dependency comparator determines update versus locally newer without a second version algorithm. No-network full-device light/dark gates prove absent and real installed-Foot states, source-preserving durable activity, and row-to-retained-review navigation on both exact ABIs.
- [x] Add clear disk-use estimates and controls for package archives, shared runtime data, build caches, and user files. The Files page now reports package downloads, the installed shared Linux system, manager-plus-Builder AUR build data, Archphene Home, and device free space from one bounded no-follow allocated-block inventory. Downloads opens package-selective cleanup, the shared system routes to Packages, Home routes to Android's document browser, and AUR cleanup is separately confirmed and clears only the manager's reviewed caches/outputs plus the isolated Builder workspace. Empty structural directories no longer create an impossible nonzero cleanup state. A repeatable exact-ABI UI gate clears a 13 MB cross-UID fixture to 0 B on the emulator and Samsung while downloads, installed-package state, and Home remain exact; a real Samsung cleanup reclaimed 4 GiB without changing its 122 MB downloads, 2.3 GB installed system, or 86 kB Home.

## P0 - VS Code and .NET daily-use milestone

- [x] Install Code through the generic package/AUR pipeline with no Code-specific bridge exceptions.
  - [x] On x86_64, the generic official-package path installs the current 198-package, 428 MiB Code closure and publishes the Code launcher. Catalog reconciliation now excludes graphical entries owned only by dependency packages, so Code no longer exposes unrelated Avahi launchers.
  - [x] Build, independently verify, and install current `visual-studio-code-bin` through the bounded AUR workflow on physical AArch64 Samsung hardware, including all 250 official closure packages and the generated Android launcher handoff.
- [ ] Validate Electron/Chromium multiprocess startup, Ozone Wayland, sandbox behavior, rendering, IME, clipboard, dialogs, file watching, extensions, and lifecycle.
  - [x] Resolve Electron's standard PulseAudio private-library dependency through the generic runtime environment.
  - [x] On Samsung, pass Chromium's `/proc` directory-FD probes, force raw `statx` to the translated libc fallback, provide app-private shared-memory/temp paths, translate absolute `dlopen`/`dlmopen` modules, and preserve logical `/proc/self/exe` re-execution.
  - [x] Reconstruct the sealed bridge/root/loader/library environment when Chromium deliberately removes `LD_PRELOAD` from utility-process environments. A clean-environment host re-exec regression passes, and the stock AArch64 Code launcher reaches a stable workbench with persistent shared-process, watcher, and extension-host Node services instead of deliberate `SIGTRAP` exits.
  - [x] Translate logical home-directory scans and inotify watches, provide a managed `openpty`/`forkpty` path with controlling-terminal and job-control semantics, and reap Code's integrated Bash when the Android launcher is force-stopped. Physical Samsung full-device evidence shows a warning-free integrated terminal.
  - [ ] Replace the temporary user `code-flags.conf` test setup with a reviewed generic Electron/Chromium compatibility profile. Android currently requires `--no-sandbox` and `--disable-dev-shm-usage`; document the reduced Chromium process isolation inside Archphene's Android sandbox and require an explicit product policy before enabling it automatically.
    - [x] Add a default-off manager policy with explicit reduced-isolation confirmation, persist it outside app-specific configuration, carry it through the coarse Kotlin/JNI launcher boundary, and append the two required flags only when the exact desktop entry or retained launcher observation contains Chromium topology. Unrelated launchers remain unchanged and pre-existing flags are not duplicated. Exact-ABI emulator/Samsung gates pass warning, Cancel, Enable, process-restart persistence, state restoration, and visually inspected full-device light/dark presentation.
    - [x] Reinstall/reconcile current Code on both device roots and prove from its supervised process trees that the generic policy launches without `code-flags.conf`, applies both required flags, preserves default-off behavior, and leaves a non-Chromium launcher untouched. Current-source x86_64 emulator and AArch64 Samsung Code launchers both retain the required flags with no user `code-flags.conf`; a freshly installed current-source Foot launcher on x86_64 authenticates, renders, and retains its normal argument vector without Chromium flags. Full-device captures and scoped process trees were inspected for all three launchers.
  - [ ] Restore and validate accelerated rendering; then pass crisp phone/IME resize, touch, mouse, hardware keyboard, clipboard, dialogs, file watching, extensions, network/DNS, Home/resume, and clean-close gates.
    - [x] Remove the diagnostic `--disable-gpu` flag on both exact-ABI Code installations. Full-device rendering and normal close remain stable; Samsung creates a GPU process but reports `--use-gl=disabled`, so this proves the flag is unnecessary rather than claiming hardware acceleration.
    - [ ] Add the compositor presentation/readback capability Chromium requires before enabling the manager-owned virgl helper for Chromium topology. A live helper plus direct EGL, ANGLE, and blocklist diagnostic profiles still produced `--use-gl=disabled`; the profiles were removed rather than shipping an ineffective app-stack override.
    - [x] Publish the active Android network's bounded DNS servers into a no-follow managed glibc resolver configuration and refresh it on network changes.
      - [x] Publish up to four validated, canonical IPv4/IPv6 Android `LinkProperties` DNS addresses through bounded direct JNI into an atomic mode-0600 Rust-owned resolver file. Preserve validated scoped IPv6 interface identifiers, reject hostile root entries, avoid unchanged-file churn, refresh on default-network changes, and retain the last usable configuration while offline.
      - [x] Rebuild both checksum-pinned patched glibc runtimes with internal private-root resolver loading. One live process proves initial resolution, failure after an atomic unavailable-resolver replacement, and success after restoration; real TLS/Open VSX API results, exact mapped libc identity, scoped fatal logs, and visually inspected full-device screenshots pass on the x86_64 emulator and AArch64 Samsung.
    - [x] Complete a real extension-gallery workflow on both exact ABIs. Code - OSS installs Red Hat YAML from Open VSX and Visual Studio Code installs it from Microsoft's gallery; both retain the extension, open a real YAML file, activate the extension host on `onLanguage:yaml`, and return document providers/diagnostics.
    - [x] Repair generic GTK file dialogs inside Chromium/Electron without patching Code. The package runtime now publishes checksum-pinned, no-Glycin GdkPixbuf/librsvg libraries plus a bounded mode-0600 SVG loader cache, avoiding Android-incompatible Bubblewrap helper startup. Full-device captures prove the native chooser and its SVG icons on the emulator and Samsung.
  - [x] Repeat Electron startup, Ozone/Wayland rendering, and the descendant-process gate on x86_64. The workbench, shared process, Node workers, extension host, file watcher, and integrated PTY remain live; full-device cold launch and close/relaunch pass without an automatically restored Android IME.
- [x] Install `dotnet-sdk` through the same shared package system and make `dotnet` available in Code's integrated terminal.
  - [x] Prove immediate row/activity progress, exact 56-package shared-root closure handling, complete signature verification, and non-mutating low-storage rejection on a deliberately undersized x86_64 root.
  - [x] On a clean 12 GiB emulator data partition, install the current official SDK and complete shared-root closure through the normal manager UI; `dotnet --info`, restore, and build execute through the generic bridge. A second absent-HOME gate proves first-run NuGet config creation and mode-0600 permission handling without manual repair; the underlying preload-free glibc path passes on x86_64 and physical AArch64.
  - [x] Prove the same `dotnet` executable and shared project tree inside Code's integrated terminal: `dotnet --info`, `dotnet new mvc`, restore, build, and run pass from the x86_64 generated Code launcher.
  - [x] On physical AArch64, review, build, independently verify, and atomically install the six-output `dotnet-core-bin` split package through the normal AUR UI. Samsung runs SDK 10.0.302, creates/restores/builds/runs an MVC project in shared Home, serves it to the Android browser, and reaps the Kestrel descendant when the shell is stopped.
- [x] Create a new ASP.NET Core MVC project in shared Arch storage.
- [ ] Open the project in Code and validate editing, search, Git, terminal PTY, language services, restore/build, and extension-host subprocesses.
  - [x] Open the shared `ArchpheneMvp` folder through Code's stock GTK chooser on Samsung and verify the resulting Explorer tree with a full-device screenshot.
  - [x] Persist and reopen the project, grant Workspace Trust, search the tree, edit and save through the phone UI, restore the exact original bytes, and complete `dotnet build` in Code's integrated terminal on Samsung.
  - [x] Install and validate `git` through Archphene's normal package UI. With user approval, the public package flow installed stock Git 2.55.0 into each shared root. Both the manager Terminal and Code's integrated Bash resolve it immediately; a repeatable exact-ABI gate runs a real `git init`, `git add`, and porcelain-status transaction, verifies `A  sample.txt`, cleans its bounded workspace, rejects fatal logs, and captures the full device on the x86_64 emulator and AArch64 Samsung.
  - [ ] Install a reviewed C# extension/debug adapter with user approval, then validate language services and extension-host subprocesses. The clean Samsung Code profile currently offers Microsoft's C# Dev Kit but has no C# extension installed.
  - [x] Give Code a usable phone-first workspace layout without application patches. Settings now explains that Auto prioritizes readable phone content while the generic remembered 75% App scale fits multi-pane desktop work. Current Code - OSS and VS Code persist that choice across manager restart, render an exact 576-logical-pixel-wide workspace and integrated terminal, emit no fatal logs, and restore the prior preference in a repeatable full-device gate on the x86_64 emulator and AArch64 Samsung. Code's standard pane controls remain responsible for hiding Explorer or Chat rather than Archphene rewriting application state.
  - [x] Fix compact secondary-window presentation/input using Code's unsaved-file confirmation as a regression. The compositor now centers the stock 404×240-logical dialog against the current Android output instead of a primary frame left stale during IME resize. Samsung full-device evidence covers keyboard-visible mapping, IME hide/height restoration, correct 14,320 logical placement, and a tap on the visible Don't Save target dismissing the dialog without changing `Program.cs`.
  - [x] Report managed child executables rather than the sealed glibc loader to parent process inspection. The bridge validates the bounded initial exec environment, translates same-UID `/proc/<pid>/exe`, and exposes a read-only bounded `/proc/<pid>/cmdline` stream with the verified Linux `argv[0]` plus preserved arguments through libc, fortified, `fopen64`, and raw-syscall paths. The native cross-process regression passes, and a newly launched Samsung Code terminal is visibly labeled `bash` instead of the content-addressed loader.
  - [x] Restore the checksum-pinned generic Qt/GTK appearance bridge in the current Rust/Kotlin launch path. Rust now atomically publishes bounded GTK 3/4 and KDE configuration, exports separate text/control/theme values without global geometry scaling, and loads the verified GTK settings module plus complete Qt platform-theme/style topology. Stock Mousepad follows live Android light/dark changes on both exact ABIs without an app or process restart; broader toolkit validation remains in the generic appearance matrix.
  - [x] Fix and regress the Samsung Code native folder chooser's displaced header actions. The compositor now sends `wl_surface.enter` and preferred buffer scale to every mapped surface, not only the surface focused when `wl_output` was bound. The chooser commits scale 3, a tap on the visible Open action activates it, and the selected shared project opens.
  - [ ] Replace phone-sized native desktop choosers with generic Android DocumentsUI/desktop-portal paths, or another reviewed mobile secondary-window policy. Correct output scale and input work, and stock GTK 3 Open, Save As, and folder selection no longer expose its roughly 1,390-logical-pixel chooser on a 432dp phone. Other toolkit callers still fall back to desktop UI.
    - [x] Route stock legacy GTK 3 single-file Open and Save As dialogs through Android DocumentsUI without patching Mousepad or another packaged application. Open imports a bounded durable copy under `~/Documents/Android`, preserves the display name, and atomically numbers collisions; Save As exports exact bytes through the selected Android descriptor.
    - [x] Route stock legacy GTK 3 multiple-file Open through Android DocumentsUI. Protocol v3 transfers at most 32 descriptors and 512 MiB per batch, rejects duplicate Android URIs and malformed result shapes, reserves non-replacing Linux destinations, rolls back the complete batch on copy failure, and preserves every selected file through GTK's plural getters. Cancellation, exact bytes, repeated single-file behavior, full-device visual inspection, and scoped logs pass on the x86_64 emulator and AArch64 Samsung.
    - [x] Route GTK 3/XDG folder selection through Android `ACTION_OPEN_DOCUMENT_TREE`. Protocol v4 streams a bounded tree without exposing Android URIs to Linux, and Rust atomically publishes a no-follow, non-replacing `~/Projects/<folder>` snapshot. Nested files, dot directories, empty files, exact hashes, collision numbering, cancellation, scoped logs, and full-device picker/application frames pass on the x86_64 emulator and AArch64 Samsung.
    - [ ] Validate Qt 6/KDE, GTK 4/libadwaita, and Electron folder callers.
- [ ] Run the project under the debugger, stop at breakpoints, inspect state, continue, and restart.
- [x] Open the served localhost URL in Android's browser and validate routing back to the running Linux process.
- [ ] Validate the workflow with touch/IME on a phone and keyboard/mouse on tablet or external display.
- [ ] Repeat the complete milestone on the x86_64 emulator and physical AArch64 Samsung using full-device screenshots and scoped logs.

## P1 - Generic desktop integration quality

- [x] Replace global `QT_SCALE_FACTOR` compatibility behavior with compositor-advertised logical size plus integer/fractional output scale; physical Samsung portrait/landscape reconfiguration passes without app-specific settings.
- [x] Extend the compositor density gate to a bounded user-selected geometry scale. Auto retains the 432dp desktop floor; explicit 75%, 100%, 125%, and 150% values scale that automatic baseline in the compositor rather than injecting toolkit-specific geometry variables. Native tests cover every accepted bound, and Samsung Code proves the 125% output resolves to 346×705 logical pixels.
- [ ] Validate live moves between Android displays while an explicit geometry scale is active.
- [ ] Define a generic overflow/panning policy for fixed desktop layouts at 200% phone text scaling without app-specific patches.
- [ ] Finish reproducible AArch64 Qt/KDE and GTK settings bridge builds by pinning the required KConfig and GLib development sysroots.
  - [x] Decouple unchanged-helper availability from rebuilding changed Qt modules. The existing AArch64 KF6Config helper may be retained only when its exact pinned checksum matches, while the cached checksum-pinned Qt sysroot rebuilds the platform-theme/style modules; missing or altered helpers fail closed without a download.
- [ ] Complete secondary-window behavior for phone, tablet, freeform, and external displays.
- [ ] Provide a generic file-dialog bridge from Linux portal requests to Android DocumentsUI while preserving Archphene Home access through the existing `DocumentsProvider`, cancellation, multiple selection, save/create, and descriptor safety.
  - [x] Complete the single-file SaveFile/create slice for stock GTK 3 applications with exact descriptor mirroring, bounded stable-write detection, cancellation, exactly-once completion, worker shutdown, symlink-safe crash recovery, and dual-device full-screen regression evidence.
  - [x] Complete the GTK 3 single-file OpenFile slice. Android URI/grant data remains outside Linux; the bridge imports at most 512 MiB into a non-replacing `~/Documents/Android/<display name>` file, preserves exact bytes and the visible name, handles cancellation, and passes full-device/log gates on both exact ABIs.
  - [x] Complete the GTK 3 multiple-file OpenFile slice with a 32-document/512 MiB batch bound, exact Binder descriptor ownership, duplicate-URI rejection, non-replacing collision names, failed-batch cleanup, and retained plural GTK results. Cancellation and exact two-file application-visible results pass on both exact ABIs.
  - [x] Implement Settings portal v2 for standardized appearance color scheme, accent, contrast, and reduced-motion reads. Honor no-reply D-Bus calls so GTK startup no longer emits the rejected reply diagnostic; the contract passes on both exact ABIs.
  - [x] Add single-folder selection through `ACTION_OPEN_DOCUMENT_TREE` and a bounded versioned stream. Preserve empty files and dot directories, reject malformed/traversing/oversized records with rollback, number destination collisions, cancel cleanly, and pass exact-content/full-device gates on both exact ABIs.
  - [x] Put SAF folder query, descriptor-open, and read operations behind a sliding 30-second watchdog. First request cancellation/descriptor close, then terminate only the generated launcher if a provider remains blocked so the manager can roll back Rust staging. Debug providers that deliberately ignore cancellation pass deadline, manager-survival, no-partial-publication, normal-retry, scoped-log, and inspected full-device gates on Android 16 x86_64 and Samsung Android 15 AArch64.
  - [x] Exercise provider query and descriptor-open failures through the real Android DocumentsProvider/Binder path on both exact ABIs. Keep launcher and manager alive, publish no partial project, show a readable user-facing failure message, and prove an exact normal retry with inspected full-device frames.
  - [x] Import the exact 10,000-entry contract boundary through the real DocumentsProvider on both ABIs: three provider cursors, 9,997 files, sampled hashes, empty-file preservation, process survival, fatal-log checks, and inspected full-device frames. The emulator completed in 13 seconds and Samsung in 49 seconds.
  - [x] Prove the provider watchdog is sliding: a real pipe-backed DocumentsProvider returns three 16-byte chunks 20 seconds apart, taking 40 seconds overall without false timeout on either ABI. Show a delayed native progress surface with an indeterminate indicator and supporting text, clear it on completion, preserve exact bytes, and inspect full-device progress/completion frames.
  - [x] Preserve XDG chooser MIME filters through Android DocumentsUI. The bounded portal parser honors the selected filter (or first available filter), canonicalizes up to 16 MIME rules, maps multiple rules to Android's required `*/*` plus `EXTRA_MIME_TYPES` contract, and falls back safely when a glob-only filter has no Android equivalent. A real two-type chooser disables an image decoy, imports exact bytes, and passes inspected full-device/log gates on Android 16 x86_64 and Samsung Android 15 AArch64.
  - [ ] Validate equivalent Qt 6/KDE, GTK 4/libadwaita, and Electron chooser callers.
  - [x] Define explicit Android-provider writeback and conflict semantics for opened documents. XDG Open always imports a non-replacing editable snapshot under `~/Documents/Android`, releases the Android descriptor, and never writes that snapshot back to the provider; Save As is the only outbound desktop-portal operation and always targets an explicit Android destination. Reopen creates a collision-numbered snapshot, while connected project trees retain their separate baseline/conflict-copy synchronizer. The GTK 3 Open gate now edits the Linux copy and proves the original Android document remains byte-exact on both ABIs.
  - [x] Emit Settings `SettingChanged` updates when a live portal-visible appearance value changes. Each private portal consumes an exact 11-byte, versioned appearance record that the manager publishes with fsync plus atomic rename off the main thread; the reader rejects links, non-regular files, malformed data, and partial replacements. Android configuration/display changes and wallpaper-color changes refresh color scheme and Material You accent without restarting the Linux process. Real light→dark→light signals match subsequent `ReadOne` results on Android 16 x86_64 and Samsung Android 15 AArch64, with inspected full-device frames and fatal-log gates.
- [ ] Validate automatic and explicit appearance settings across:
  - [ ] Qt 6/KDE
    - [x] Replace the Qt platform theme's 500 ms polling timer with a deferred event-driven exact-file watcher that survives atomic `kdeglobals` replacement and falls back to the directory only when the file is absent. A native gate loads the exact production plugin and requires a same-process light-to-dark palette change; rebuilt x86_64 and AArch64 modules are installed in the current manager APKs. KCalc visual, interaction, font, control, and explicit-setting device coverage on the new shared root remains open.
  - [x] GTK 3 — stock Mousepad now consumes the standard embedded Adwaita light/dark variant through the manager-owned settings file and an event-driven GTK module. The Rust runtime republishes only color fields without altering the current published font/control snapshot. Exact light→dark→light gates on Android 16 x86_64 and Samsung Android 15 AArch64 require unchanged launcher, manager, and Linux PIDs; GTK's applied state; fatal-log cleanliness; and measured full-device GTK content plus Android system-bar luminance.
  - [ ] GTK 4/libadwaita
  - [x] native Wayland/Foot — Samsung proves both the 150% explicit geometry workspace (288×587 logical output) and the restored automatic phone workspace (432×881), with full-device visual inspection and real shell input. The generic supervised-process bridge now recognizes Foot's `ptsname` → close-master → `setsid` → reopen-slave controlling-terminal sequence without releasing ordinary GUI descendants from manager supervision; exact host PTY regressions and the sealed AArch64 runtime pass.
  - [ ] SDL
  - [ ] Electron/Chromium
  - [ ] XWayland
- [x] Keep geometry scale, text scale, visible control size, and touch target size distinct, documented, persisted, and predictable after relaunch.
- [ ] Evaluate safe live appearance updates per toolkit; keep relaunch semantics when a toolkit cannot update geometry, fonts, and controls consistently.
  - [x] GTK 3 live color-scheme and Material accent publication. The bridge uses atomic bounded files plus a directory change monitor rather than steady 250 ms polling, while font percentage, control visual size, touch target size, and geometry retain relaunch semantics.
  - [x] Qt 6 live color-scheme and Material palette publication. The platform theme watches atomic `kdeglobals` changes without polling, reparses KF6 configuration, publishes application/widget palette notifications, and re-arms file watches after replacement. Font, control, touch-target, and geometry changes retain relaunch semantics.
- [x] Expose three described appearance controls in the manager with Auto as each default: app geometry scale, app text at explicit 100–200%, and control visual size in dp. The bounded ticked sliders retain the requested 20dp phone control default and keep touch targets separate. Samsung proves all three overrides persist, an active Code session keeps its launch snapshot after the settings return to Auto, and a fresh Code launch resolves Auto again. The x86_64 emulator independently passes the full-device manager layout and accessibility-tree check.
- [ ] Validate Android light/dark, explicit Archphene override, Material You accents, font settings, phone/tablet/docked auto policy, and runtime display changes.
  - [x] Fix and regress the generated launcher's dark-mode system-bar contrast and GTK 3 live toolkit handoff. Generated launchers explicitly repaint decor, content, status, and navigation backgrounds when Android configuration changes. The shared Rust runtime publishes live colors through primitive JNI without restarting or resizing apps; current GTK uses `Adwaita` plus the standard prefer-dark variant instead of the invalid `Adwaita-dark` resource name or the debug-only `GTK_THEME` override. Exact-ABI gates measure full-device GTK content and both system bars in light/dark frames, verify GTK diagnostics and Settings portal readback, keep all three PIDs stable, and reject fatal logs.
- [ ] Continue using full-device screenshots, rendered-pixel checks, accessibility trees, content geometry, input traces, and logs for visual claims.

## P1 - Compatibility and performance

- [ ] Complete the release representatives in `docs/compatibility-matrix.md`; package search or launch alone is not a support claim.
- [ ] Finish SuperTux gameplay, pointer capture, audio focus/interruption, and fullscreen/window transitions on emulator and Samsung.
- [ ] Validate XWayland with a representative unmodified X11 application.
- [ ] Validate an Android-backed Vulkan presentation path; keep Vulkan presentation unclaimed until `vkcube-wayland` renders.
- [ ] Add a low/zero-copy Android HardwareBuffer or dmabuf presentation path while retaining SHM fallback.
- [ ] Broaden testing to Rust-native, browser, office, creative, multimedia, accessibility, USB, and multiwindow applications.
- [ ] Validate sustained external-display use with keyboard/mouse, display hotplug, density changes, audio routing, sleep/resume, and thermal/memory pressure.

## P1 - Test and release gates

- [x] Finish the remaining standalone Bash-script assertion audit and run each applicable entry point.
  - [x] Review the final 11 mechanically migrated entry points against their removed PowerShell sources. Restore provider-readiness, fresh-probe, ABI-aware catalog, visible-window/touch, fatal-log, runtime-descriptor, local-container, and prior-state assertions; retire the misleading hand-built KCalc version fixture in favor of the supported manager-generated replacement gate.
  - [x] Pass all applicable host and device lanes: Bash syntax/help, x86_64 manager-native catalog, both PipeWire container gates, dual-ABI drag/drop, and Samsung ARM bridge, legacy catalog, Mousepad touch, Qt/GTK descriptor, and Android-confirmed KCalc replacement. Record the 16 KB gates as unavailable because only 4 KB targets are connected, and do not manufacture an ARM legacy-static result from the incompatible greenfield APK or repopulate the emulator's deliberately empty legacy-wrapper catalog.
- [ ] Keep tests state-preserving by default and require explicit flags for destructive device changes.
  - [x] The signed package regression preserves state by default and requires `--clean-data` for uninstall/data reset.
  - [x] Require explicit `--clean-data`/`--reset-data`/`--reset-app-data` opt-in for every standalone `pm clear` path that does not archive and restore the exact app sandbox. Thirteen clean-start manager/bridge gates now reject ordinary invocation before ADB access; the generated camera gate retains its exact archive/restore path. Terminal project-tree coverage now preserves data by default, uses per-run aliases/folders, fails closed on collisions, and removes only its own mapping, mirror, state, grant, and Android folder; the physical Samsung default-preserving run leaves no test marker.
  - [x] Make every existing optional-install entry point non-installing by default. Forty-six manager/Terminal/document/capability gates now require explicit `--install-apk` before `adb install -r`; the former `--skip-install` remains a compatibility alias, and documented development commands show the mutation. The 13 clean-data gates require separate explicit authorization for both APK replacement and sandbox reset. Default emulator manager, physical Samsung project-tree, Samsung document-open, and emulator document-share runs pass without replacement; Samsung's installed Terminal APK remains byte-identical. Android chooser captures wait for a visible, actionable system chooser instead of accepting activity metadata while the prior picker or an unrendered transition is still on screen.
  - [x] Make interrupted-session tests non-installing by default, reject an already active shared shell before mutation, preserve notification permission and prior manager running state, and require `--allow-reboot` before the reboot gate can contact ADB. Process-death and real-reboot paths pass on the emulator with inspected full-device recovery frames.
  - [x] Add an executable standalone-script mutation-policy gate covering app-data reset, optional APK install, reboot, and permission/settings restoration contracts. It exposed and closed implicit reboot/install/running-state gaps in startup and package-reboot coverage. Package reboot now restores exact durable job/recovery hashes and leaves package/cache state unchanged; default startup and explicitly authorized reboot recovery pass with inspected emulator frames.
- [ ] Require emulator and physical-device coverage for runtime, storage, package, input, visual, permission, and lifecycle changes.
- [ ] Capture device screenshots rather than app-only frames whenever asserting what the user sees or where touch lands.
- [ ] Add long-running upgrade, package churn, process-death, reboot, storage-pressure, network-failure, and recovery tests.
- [ ] Validate on a supported GrapheneOS Pixel and a physical x86_64 Android target before a public support claim.
- [ ] Pass the full phone, tablet, docked, GPU, document, multimedia, accessibility, failure, and release-signing matrix.

## P2 - Documentation and publication

- [x] Update README, architecture, security, roadmap, and storage documentation to the approved shared-environment model; clearly mark older per-wrapper runtime-pack results as historical.
- [x] Document the Android/Linux filesystem boundary, `/mnt/android`, backup/export, permissions, revocation, and uninstall consequences.
- [x] Document normal-Arch compatibility limits imposed by Android's kernel, SELinux, seccomp, background execution, and lack of root/systemd assumptions.
- [ ] Update README, roadmap, project status, compatibility matrix, security model, changelog, and release notes after behavior is implemented.
- [ ] Run the final public-repository, provenance, licensing, secret, reproducibility, CI, and release-artifact audit.

## Later - ArchpheneOS

Start only after the Android application reaches its release gate and the user approves the next project.

- [ ] Boot an x86_64 VM into an AOSP/GrapheneOS-derived laptop image and validate its security model.
- [ ] Define which GrapheneOS guarantees depend on supported Pixel hardware and cannot transfer directly to generic x86 systems.
- [ ] Design explicit Linux administration and escalation while Android applications remain sandboxed.
